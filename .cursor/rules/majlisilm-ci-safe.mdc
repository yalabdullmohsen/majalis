---
description: بروتوكول منع فشل GitHub CI قبل الدفع والدمج والنشر
alwaysApply: true
---

# المجلس العلمي — CI-safe قبل الدمج

## قبل أي تعديل
1. ابدأ من جذر git الحقيقي فقط:
   `cd "$(git rev-parse --show-toplevel)"` ثم `git status --short`
2. لا تخمّن المسارات — تحقق بوجود الملفات قبل التعامل معها:
   ```bash
   test -f package.json
   test -f pnpm-workspace.yaml
   # تطبيق الويب/Capacitor داخل monorepo:
   test -d artifacts/majalis/ios/App || true
   test -f artifacts/majalis/ios/App/App.xcodeproj/project.pbxproj || true
   test -f artifacts/majalis/ios/App/App.xcworkspace/contents.xcworkspacedata || true
   ```
3. اقرأ ملفات التحكم ذات الصلة أولاً: `package.json`، `pnpm-workspace.yaml`، `.github/workflows/*`، سكربتات gates/`verify:ci`، وملفات Capacitor/iOS/Android عند التعديل الأصلي.
4. لا تضعف الاختبارات ولا تحذف checks ولا تجعل الفحص optional لإخفاء خطأ — أصلح السبب الجذري.
5. PR واحد بهدف واحد (UI / مصحف / أذان / SEO / iOS / بيانات) — لا خلط نطاقات.

## zsh / glob
لا تستخدم:
`artifacts/majalis/ios/App/App/Sounds/adhan-short*.caf`
استخدم:
`find artifacts/majalis/ios/App/App/Sounds -name 'adhan-short*.caf' -print`
أو اقتبس النمط.

## iOS / Android
- ابنِ الويب أولاً: `PORT=24216 BASE_PATH=/ pnpm --filter @workspace/majalis run build`
- ثم من `artifacts/majalis`: `npx cap sync ios`
- تحقق من وجود `project.pbxproj` / workspace قبل التعديل.
- الأصوات/الموارد يجب أن تكون في Copy Bundle Resources فعلياً.

## قبل الدفع (إلزامي)
```bash
corepack enable
pnpm install --frozen-lockfile
pnpm run verify:ci
# أو مع تضييق: pnpm run verify:ci -- --changed
# UI: --ui · native: --native · بلا مصحف: --no-mushaf
```
عند فشل أي أمر: **لا commit ولا push** — اقرأ الخطأ، أصلح، أعد نفس الأمر حتى ينجح.

## بعد الدفع
```bash
gh pr checks --watch --fail-fast
# عند الفشل:
gh run view --log-failed
```
لا دمج ولا نشر إلا بعد نجاح على الأقل:
- Verify build
- repo-gates
- build
- static-checks
- Color contrast إذا كان التعديل UI
- بوابات iOS/Android إذا كان التعديل native

`verify:pr` مسار أسرع جزئي؛ **`verify:ci` هو البوابة الإلزامية قبل الدفع**.
