# Fastlane Appfile — مجلس العلمي (Majalis Al-Ilm)
# Values are resolved from environment / GitHub Actions secrets at runtime.

app_identifier(ENV.fetch("APP_IDENTIFIER", "com.yousef.majlisilm"))
apple_id(ENV.fetch("APPLE_ID", ""))
itc_team_id(ENV.fetch("ITC_TEAM_ID", ""))
team_id(ENV.fetch("APPLE_TEAM_ID", "5D8TX37HTS"))
