# خطة استئناف — تدقيق محتوى "المجلس العلمي" (فرع majalis-content-fill)

> اقرأ هذا الملف أولاً قبل أي استئناف. ثم اقرأ `AUDIT_INVENTORY.md` بجذر
> المستودع للسياق الكامل. التكليف الأصلي الكامل (16 مرحلة) موجود في تعليمات
> النظام لهذه الجلسة — لم يُنسخ هنا لتجنّب التكرار، فقط الحالة والخطوة التالية.

## ▶️ استؤنف صراحة من المالك ("اكمل") — العمل جارٍ تلقائيًا

أمر الإيقاف السابق (كان هنا) أُلغي صراحة برسالة لاحقة من المالك. الجلسات
الرابعة والخامسة والسادسة نُفِّذت تلقائيًا دون توقف حسب هذا الملف. **لا
تُعِد قراءة هذا كإيقاف — استمر من قسم "المهمة التالية بدقة" أسفل الملف.**

آخر حالة مؤكدة (الجلسة السادسة، جارية): ثلاثة مسارات تعليمية بُنيت
بالكامل بنفس النمط (3 مراحل لكل مسار) وطُبِّقت على DB الحية — `uloom-quran`
(علوم القرآن)، `akhlaq` (الأخلاق)، `adab` (الآداب، بتمييز موضوعي متعمَّد
عن akhlaq) — كلها `status`: `draft`→`published` — إضافة لـPhase 6: 5 كتب
جديدة مُتحقَّقة فردياً في `library-catalog.ts` (117→122). البنية التقنية
(typecheck+lint+test:regression+build) نجحت معًا دفعة واحدة على كل
التغييرات. راجع قسم "الجلسة السادسة" أدناه للتفصيل الكامل — commit جارٍ
بعد هذا التحديث مباشرة.

## 📋 خلاصة موحَّدة — الحالة الحالية (آخر تحديث 2026-07-18، بعد 20+ دفعة)

> **هذا القسم هو نقطة الدخول الوحيدة الموصى بها لأي استئناف جديد.** كل ما
> تحته من أقسام مفصَّلة (بدءاً من "ماذا أُنجز فعليًا 3 جلسات" وحتى نهاية
> الملف) هو **سجل تاريخي/تفصيلي** لكل دفعة على حدة — مفيد للرجوع لتفصيل
> استشهاد بعينه أو قرار تصميم محدَّد، لكن **غير ضروري لفهم الحالة العامة**؛
> اقرأه فقط إن احتجت تفصيل قرار بعينه ذُكر هنا بإيجاز. لا تُعِد تلخيصه
> مجدداً في استئناف قادم — أضف فقط قسم جلسة جديد أسفله إن استؤنف العمل،
> وحدِّث الأرقام في هذا القسم الموحَّد.

**آخر commit مدفوع فعلياً**: `0b6ed612` (بعد `183f2e95`←`96ed51a6`←`9477d665`←`cca48c58`←
`1e42f0d6`←`c575ea42`←`0c9f2c76`←`218f6547`←`2c3eb274`←`8e0e2d14`←
`b3e66c12`←`49eee100`←`a06ba3d1`←`48286977`←`68344bc1`←`967f14c4`←
`d3c326b4`←`135d5ddd`←`5d3eb8cc`←`dfa7df67`←`30a3c8fe`←`8009bab8`←
`bbba9372`←`90b333fd`←`c0ad0121`←`3a4c432d`←`65bca303`←`239edf6e`←
`139b5228`←`6bda7e0e`←`d50e988f`←`74adbc42`←`f9c07987`←`8336a700`←
`52620aaf`←`3f7e87d3`←`9e8a4856`←`47e3cb82`←`8ff7dd6a`←...).
فرع `majalis-content-fill` فقط — **ممنوع الدمج لـmain، ممنوع النشر للإنتاج**.

### ✅ جلسة عاشرة — استئناف "courses بلا course_books/items ≤2" بعد إغلاق شبه كامل للجولة السابقة (commits `1e42f0d6`→`96ed51a6`)

بطلب المنسّق ("اختر أكبر فجوة حقيقية متبقية"): أعدتُ تشغيل استعلام
`HAVING count(li.id) <= 2` على كل `courses` منشورة (لا `course_books`
هذه المرة، بل عدد العناصر التعليمية نفسها) فكشف **16 مقرراً** لا يزال
ضحلاً. عالجتُ 4 منها بنفس منهجية الجلسات السابقة (تحقُّق مباشر عبر
WebFetch قبل أي إضافة، فحص عدد العناصر قبل توزيع الوزن، تحقُّق
`SUM(weight)=100` و`SUM(session_estimate)` يطابق `total_sessions`
الحي مباشرة بعد كل تعديل):

1. **`adab-al-usrah` (مسار adab) — إصلاح تكرار حقيقي، لا إضافة عدد**:
   عنصر "حق الجار" كان موجوداً بنفس العنوان **حرفياً** في كورسين مختلفين
   من نفس المسار (`adab-al-usrah` و`adab-al-mujtama`) — فحص برمجي شامل
   لكل عناوين `learning_items` عبر كل المسارات (`GROUP BY title HAVING
   count(*)>1 AND عدد كورسات مختلفة>1`) كشف هذه الحالة الوحيدة الحقيقية
   (حالة ثانية ظاهرية في `kitab-tawheed`/`kitab-tawheed-full` فُحصت
   ووُجدت غير مشكلة — عنوان عام "شرح كتاب التوحيد" لكورسين يغطيان أبواباً
   مختلفة تماماً من الكتاب، لا تكرار محتوى فعلي). العنصر في `adab-al-usrah`
   لم يكن له حتى استشهاد حديثي محدد (وصف عام فقط)، وموضوعياً خارج نطاق
   "آداب الأسرة" أصلاً (الجيرة لا الأسرة). استُبدل بـ"صلة الرحم" (حديث
   أبي هريرة "من كان يؤمن بالله واليوم الآخر..." — متحقَّق عبر WebFetch من
   نص صحيح البخاري الكامل على ويكي مصدر، كتاب الأدب).
2. **`qawaid-muthla` (مسار aqeedah)**: عنصر عام واحد → 4 (+ 3 أبواب
   الكتاب الحقيقية لابن عثيمين: الأسماء [7 قواعد]/الصفات [7 قواعد]/الأدلة
   [4 قواعد] — هيكل الكتاب ومثال قاعدة حرفي لكل باب تحقَّقا عبر WebFetch
   من ويكيبيديا العربية). `aqeedah.total_sessions`: 74→77.
3. **`minhaj-muslim` (مسار fiqh)**: عنصر عام واحد → 6 (+ الأبواب الخمسة
   الحقيقية لكتاب الجزائري بترتيبها الأصلي: العقيدة/الآداب/الأخلاق/
   العبادات/المعاملات — هيكل الكتاب تحقَّق عبر WebFetch من ويكيبيديا
   العربية). `fiqh.total_sessions`: 114→119.
4. **`seerah-deep` (مسار seerah)**: عنصر عام واحد → 4 (+ 3 أحداث مدنية
   متأخرة لم تُغطَّ في `seerah-intro` [مكية مبكرة] أو `seerah-maghazi-
   kubra` [الغزوات]: الهجرة إلى المدينة، صلح الحديبية، حجة الوداع — كل
   حدث بتواريخه الهجرية/الميلادية الدقيقة تحقَّق عبر WebFetch من ويكيبيديا
   العربية). `seerah.total_sessions`: 38→41.
5. **`arabic-mujam-wasit` (مسار arabic)**: عنصر واحد → 3 (+ نشأة المعجم
   الوسيط ومنهج ترتيبه [مجمع اللغة العربية بالقاهرة، 1960م] وميزته
   المقارنة بالمعاجم التراثية — تحقَّق عبر WebFetch من ويكيبيديا العربية).
   `arabic.total_sessions`: 3→5.

**محاولات فُحصت ولم تُنفَّذ (انضباط، لا تسرّع)**: `arabic-durus-nahwiyya`
(WebFetch على مقالة "جامع الدروس العربية" لم يُرجع تفصيلاً هيكلياً كافياً
— لا كتاب أُعرف تحديداً محتواه) و`arabic-asrar-balagha` (WebFetch على
مقالة "بلاغة" أرجع تعريفاً متناقضاً مع التصنيف الكلاسيكي المعروف [علم
البيان/علم المعاني بدت معكوسة] — رُفض عدم الثقة بمصدر غير متسق داخلياً).
كلاهما مؤجَّل لجلسة قادمة بمصدر أوثق.

**تكملة فورية (commit `0b6ed612`)**: عُمِّق `tafsir-general` (مسار
tafseer) أيضاً — عنصر عام واحد (مربوط بتفسير ابن كثير) → 3 عناصر (+
تفسير سورة الفاتحة وآية الكرسي، أعظم سورة وأعظم آية، لم تُغطَّيا في أي
كورس آخر من المسار [الكهف/النحل/يوسف] — حديث الخِداج لمسلم وحديث أُبيّ بن
كعب لمسلم وقصة أبي هريرة والشيطان للبخاري، كلها تحقَّقت عبر WebFetch من
ويكيبيديا العربية). `tafseer.total_sessions`: 47→49.

**11 مقرراً ضحلاً (≤2 عنصر) آخر متبقٍّ** من أصل 16 المكتشَفة هذه الجلسة
(القائمة الكاملة: `adab-al-usrah` [بقي عند عنصرين بعد إصلاح التكرار —
يحتاج عنصراً حقيقياً ثالثاً لا مجرد إزالة تكرار]، `fiqh-women`،
`fiqh-shafii`، `fiqh-hanafi`، `talkhees-muqni`، `zad-mustaqni`،
`fiqh-ibadat`، `arabic-durus-nahwiyya`، `arabic-asrar-balagha`،
`sunnah-vs-science`، `nahw-sarf-marah`).

**اكتشاف مهم لم يُعالَج بعد (يحتاج قراراً محتملاً من المالك، لا إصلاح
تقني بسيط)**: فُحصت جداول `arbaeen_love_of_allah` (35 حديثاً كاملة
النص/المصدر/رقم الحديث) و`week_day_facts` (5 صفوف) — كلتاهما تُستهلَكان
فعلياً من صفحات حية (`/hadith/arbaeen-love-of-allah`، ودّة الصفحة
الرئيسية `HomeMajlisToday`). **`arbaeen_love_of_allah`: 35 من 35 صفاً
بحالة `in_review`، صفر صف `published`** — الصفحة الحية تعرض فارغة
بالكامل رغم اكتمال 35 من 40 حديثاً نصياً. **لكن هذا على الأرجح تصميم
مقصود لا بق**: الكود (`arbaeen-love-service.ts`، `ArbaeenLoveSection.tsx`)
يفرض صراحة أن الانتقال لـ`published` لا يكون إلا من `verified` بعد
"مراجعة عالِم شرعي فعلي لدرجته وصحة نسبته" — نص الواجهة نفسه يؤكد هذا
للزوار. **لم أُغيِّر `review_status` لأي صف** (لا أستطيع تعبئة `grade`/
`verified_by` بمصداقية بصفتي عاملاً آلياً بلا مراجعة عالِم حقيقي — يخالف
قيد "لا ترجيح شخصي/لا تصعيد نيابة عن مراجعة بشرية مقصودة"). مرشَّح **قرار
مالك** لا عمل تقني: إما تكليف مراجعة بشرية فعلية، أو توضيح إن كانت الحالة
الحالية (35 حديثاً جاهزاً غير منشور) مقبولة مؤقتاً. `week_day_facts`
مشابه لكنه أقل حدة (4 من 5 صفوف منشورة فعلاً، أي المراجعة تحدث فعلياً هنا
لكنها بطيئة — 4 أيام من 7 بلا أي مادة إطلاقاً).

كل دفعة من الخمس اجتازت البوابات الأربع (typecheck+lint+test:regression
16 حزمة+build) معًا قبل الـcommit، ودُفعت فور نجاحها (بعض عمليات
commit/push استغرقت عدة دقائق بسبب git hooks تُعيد تشغيل البناء الكامل —
طبيعي في هذه البيئة، ليس فشلاً).

### ✅ جلسة تاسعة (تكملة عاشرة) — باب علمي عميق ثانٍ: 4 أحاديث محورية من الأربعين النووية (commit `8ff7dd6a`)

بطلب المنسّق ("مقرر آخر بـ≤2 عنصر يستحق تعميقًا"): مقرر "الأربعون
النووية" (مسار hadith) كان بعنصر واحد عام. عُمِّق بنفس نمط قصص سورة
الكهف: **4 عناصر جديدة لأربعة أحاديث محورية متباعدة عمداً** (الحديث 1:
الأعمال بالنيات — متفق عليه؛ الحديث 6: الحلال بيّن والحرام بيّن — مسلم؛
الحديث 34: تغيير المنكر — مسلم؛ الحديث 42: من عادى لي ولياً — حديث قدسي،
البخاري، خاتمة المجموعة)، **تجنَّبتُ عمداً حديث جبريل (رقم 2) لأنه
مُستخدَم فعلاً في مقرر أركان الإيمان الستة بمسار aqeedah** — لا تكرار.

**سلامة الاستشهاد بنمط مختلف هذه المرة**: النصوص لم تحتَج WebFetch
جديداً — أُخذت حرفياً من `src/lib/arbaeen-nawawi-seed.ts` (بيانات محلية
مُثرَاة تُعرَض فعلياً في صفحة `/arbaeen-nawawi` الحية، نفس النص الذي
يراه الزوار بالفعل — مصدر محلي موثوق أعلى موثوقية من أي جلب خارجي جديد).

نفس اتفاقية weight/`total_sessions` — تحقَّق مباشر: `hadith.total_sessions`
102→106، يطابق `SUM(session_estimate)` الحي تماماً.

بطلب المنسّق ("أي باب علمي إضافي بنفس نمط الأبواب العميقة السابقة"):
مقرر `tafsir-kahf` (مسار tafseer) كان بعنصر واحد عام (مرتبط بتيسير
الكريم الرحمن للسعدي يغطي السورة كاملة، أُضيف الجلسة السابقة). عُمِّق
بنفس نمط "أركان الإيمان الستة" الناجح: **4 عناصر جديدة، كل واحد لإحدى
القصص الأربع الشهيرة في السورة** (أصحاب الكهف، صاحب الجنتين، موسى
والخضر، ذو القرنين)، كل عنصر مربوط بتفسير ابن كثير (المرجع القياسي
لقصص القرآن) بنطاق آيات دقيق.

**سلامة الاستشهاد**: حدود الآيات الأربع (9-26، 32-44، 60-82، 83-98)
تحقَّقت مباشرة بقراءة الآيات الحدّية فعلياً من `public/data/quran/
surah-018.json` (لا افتراضاً من الذاكرة). حديث فضل حفظ عشر آيات من
أول/آخر السورة (العصمة من الدجال، رواه مسلم عن أبي الدرداء) تحقَّق عبر
WebFetch — **لم يُذكر رقم دقيق** لعدم التمكن من تأكيده حرفياً، اكتُفي
بـ"رواه مسلم" (انضباط عدم الادّعاء بما لم يُتحقَّق منه بدقة).

**اكتشاف تقني أثناء التنفيذ**: `learning_items.weight` تتبع اتفاقية
تجمع 100 لكل عناصر الوحدة (وحدة بعنصر واحد → weight=100 لذلك العنصر) —
أُعيد توازنها لجميع العناصر الخمسة (20 لكل عنصر) بعد الإضافة، وهذا
يفسّر لماذا `total_sessions` (المكتشَف سابقاً هذه الجلسة أنه
`SUM(session_estimate)`) ظل متسقاً: 39→43، **تحقَّق مباشر أنه يطابق
المجموع الحي تمامًا**.

بطلب المنسّق ("مزيد من عمق المكتبة"): فُحص توزيع الفهرس فعلياً (127 كتاباً
عبر 13 تصنيفاً)، فتبيَّن أن **"منطق وفلسفة" (2 فقط) و"علوم القرآن" (4
رغم مسار uloom-quran الكامل المنشور)** أرقّ تصنيفين. أُضيف كتابان
لمنطق وفلسفة (**تهافت الفلاسفة** للغزالي، **السلم المنورق** للأخضري —
يكمّلان الموجودَين مسبقاً «درء تعارض العقل والنقل» و«إيساغوجي» بزاوية
مختلفة/نظماً له) وكتاب لعلوم القرآن (**أسباب النزول** للواحدي — أقدم
مصنَّف مستقل في بابه، **مع ذكر صريح للنقد المعروف عليه**: روايات ضعيفة
وواهية لعدم تخصص المؤلف في علل الحديث — لا عرض بلا تحفظ). كل كتاب تحقَّق
فردياً عبر WebFetch (WebSearch لا يزال منتهياً، لم يُعَد اختباره هذه
الدفعة تحديداً بناءً على توجيه سابق بعدم التكرار كل دفعة). `library-
catalog.json` أُعيد توليده فوراً (127→130، فحص `JSON_TS_DRIFT` الدائم
تحقَّق: صفر مخالفة).

بطلب المنسّق: "آداب طلب العلم" — تربط الصفحة المرجعية الموجودة مسبقاً
`/adab-talab-ilm` (غنية، عشرات الآداب مفصَّلة) بمسار `adab` التعليمي
(منشور هذه الجلسات) وكتاب حقيقي مربوط فعلياً بعناصره (`الأدب المفرد`
للبخاري، `book-adab-al-mufrad`). **سلامة الاستشهاد**: آيتا المجادلة:11
وطه:114 حرفياً من الملفات المحلية، حديث «من سلك طريقاً...» (مسلم 2699)
تحقَّق عبر WebFetch من نص صحيح مسلم الكامل (WebSearch لا يزال منتهياً،
تحقَّق مجدداً أول هذه الدفعة). **حديث «طلب العلم فريضة على كل مسلم»
(ابن ماجه) ذُكر مع تصريح صريح بخلاف العلماء في تصحيح سنده** — لم يُقدَّم
كمُسلَّم به، الاعتماد الأصلي على حديث مسلم الصحيح فقط (انضباط "عند خلاف
علمي: اعرض الأقوال بأمانة، لا ترجيح شخصي" مُطبَّق حرفياً).

طُبِّقت مباشرة على DB الحية (`INSERT` في جدول `articles`)،
`is_approved: false` (نفس نمط المقالتين السابقتين). **تحقَّق مباشر**:
`SET LOCAL ROLE anon` → 0 صف، RLS يعمل كما هو مصمَّم. **جدول `articles`
الآن 3 مقالات** (كان 2).

### ✅ جلسة تاسعة (تكملة سادسة) — إغلاق البند الصغير المتبقي + تحقُّق articles/universities (بلا commit كود — DB فقط)

بطلب المنسّق، ثلاث خطوات ختامية لمنهجية "تتبُّع المسار حتى القاعدة الحية":

1. **9 صفوف `sharia_rulings` يتيمة بتصنيفات بائدة (الحج/المواريث بلا
   "والعمرة"/"والوصايا")** — أُصلحت مباشرة بـ`UPDATE` بسيط (لا ملف كود
   متأثر، الصفوف لم يكن لها أي مقابل في أي seed محلي أصلاً): `الحج`→
   `الحج والعمرة` (44 صفاً الآن)، `المواريث`→`المواريث والوصايا` (10
   صفوف الآن). **تحقَّق نهائي**: استعلام `NOT IN` بكل الـ20 تصنيفاً
   الحالية رجع **صفر صف** — لا تصنيف بائد متبقٍ في `sharia_rulings` إطلاقاً.
2. **جدول `articles` (المقالتان الأصليتان)**: تحقَّق مباشر — كلتا
   المقالتين موجودتان حيّاً (`is_approved: false` لكلتيهما، كما هو
   موثَّق ومقصود). **RLS مُتحقَّق مباشرة**: سياسة `articles_public_read`
   تشترط `is_approved = true` حرفياً — أي زائر مجهول لا يرى أيّاً منهما،
   مطابق تمامًا للتوثيق السابق ("لا صفحة عرض علنية بعد، قرار متعمَّد").
   **المستهلِك الوحيد في src/**: `ContentFileImport.tsx` (أداة استيراد
   إدارية، لا صفحة عرض عامة) — لا فجوة، كل شيء يعمل كما هو مُصمَّم بالضبط.
3. **جدول `universities`**: تحقَّق مباشر — 15/15 منشورة (`is_published=
   true`)، مطابق تمامًا للموثَّق. المستهلِك الحي `lib/api-handlers/
   universities.js` يفلتر بنفس الشرط فعلياً (مؤكَّد من فحص سابق هذه
   الجلسات). **لا فجوة هنا أيضاً**.

**الخلاصة الكلية لمنهجية "تتبُّع المسار حتى القاعدة الحية" عبر 7 أنظمة
بيانات فُحصت هذه الجلسة**: 2 معطَّلان فعلياً بعطل خطير (quiz_questions،
sharia_rulings — كلاهما مُصلَح ومُزامَن بالكامل)، 1 انجراف تافه
(aqeedah.total_sessions، مُصلَح)، 1 تنظيف بيانات يتيمة صغير (9 صفوف
أحكام، مُصلَح)، 3 سليمة تماماً بلا أي تدخل (scholars، knowledge_
relationships، articles، universities — أربعة فعلياً). **لم يبقَ نظام
بيانات مبني بكثافة هذه الجلسة لم يُتحقَّق منه مباشرة مقابل القاعدة
الحية.**

### 🚨🚨🚨 جلسة تاسعة (تكملة خامسة) — نفس منهجية "تتبع المسار حتى القاعدة الحية" طُبِّقت على كل ما بُني هذه الجلسة (commit `21068504`)

بطلب المنسّق بعد اكتشاف عطل quiz_questions: فحصتُ **كل** ما بُني بكثافة
هذه الجلسة بنفس الشك المباشر (مقارنة فعلية بالـDB الحية، لا افتراض):

1. **`scholars-data.ts` (96 عالماً، 32 أُثريَ هذه الجلسة)**: تحقَّقتُ —
   `ScholarProfilePage.tsx`/`IslamicScholarsPage.tsx` يستوردان `SCHOLARS`
   مباشرة من الملف الثابت، **صفر اعتماد على أي جدول Supabase**. لا فجوة
   مزامنة ممكنة أصلاً هنا — الإثراء حيّ بالكامل فور الدمج/البناء.
2. **`knowledge_relationships` (153 صفاً)**: تحقَّق مباشر — العدد يطابق
   الموثَّق حرفياً (32+56+6+5+54).
3. **`learning_paths.total_sessions`**: اكتُشف أن هذا العمود **مجموع
   `session_estimate`** عبر عناصر المسار (ليس عدد العناصر — تحقَّقتُ
   ببرهان: `SUM(session_estimate)` لـaqeedah = 58، مطابق تقريباً لا
   `COUNT(learning_items)` = 13). **كل المسارات مطابقة تماماً ما عدا
   aqeedah** (57 مُخزَّن مقابل 58 حيّ فعلياً — انجراف بوحدة واحدة، على
   الأرجح من تعديل لاحق لعنصر واحد بلا تحديث العدّاد المخزَّن) — **صُحِّح
   مباشرة** (`UPDATE learning_paths SET total_sessions=58 WHERE
   slug='aqeedah'`).
4. **موسوعة الأحكام (`sharia_rulings`)**: 🚨 **أكبر اكتشاف ثانٍ في هذه
   الجلسة بعد quiz_questions** — راجع القسم أعلاه (commit `21068504`)
   للتفصيل الكامل: `rulings-service.ts` يُصرِّح صراحة أن DB هي "single
   source of truth at runtime"، وجدول `sharia_rulings` كان عند 268 صفاً
   قديماً (يفتقد كل الـ12 حكماً الجديدة لهذه الجلسة + على الأرجح عشرات
   الإصلاحات السابقة) بسبب عطل `verification_status` CHECK constraint
   حقيقي كان سيُسقِط أي مزامنة مستقبلية شرعية صامتاً. أُصلح الكود
   (`lib/rulings-db-seed.mjs`) + طُبِّقت مزامنة فعلية كاملة (527 حكماً):
   **`sharia_rulings`: 268→690 صفاً**، تحقَّق مباشر أن الـ12 حكماً الجديدة
   حيّة الآن بتصنيفاتها الصحيحة.

**الخلاصة**: من أصل 5 أنظمة بيانات فُحصت بنفس المنهجية، **2 كانا معطَّلين
فعلياً بعطل حقيقي خطير** (quiz_questions، sharia_rulings)، **1 له انجراف
تافه بوحدة واحدة** (aqeedah.total_sessions)، **2 سليمان تماماً** (scholars،
knowledge_relationships). درس نهائي: "تتبُّع المسار الكامل حتى القاعدة
الحية بفحص مباشر، لا افتراض" أثبت قيمته القصوى مرتين متتاليتين في نفس
الجلسة — يستحق أن يكون الخطوة الأولى الإلزامية لأي جلسة قادمة قبل الثقة
بأي ادّعاء "تم إنجازه" من سجل commits سابق.

### 🚨🚨 جلسة تاسعة (تكملة رابعة) — أكبر اكتشاف في هذه الجلسة الطويلة: 943 سؤال مسابقة لم تصل اللاعبين إطلاقاً (commit `51d51fe2`)

بطلب المنسّق ("لا تفترض نظافة أي مصدر بيانات دون تحقق مباشر")، تتبَّعتُ
مسار بيانات لعبة "الأسئلة الجاهزة" (`IslamicQuizGame.tsx`) من الواجهة
حتى قاعدة البيانات — بلا افتراض. **جدول `quiz_questions` الحي كان به 15
صفاً اختبارياً قديماً فقط** رغم أن `src/lib/quiz-seed.ts` (المُثرى عبر
عشرات الدفعات طوال هذه الجلسة الطويلة) وصل **943 سؤالاً**. الغالبية
الساحقة من محتوى بنك الأسئلة **لم تكن مرئية لأي لاعب حقيقي إطلاقاً**.

**عطل من 3 طبقات متراكبة، كل واحدة كانت تُسقِط المزامنة صامتاً بلا أي
خطأ ظاهر للمستخدم النهائي:**
1. `upsertQuizSeedToDb()` (زر إداري حقيقي في `QuizSection.tsx`) يُحوِّل
   `level` إلى إنجليزي بينما CHECK constraint حي يقبل عربي فقط — أي
   محاولة مزامنة كانت تفشل فوراً (تحقَّقتُ بمحاولة إدراج فعلية، رسالة
   الخطأ الصريحة من Postgres أكَّدت هذا حرفياً).
2. `quiz_questions_section_check` كان يقبل 6 قيم إنجليزية ضيقة فقط —
   تصميم مبكر لا يطابق الأربعين قيمة `section` العربية الفعلية في
   quiz-seed.ts (كلها مُعرَّفة فعلياً وتعمل في `SECTION_TO_CATEGORY`،
   تحقَّق برمجياً: صفر قيمة غائبة).
3. `LEVEL_TO_POINTS` في `islamicQuizData.ts` يقبل إنجليزي فقط — حتى لو
   نجحت المزامنة، كل الأسئلة كانت لتسقط في نقاط 400 الافتراضية بصمت.

**الإصلاح**: `ALTER CONSTRAINT` حي يوسِّع `section` (الأربعين + الستّ
الأصلية للتوافق) + مزامنة يدوية فعلية (`scripts/gen-quiz-sync-sql.mjs`
جديد، `ON CONFLICT (question) DO NOTHING` على قيد UNIQUE حي تحقَّقتُ منه
مباشرة) — **`quiz_questions`: 15→953 صفاً** — + إصلاح `upsertQuizSeedToDb()`
(level: سهل→أساسي فقط، `.insert()`→`.upsert(...,{ignoreDuplicates:true})`
ليعمل الزر الإداري صحيحاً لأي إضافة مستقبلية) + توسيع `LEVEL_TO_POINTS`
بالمفاتيح العربية. **تحقُّق نهائي حاسم**: استدعاء `mergeSupabaseQuestions()`
فعلياً بعينة من الصفوف الحية المُزامَنة أثبت وصولها للفئة الصحيحة
وبالنقاط الصحيحة بالضبط — السلسلة الكاملة تعمل الآن من DB حتى واجهة اللعبة.

**درس منهجي حاسم لأي جلسة قادمة**: عدد الأسئلة/الكتب/العلماء في ملف
المصدر (`*-seed.ts`) **لا يعني أبداً** أنها مرئية فعلياً في أي تجربة
مستخدم حية تعتمد على DB وسيطة — تتبَّع دائماً السلسلة كاملة (ملف مصدر ←
دالة مزامنة/API ← قيود DB الفعلية ← منطق دمج/عرض العميل) بفحص مباشر لكل
حلقة، لا افتراض أن أي حلقة منها سليمة لمجرد عدم ظهور خطأ واضح.

### 🚨 جلسة تاسعة (تكملة ثالثة) — buildFeedXml كان يَعِد بـ"فتاوى وقرارات" ولا يجلبها (commit `76200fd9`)

بطلب المنسّق: راجعتُ `buildFeedXml()` (نفس `lib/cms/sitemap-builder.mjs`
المُصلَح لعطل `scholars-list.json`) بحثاً عن نفس فئة العطل بمصدر بيانات
آخر. **النتيجة الحرفية: لا — buildFeedXml لا يقرأ أي لقطة JSON ثابتة
إطلاقاً** (استعلام Supabase حي فقط على جدول `lessons`)، فهو سليم من نمط
"المرآة المجمَّدة" حرفيًا. **لكن اكتُشف عطل من نفس العائلة الأعمق**: نص
وصف قناة RSS الثابت في نفس الدالة يقول حرفيًا "دروس **وفتاوى وقرارات**"
بينما التنفيذ كان يجلب الدروس فقط — جدول `fiqh_council_issues` (قرارات
المجمع الفقهي) حي وموعود به في واجهة القناة نفسها لكنه غير مُستعلَم
إطلاقاً. **تأكيد إضافي حاسم**: النسخة الثابتة القديمة لـ`feed.xml`
(المولَّدة عبر `generate-seo.mjs`، والتي عاملتها هذه الجلسة الطويلة
كـ"ضجيج طوابع زمنية" تُرجَع بـ`git checkout --` بعد كل بناء — **الفرضية
لا تزال صحيحة تقنيًا لأن `vercel.json` يُعيد توجيه `/feed.xml` لـ
`/api/feed` مباشرة نفس آلية `/sitemap.xml`**) كانت تتضمن فعلاً قرارات
مجمعية ودورات علمية — يؤكد أن النطاق الأوسع كان مقصودًا منذ البداية ثم
فُقِد عند الانتقال من المولِّد الثابت لمسار API الحي.

أُضيف استعلامان حيّان إضافيان (`fiqh_council_issues` بشرط
`status='published'`+`documentation_level='official_verified'` مطابقةً
لـ`fiqh-council-issues-service.ts`؛ `annual_courses` بشرط
`status='approved'` مطابقةً لـ`platform-content-service.ts`)، دُمجا مع
الدروس ورُتِّبا زمنيًا معًا. **تحقَّق مباشر عبر `supabase db query
--linked`**: استعلام `fiqh_council_issues` رجع 3 قرارات حقيقية حديثة
بنجاح؛ `annual_courses` رجع 0 صف (الجدول فارغ فعليًا حاليًا، ليس خطأ في
الاستعلام — نفس شرط الفلترة المُستخدَم في الواجهة الحية). `sitemap-
builder.mjs` ملف `.mjs` خارج نطاق `typecheck`/`lint` (يغطيان `.ts`/`.tsx`
فقط) — تحقَّقتُ بـ`node --check` (تركيب سليم) + الاستعلامات الفعلية
يدويًا. `test:regression` (16 حزمة، 0 فشل) + `build` نجحا كاملين (لا
تغيير في أي ملف TS يؤثر على البناء).

### 🚨 جلسة تاسعة (تكملة ثانية) — تدقيق شامل لكل بوابات test:*/audit-*/verify:* (commit `552e8a35`)

بطلب صريح من المنسّق بعد اكتشاف عطل `library-catalog.json`: فحصتُ **كل**
سكربت في سلسلة `test:regression` (16 حزمة) بنفس الشك — هل يقرأ لقطة JSON
مجمَّدة بدل المصدر الحي؟ **النتيجة: سليمة كلها إلا واحدة**:
- `test:content-counts` — يُعيد التوليد فعليًا ويقارن قبل/بعد (تصميم
  محصَّن أصلاً، لا لقطة مجمَّدة).
- `test:content-governance` (فحص `rulings-data`) — يقرأ من
  `public/data/rulings-encyclopedia/chunks/*.json` و`rulings-encyclopedia-
  seed.generated.ts`، وهما ملفان يُعاد توليدهما بأي `pnpm run build` —
  تحقَّقتُ فعليًا بتشغيل المولِّد يدويًا ثم الفحص: **746 سجلًا، 0 مخالفة**
  (المولِّد نفسه يُخفِّض `status` تلقائيًا لـ`pending_review` عند غياب
  `reviewed_by`، تصميم سليم من جلسة سابقة).
- بقية السكربتات (`scholars-integrity`، `sheikhs-dedup`، `library-
  integrity` [مُصلَح جلسة سابقة]، `inheritance-engine`، `learning-paths-
  engine`، `category-tree`، `quran-data`...) تستورد مصادرها حيةً مباشرة
  (عبر `tsx` أو قراءة نص خام محدَّث)، لا لقطات وسيطة.

**اكتشاف واحد أخطر بكثير من `library-catalog.json` نفسه**: وسّعت الفحص
خارج `test:regression` لأي ملف `src/data/*.json` يُستهلَك من كود حي —
`src/data/scholars-list.json` كان مجمَّداً عند **78 من 96 عالماً** (20
غائبون). الفرق الحاسم عن `library-catalog.json`: مستهلِكه
`lib/cms/sitemap-builder.mjs` **ليس كوداً ميتاً** — مسجَّل فعليًا في
`lib/api-dispatch.mjs` (`/api/sitemap`، `/api/feed`)، و`vercel.json`
يُعيد توجيه `/sitemap.xml` و`/feed.xml` صراحةً إلى هذين المسارين. أي أن
**خريطة الموقع الحقيقية المُرسَلة لمحركات البحث في الإنتاج** (لا
`public/sitemap.xml` الثابت الذي تعاملت معه الجلسة كضجيج طوابع زمنية —
قد لا يُخدَّم إطلاقاً بسبب هذا الـrewrite الصريح) كانت تفتقد 20 صفحة عالم
لأشهر بصمت تام. أُصلح بنفس نمط `library-catalog.json` تمامًا:
`scripts/regen-scholars-list-json.mjs` جديد + فحص انجراف دائم (القسم ١٠
في `scholars-data-integrity.test.ts`، يعمل ضمن `test:scholars-integrity`
الحالي فلا حاجة لتسجيل سكربت جديد في `package.json`).

**درس منهجي لأي جلسة قادمة**: لا تكتفِ بفحص `test:regression` وحدها عند
البحث عن هذا النمط — افحص أيضًا أي ملف JSON ثابت يقرأه كود `lib/`
المسجَّل فعليًا في `api-dispatch.mjs` (نقطة الدخول الحية لكل مسارات
`/api/*`)، فبعض الأعطال الأخطر لا تظهر في بوابات الاختبار إطلاقًا بل في
مسارات إنتاج منفصلة كليًا لا يفحصها أي `test:*` script قائم.

### 🎯 جلسة تاسعة (تكملة) — تدقيق مكتبة أعمق + ربط 8 مقررات فارغة (commits `3fc56ccf`، `43149f58`)

بطلب المنسّق بعد إغلاق الأولويتين (أحكام + علماء)، تابعتُ نحو منهجية
"courses بلا course_books" المُثبَتة — أعدت تشغيل نفس استعلام SQL الذي
استُخدم مرتين سابقاً فكشف **10 مقررات منشورة بلا كتاب واحد مربوط**: fiqh-
ibadat، fiqh-shafii، fiqh-women، fiqh-hanafi، talkhees-muqni (fiqh)،
seerah-intro، seerah-deep (seerah)، tafsir-general، tafsir-kahf،
tafsir-nahl (tafseer).

**اكتشاف معماري حاسم أثناء التحقق**: كل عناصر هذه المقررات (`item_type=
'lesson'`) كانت تشير فعلاً لصفوف حقيقية موجودة في جدول `lessons` (ليست
مراجع معطوبة) — لكن `content_ref_table`/`content_ref_id` **لا تُستعلَم
إطلاقاً** في `learning-paths-service.ts` (تأكيد مباشر لملاحظة معمارية
موثَّقة من الجلسة الثالثة)، فتُعرَض هذه الدروس بزر "إكمال" ذاتي التقرير
بلا أي محتوى مرتبط ظاهر — `course_books` هو الرابط الحقيقي الوحيد الذي
يُنتج بطاقة كتاب قابلة للنقر (`LearningPathDetailPage.tsx` يبني الرابط
عبر مطابقة نصية حرفية `LIBRARY_TITLE_TO_ID`، فـ`book_title` يجب أن يطابق
`title` في `library-catalog.ts` حرفياً).

**8 من 10 رُبطت بكتب حقيقية** (10 صفوف `course_books`، أحدها لكتاب جديد
كلياً "إعانة الطالبين على حل ألفاظ فتح المعين" للدمياطي — تحقَّق عبر
WebFetch، اسمه مطابق حرفياً لعنوان أحد الدروس). **2 استُبعدا عمداً**:
`fiqh-women` (لم يُعثر على كتاب فقه نسائي معروف بثقة كافية) و`talkhees-
muqni` (نفس القرار من جلسات سابقة — نتيجة WebFetch وحيدة منخفضة الثقة
لمؤلف غير مؤكَّد، لم تُستخدم). راجع `supabase/course_books_fiqh_seerah_
tafseer_gap_v1.sql` للتفصيل الكامل.

**اكتشافان جانبيان أثناء البحث عن كتب مناسبة (قيمة أكبر من الهدف الأصلي)**:
1. **تكرار محتوى حقيقي**: `book-minhaj-talibin` و`book-al-minhaj-nawawi`
   في `library-catalog.ts` نفس الكتاب (منهاج الطالبين، النووي) بعنوانين
   مختلفين قليلاً — أُزيل الأقدم (127→126، ثم 127 بعد إضافة إعانة
   الطالبين).
2. **انجراف صامت خطير بين ملفين متوازيين** (نفس عائلة عطل "أخلاق
   إسلامية" المتكرر طوال هذه الجلسات، لكن هذه المرة في بوابة اختبار لا
   في UI): `src/data/library-catalog.json` (الملف الذي يقرأه
   `test:library-integrity` ضمن `test:regression` على **كل commit**) كان
   مجمَّداً عند **102 كتاب** بينما `library-catalog.ts` الحي وصل **127**
   — **25+ كتاباً لم تُفحص إطلاقاً بهذه البوابة لأشهر**. `generate-seo.mjs`
   كان قد أُصلح مسبقاً لهذه المشكلة بعينها (تعليق موجود فيه يوثّقها) لكن
   الإصلاح لم يمتد لملف الفحص. أُعيد توليد الملف كاملاً
   (`scripts/regen-library-catalog-json.mjs` جديد، يحافظ على إثراء كل
   كتاب مطابق [authorId/sources/verificationStatus] ويضع افتراضات محايدة
   للجديد)، وكشف الفحص فوراً **11 كتاباً من الـ25 المهملة تحتوي عبارات
   مبالغ فيها بلا مصدر** ("أشمل موسوعة"، "لا غنى عنه"، "المرجع الأول"...)
   تخالف قاعدة `OVERCLAIM` القائمة أصلاً — صُححت أوصافها لصياغة محايدة.
   **فحص دائم جديد `JSON_TS_DRIFT`** أُضيف لـ`test-library-integrity.mjs`
   (regex على نص .ts خام، لا استيراد وحدة، يعمل بـ`node` عادي بلا `tsx`)
   يفشل الاختبار فوراً إن انجرف الملفان مجدداً — **لا يمكن لهذا الانجراف
   أن يتكرر صامتاً بعد الآن**.

### 🎯 جلسة تاسعة (2026-07-18) — الأولويتان المسجَّلتان "التالي بالضبط" أُنجزتا بالكامل

**WebSearch لا يزال منتهياً (تحقَّق أول الجلسة) — لكن `WebFetch` يعمل
فعلياً** واستُخدم كبديل تحقُّق حقيقي طوال هذه الجلسة (جلب صفحات ويكيبيديا
العربية وويكي مصدر مباشرة وقراءة محتواها الفعلي، وليس توليدًا من الذاكرة)
— **درس تشغيلي مهم لأي جلسة قادمة**: إن كان WebSearch منتهيًا، اختبر
`WebFetch` قبل الاستسلام للعمل الهندسي البحت؛ نجح هنا في كل من التحقق من
نصوص الأحاديث (مباشرة من صفحات صحيح البخاري/مسلم الكاملة على
`ar.wikisource.org`) وسير العلماء (`ar.wikipedia.org`).

**1) 6 تصنيفات فارغة في موسوعة الأحكام — مُلئت بالكامل (commit
`d516a914`):** القضاء والحدود، الأطعمة والأشربة، الجنائز، الجهاد
والسياسة الشرعية، اللباس والزينة، الأيمان والنذور — كل تصنيف كان **صفر
حكم شرعي** رغم وجود شريحة فلترة حية له على `/rulings` (فجوة مسجَّلة منذ
الجلسة الثامنة). أُضيف **12 حكماً جديدًا** (2 لكل تصنيف) في
`src/lib/rulings-seed.ts` — كل استشهاد قرآني استُخرج حرفيًا من
`public/data/quran/surah-*.json` المحلي (بايثون مباشر، لا من الذاكرة)،
وكل حديث حُقِّق عبر `WebFetch` من نص صحيح البخاري/صحيح مسلم الكامل على
ويكي مصدر (رقم الحديث والباب مذكوران، مثال: حديث الإسبال رقم 5450 وحديث
نصاب السرقة "ربع دينار" رقم 6407، كلاهما من `كتاب اللباس`/`كتاب الحدود`
مباشرة). **صفر تصنيف فارغ متبقٍ في موسوعة الأحكام الآن.**

**2) دفعات تاسعة-عاشرة-حادية عشرة-ثانية عشرة لإثراء العلماء — 96/96
مكتمل بالكامل (commits `b6b65564`→`c062408a`→`5f7bcea1`→`b3b4da2a`):**
كانت 32 عالمًا من 96 بلا `sources`/علاقات شيخ-تلميذ (64/96 مُثرى مطلع
الجلسة) — **الآن 96/96 لديهم `sources` موثَّقة و`verificationStatus:
"reviewed"`**. أُثري كل عالم عبر `WebFetch` مباشر من ويكيبيديا العربية
(شيوخه/تلاميذه/سنة وفاته/مؤلفاته)، لا من الذاكرة. **اكتشاف تحقُّق مهم
أثناء العمل**: معرِّف `ibn-aqil` في `scholars-data.ts` هو أبو الوفاء علي
بن عقيل البغدادي الحنبلي (ت٥١٣هـ) — أول محاولة جلب لصفحة ويكيبيديا
بعنوان "ابن عقيل" جلبت **شخصًا آخر تمامًا** (ابن عقيل النحوي المصري،
ت٧٦٧هـ) بنفس العنوان المختصر؛ اكتُشف الفارق قبل الكتابة وأُعيد البحث عن
المقالة الصحيحة ("أبو الوفاء بن عقيل") — **درس منهجي: تحقَّق دائمًا من
تطابق الهوية (التواريخ/المذهب/التخصص) لا الاسم المختصر فقط عند جلب صفحة
ويكيبيديا لعالم قديم، الأسماء المختصرة تتكرر بين علماء مختلفين تمامًا**.

**17 علاقة شيخ_تلميذ جديدة** اكتُشفت أثناء التحقق من كل بيو (كل زوج
تحقَّق أن معرِّفَيه موجودان فعلاً في `scholars-data.ts` وأن الزوج غير
موجود مسبقًا في DB قبل الإدراج، عبر 4 ملفات SQL منفصلة `_v3` إلى `_v6`):
`izz-ibn-abd-al-salam→ibn-daqiq`، `laith-ibn-sad→shafi`،
`ibn-ashur→ibn-badis`، `sufyan-uyayna→ibn-abi-shaybah`،
`ibn-taymiyya→ibn-muflih`، `al-mizzi→ibn-muflih`، `dhahabi→ibn-muflih`،
`ibn-baz→abu-zayd`، `al-mundhiri→ibn-daqiq`، `ibn-daqiq→dhahabi`،
`izz-ibn-abd-al-salam→al-qarafi`، `al-mundhiri→al-qarafi`،
`bukhari→ibn-al-mundhir`، `tirmidhi→ibn-al-mundhir`، `khattabi→hakim`،
`khatib-baghdadi→ibn-aqil`، `ibn-hajar→al-sakhawi`. عطل واحد اكتُشف
ومُعالج فورًا أثناء الإدراج: محاولة أولى لدفعة `_v4` فشلت بالكامل
(`duplicate key`, `shanqiti→abu-zayd` كانت موجودة مسبقًا) — **تحقَّقتُ أن
Postgres تراجَع عن الاستعلام بأكمله ذرّيًا (لا إدراج جزئي)** قبل إعادة
المحاولة بدون الزوج المكرر. `knowledge_relationships`: شيخ_تلميذ 39→56،
**الإجمالي 136→153**.

**درس تصميم موثَّق (اتُّبع باستمرار طوال الجلسة)**: `generate:seo`
(جزء من `pnpm run build`) يُعيد توليد `seo-prerender/scholars/*/index.html`
تلقائيًا من `scholars-data.ts` الحالي — هذه **محتوى حقيقي يعكس التغيير**
(وليس ضجيج طوابع زمنية كملفات `rulings-encyclopedia/*`/`feed.xml`/
`sitemap.xml`) فأُبقيت عمدًا في كل commit، بخلاف الثلاثة الأخيرة التي
أُعيدت لحالتها الأصلية بـ`git checkout --` بعد كل بناء كالمعتاد.

كل دفعة من الأربع اجتازت البوابات الأربع معًا (typecheck+lint+
test:regression 16 حزمة 0 فشل+build) قبل commit، ودُفعت فور نجاحها.

### ✅ تأكيد نهائي: لا انحدار حقيقي عبر 20+ دفعة (تدقيق مسارات فعلي، 2026-07-18)

بطلب المنسّق، شُغِّل `scripts/audit-all-routes.mjs` عبر Playwright ضد
`vite preview` فعلي (لا تخمين) على **236 مساراً مُختاراً بعناية**: كل
صفحة عُدِّلت هذه الجلسات (`/rulings`, `/library`, `/qa`,
`/knowledge-graph`, `/arbaeen-nawawi`, `/lessons`, `/miracles`,
`/stories`, `/fawaid`, `/universities`, `/occasions`, `/adhkar`) + **كل
الـ96 صفحة `/scholars/:id`** + **كل الـ127 صفحة `/library/:id`** — هذا
النطاق تحديداً لأنه يغطي كل ما لمسته دفعات هذه الجلسة الطويلة، ولأن
`/scholars` نفسها كانت مصدر الاكتشاف الأصلي لهذا النمط من الأعطال.

**النتيجة: 235 من 236 مساراً نظيفة تماماً** — توقيع مطابق حرفياً
لكل الـ235: 5 رسائل console متطابقة "resource 404" (نداءات API خلفية
مثل `/api/prayer-times` تفشل محلياً تحت `vite preview` بلا خادم
Express/Vercel serverless حقيقي — عطل بيئة اختبار محلية موثَّق مسبقاً في
تعليقات السكربت نفسه، **لا يحدث في الإنتاج الحقيقي**)، صفر `pageErrors`
(لا أخطاء JavaScript حقيقية)، صفر `isBlank`، عناوين وh1 صحيحة لكل صفحة.

**اكتشاف واحد بدا حقيقياً عند الفحص الأول** (`/scholars/abu-zayd`،
`status: 404` فعلي، عنوان/h1 فارغان تماماً) **تحقَّق منه فعلياً وثبت أنه
سباق توقيت بيئي بحت لا عطل تطبيق حقيقي**، عبر 3 تحققات مستقلة:
1. `abu-zayd` سجل حقيقي وسليم في `scholars-data.ts` (الشيخ بكر بن عبد
   الله أبو زيد، فقيه سعودي معاصر — **ليس** ابن أبي زيد القيرواني صاحب
   "الرسالة الفقهية" المُضافة كعلاقة `شرح_لكتاب` هذه الجلسة، رغم تشابه
   الاسم؛ لا علاقة بين الاثنين).
2. `pnpm run build` نظيف بعد ذلك ولّد `dist/scholars/abu-zayd/index.html`
   بعنوان صحيح تماماً ("الشيخ بكر أبو زيد — سيرة العالم") — تحقَّق مباشرةً.
3. **تحليل التوقيت يفسِّر الحادثة بدقة**: commit التوثيق `535000aa` (الذي
   دُفع أثناء تشغيل التدقيق نفسه) له pre-commit hook يُشغِّل `vite build`
   كاملاً، وهذا يحذف `dist/scholars/` بالكامل مؤقتاً قبل أن يُعيد
   `prerender.mjs`/`post-build-seo.mjs` تعبئته. توقيت الـhook (12:05:48)
   يطابق تقريباً اللحظة التي كان التدقيق يعالج فيها المسار رقم 46 من 236
   (`abu-zayd`، حسب ترتيب الملف) — طلب Playwright وصل بالضبط أثناء نافذة
   حذف `dist/scholars/` المؤقتة أثناء إعادة بناء متزامنة غير مرتبطة
   بالمحتوى إطلاقاً، لا بعطل في `abu-zayd` نفسه. **لم يلزم أي إصلاح كود.**

**الخلاصة**: 20+ دفعة هذه الجلسات الطويلة (روابط صامتة، تصنيفات غير
مرئية، علاقات معرفية، محتوى) **لم تُحدث أي انحدار حقيقي واحد** في أي من
الـ236 مساراً المُدقَّقة فعلياً، بما فيها كل صفحة عالم وكل صفحة كتاب في
الموقع بأكمله.

### أرقام المحتوى الحالية (تحقَّق منها آخر بناء ناجح)
- **96 عالماً (`scholars-data.ts`) — 96/96 مُثرى بالكامل** ببيانات
  شيخ/تلميذ ومصادر حقيقية (`sources` غير فارغ لأي عالم، `verificationStatus:
  "reviewed"` للجميع تقريباً) — **مكتمل عبر الجلسة التاسعة** (كان 64/96
  مطلعها، 32 متبقٍ أُثري عبر 4 دفعات `_v3`→`_v6`).
- **130 كتاباً في `library-catalog.ts`** (127 بعد إزالة تكرار "منهاج
  الطالبين" وإضافة "إعانة الطالبين"، +3 هذه الدفعة: تهافت الفلاسفة،
  السلم المنورق، أسباب النزول — استهدفت أرقّ تصنيفين: منطق وفلسفة
  وعلوم القرآن). `src/data/library-catalog.json` (الملف الموازي الذي
  يقرأه `test:library-integrity`) كان منجرفاً بشدة (102 فقط) وأُعيد
  مزامنته بالكامل + حُصِّن ضد الانجراف مستقبلاً (`JSON_TS_DRIFT` check).
- 515 فائدة في `fawaid-seed.ts` (كانت 508 مطلع الجلسات، +7 هذه الجلسات
  تُعيد استخدام استشهادات مُتحقَّقة سابقاً).
- 943 سؤال مسابقة في `quiz-seed.ts` — **و953 صفاً الآن في جدول
  `quiz_questions` الحي فعلياً** (كان 15 فقط، راجع قسم "أكبر اكتشاف" أعلاه)
  بعد إصلاح سلسلة أعطال المزامنة الثلاثية.
- **موسوعة الأحكام: صفر تصنيف فارغ** من 20 تصنيفاً في `RULINGS_CATEGORY_
  TREE` — **مكتمل عبر الجلسة التاسعة** (كانت 6 تصنيفات فارغة تمامًا،
  أُضيف 12 حكماً جديداً في `rulings-seed.ts`، راجع قسم "جلسة تاسعة" أعلاه).
- **153 علاقة معرفية** في `knowledge_relationships` (كان فارغاً كلياً 0
  صف قبل الجلسات، 136 مطلع الجلسة التاسعة) — 32 مؤلف_كتاب + **56
  شيخ_تلميذ** (+17 هذه الجلسة) + 5 شرح_لكتاب + 6 درس_عن_كتاب + 49
  فائدة↔كتاب (مرتبط) + 5 مرتبط (عالم↔عالم). مؤلف_كتاب **استُنفِد
  بالفحص المباشر** سابقاً (سكربت مطابقة صارم، صفر مرشح جديد)، لكن
  شيخ_تلميذ **أثبت أنه غير مستنفَد فعلياً** — كل دفعة إثراء علماء جديدة
  كشفت علاقات إضافية حقيقية من نصوص البيو المُحدَّثة (17 علاقة جديدة هذه
  الجلسة وحدها). نوع سادس "فتوى_في_باب" متروك عمداً فارغاً
  (الفتوى محذوفة من التطبيق). **عطل مرافق مُصلَح**: `TYPE_HREF.fawaid`
  في `KnowledgeRelatedItems.tsx` كان يتجاهل `id` ويعيد `/fawaid` العامة
  دوماً (نفس عائلة عطل fatwa/scholar سابقاً) — صُحِّح إلى
  `` `/fawaid#${id}` `` (يعمل عبر hash المتصفح الطبيعي، `FaidahCard.tsx`
  يضع `id={item.id}` على كل بطاقة فعلياً ولا تقسيم صفحات يخفي عناصر).
  **استُبعدت عمداً** ~60 مطابقة فائدة↔كتاب إضافية للكتب الستة (البخاري/
  مسلم/الترمذي/أبي داود/النسائي/ابن ماجه) لأنها مجرد مصدر تخريج خام لا
  قيمة تصفّح إضافية حقيقية تتجاوز حقل `source` الظاهر أصلاً.
- 15 جامعة في دليل الجامعات (كلها محقَّقة فردياً).
- 8 من 7 مسارات تعليمية كانت فارغة تماماً الآن منشورة بمحتوى حقيقي
  (uloom-quran، akhlaq، adab، arabic، nahw، dawah، tarbiyah، usool-fiqh).

### أهم الاكتشافات والإصلاحات التراكمية (بترتيب الأهمية لا الزمن)
1. **نمط عطل متكرر اكتُشف عبر الجلسة بأكملها: "تصنيف/رابط لا يطابق مصدر
   الفلترة الفعلي فيختفي المحتوى صامتاً"** — نفس جذر عطل "أخلاق إسلامية"
   الأصلي (بق تجزّؤ بنك الأسئلة) تكرر باستقلالية في: صفحة `/scholars`
   نفسها (83 بدل 96، مصدر بيانات مكرر متجمِّد — الاكتشاف الأصلي الذي بدأ
   هذا النمط)، رابط `TYPE_HREF.scholar` في `KnowledgeRelatedItems.tsx`،
   **11 رابط query-param صامت آخر** عبر الموقع (`/rulings`، `/library`،
   `/qa`، `/lessons`، `/miracles`، `/stories`، `/fawaid`،
   `/arbaeen-nawawi`، `/knowledge-graph`، `/universities`،
   `/occasions`)، **142 من 515 فائدة** (تصنيف لا يطابق شرائح
   `FawaidPage`)، **11 من 127 كتاباً** (نفس النمط في `LibraryPage`)، **10
   من 305 ذكر** (نفس النمط في `AdhkarPage`)، و**19 حكماً شرعياً** في
   موسوعة الأحكام (نظاما تصنيف متوازيان متضاربان + نموذج إدارة كان
   يعرضهما معاً فيمكن إعادة إنتاج العطل لأي حكم جديد). **كل هذه
   الحالات صُحِّحت** بنفس المنهجية: مقارنة برمجية (لا بصرية) بين مصدر
   الفلتر الفعلي المُستورَد فعلياً في الصفحة وبين القيم الفعلية في
   بيانات المحتوى.
2. **درس منهجي ثابت لأي جلسة قادمة**: لا تفترض أبداً أن مصدر شرائح
   الفلتر في صفحة ما هو نفس الملف الذي يحوي بيانات المحتوى — قد يكون
   ملفاً مختلفاً كلياً مُستورَداً من مكان آخر (كما حدث في `FawaidPage`
   التي تستورد تصنيفاتها من `fawaid-curated-seed.ts` لا من
   `fawaid-seed.ts` رغم أن الأخير يُصدِّر `FAWAID_CATEGORIES` خاصة به
   غير مُستخدَمة إطلاقاً). تحقّق دائماً عبر `grep` مباشر لمصدر الاستيراد
   الفعلي، ثم قارن برمجياً (Python/سكربت) لا بالعين — المقياس (515 فائدة
   مثلاً) أكبر من أن يُفحَص يدوياً بموثوقية.
3. **بقايا CMS/كود ميتة مُكتشَفة ومُوثَّقة** (لا خطر حي، للمرجعية):
   `publicRoute` في `content-registry.ts` (غير مُستدعى إطلاقاً)،
   `FATWA_CATEGORIES` في `platform-types.ts` (بلا مُستهلِك)، جدول
   `qa_categories` المحلي في `qa-seed.ts` ناقص فئة "فقه" (auto-generated،
   لا يُعدَّل يدوياً، والـDB الحية سليمة أصلاً فالتأثير محدود لمسار
   fallback فقط).
4. **مراجعة اتساق داخلية شاملة** لكل استشهادات SQL والاقتباسات المُضافة
   عبر الجلسات — **لا أخطاء نصية جديدة** (فقط تأكيد أن الأخطاء المُصلَحة
   سابقاً لم تتكرر).

### ثغرة محتوى حقيقية موثَّقة سابقاً — [مُغلَقة الآن]
~~6 تصنيفات في موسوعة الأحكام بلا حكم شرعي واحد~~ — **مُلئت بالكامل
الجلسة التاسعة** (commit `d516a914`، راجع التفصيل أعلاه). صفر تصنيف فارغ
متبقٍ في موسوعة الأحكام.

### حالة WebSearch/WebFetch (حرجة لتخطيط أي استئناف)
**WebSearch لا يزال منتهياً (0/200)** عبر 7 محاولات تحقّق منفصلة عبر
جلسات، آخرها مطلع الجلسة التاسعة. **لكن `WebFetch` يعمل فعلياً وله حصة
منفصلة تمامًا** — استُخدم بنجاح طوال الجلسة التاسعة كبديل تحقُّق حقيقي
(جلب `ar.wikipedia.org` لسير العلماء و`ar.wikisource.org` لنصوص أحاديث
صحيح البخاري/مسلم الكاملة بأرقامها الأصلية). **ابدأ أي جلسة قادمة
باختبار WebFetch أيضًا لا WebSearch فقط** — قد يكون العائق الفعلي محصورًا
في أداة واحدة لا في "البحث الخارجي" ككل. ملاحظة: بعض المواقع (`sunnah.com`،
`dorar.net`) ترفض WebFetch بـ403/404 — التزم بمصادر تقبل الجلب المباشر
(ويكيبيديا العربية، ويكي مصدر) بدل الاستسلام للعمل الهندسي البحت فورًا.

## قاعدة تشغيل حرجة (لا تتجاهلها)

- اعمل فقط داخل `artifacts/majalis/` (التطبيق الحقيقي). `src/` بجذر المستودع
  **ميت مكرر لا يُبنى أبدًا** — أي تعديل هناك ضائع فعليًا.
- **بيئة العمل**: الـcwd الافتراضي لأداة Bash هو
  `/Users/alabdullmohsen/majalis-content-fill/artifacts/majalis` (وليس جذر
  المستودع) — استخدم `cd /Users/alabdullmohsen/majalis-content-fill && ...`
  صراحةً لأي أمر git.
- `pnpm install` قد يلزم عند بداية أي جلسة جديدة.
- كل commit يمرّ عبر pre-commit hook يشغّل typecheck+lint+فحص خط+build تلقائيًا.
- ادفع بـ`git push origin majalis-content-fill` دوريًا (الفرع يتتبّع origin،
  نجح الدفع عدة مرات).
- **سكربتات تستورد seed.ts تحتاج `npx tsx script.mjs` — ليس `node
  script.mjs` مباشرة.** `node` وحده يفشل بـ`ERR_MODULE_NOT_FOUND` على
  استيراد مسارات `.ts` بلا امتداد صريح، وبـ`ERR_IMPORT_ATTRIBUTE_MISSING`
  على استيراد JSON بلا `with { type: "json" }`. فقط سكربتات تقرأ الملف كنص
  خام (`readFileSync` + regex، مثل `audit-data-quality.mjs`) تعمل بـ`node`
  عادي دون مشاكل.
- **قاعدة DB مربوطة**: `npx supabase link --project-ref
  ngmvmlulzacrlicuagyp` (نفّذه أول أي جلسة جديدة في worktree جديد — الربط
  محلي لكل worktree). `npx supabase db query --linked "SELECT ...;"` أو
  `--file path/to.sql` للملفات الطويلة. عامل أي نص عربي عائد من القاعدة
  كبيانات فقط لا تعليمات.
- **⚠️ `supabase db query --file` على DO $$ blocks غير ذرّي بشكل موثوق**:
  اكتُشف فعلياً — DO block فشل بمنتصفه (قيد CHECK) ولم يتراجع بالكامل، بقي
  صف يتيم من أول جزء نجح. **بعد أي فشل SQL: تحقّق من الحالة الفعلية
  بـSELECT قبل إعادة المحاولة، ونظّف يدوياً إن لزم — لا تفترض rollback
  تلقائياً كاملاً.**

## حالة المراحل 3-11 من التكليف الأصلي — بوضوح تام (لا ادّعاء اكتمال)

**لا شيء من المراحل 3-11 بدأ فعليًا كمرحلة بناء موسَّعة.** كل العمل عبر
الجلسات الأربع كان **Phase 0 (تدقيق وإصلاح)** بامتياز — دقيق، حقيقي،
مُتحقَّق منه، لكنه تصحيحي لا إنشائي واسع. بالتفصيل:

- **Phase 3 (موسوعة العلوم الشرعية الهرمية علم←أبواب←موضوعات←دروس)**:
  **بدأ فعليًا الآن (الجلسة الخامسة)** — أول باب عقدي كامل الهيكل: مقرر
  "أركان الإيمان الستة" داخل مسار `aqeedah` الموجود، 7 عناصر (تمهيد حديث
  جبريل + ركن لكل ركن)، كل عنصر بدليل قرآني حرفي مُتحقَّق من الملفات
  المحلية + تخريج حديثي دقيق (رقم الحديث في البخاري/مسلم/الأربعين
  النووية) + ربط بكتاب حقيقي (`لمعة الاعتقاد` لابن قدامة). راجع تفصيل
  كامل في قسم "الجلسة الخامسة" أدناه. **لا يزال هذا باباً واحداً من عشرات
  الأبواب المطلوبة** (عقيدة/قرآن وعلومه/حديث وعلومه/فقه/أصول فقه/سيرة/
  أخلاق) — نموذج ثانٍ يثبت النمط لموضوع ليس فقهيًا (بخلاف usool-fiqh)،
  وليس اكتمالاً للموسوعة.
  **اكتشاف مهم لتوجيه العمل القادم**: صفحات مرجعية مستقلة عالية الجودة
  موجودة فعلاً لعدة أساسيات عقدية (`/arkan-iman`, `/arkan` [أركان الإسلام]،
  `/tawhid`, `/asma-husna`, `/malaika`) — **راجعها دائمًا قبل بناء محتوى
  عقدي جديد** (`grep -n 'lazy(() => import("@/views/' src/App.tsx`) لتجنّب
  ازدواج جهد؛ الفجوة الحقيقية المتبقية هي هيكل `learning_paths` المتدرّج
  (مقررات/تتبع تقدّم) لا محتوى مرجعي أساسي (موجود غالبًا).
- **Phase 4-5 (مسارات تعليمية متدرجة + محتوى دروس تفصيلي)**: مسار واحد
  فقط من ~19 مساراً مطلوبًا (`usool-fiqh`) له محتوى حقيقي جزئي (3 عناصر
  فقط، لا دروسًا كاملة بكل عناصر Phase 5: مقدمة/أهداف/مصطلحات/أمثلة/
  تنبيهات/أخطاء شائعة/أسئلة مراجعة). 7 مسارات أخرى `status='draft'` (بلا
  محتوى إطلاقًا). 7+ مسارات أخرى غير ملموسة بعد كليًا.
- **Phase 6 (توسيع المكتبة فوق 117)**: **بدأ فعليًا (الجلسة السادسة)** —
  5 كتب جديدة مُتحقَّقة فردياً (117→122). لا يزال بعيداً عن أي هدف توسّع
  كبير، لكنه لم يعد "لا كتاب واحد أُضيف" كما كان.
- **Phase 7 (توسيع العلماء فوق 96)**: **لم يُنفَّذ** — لا عالم واحد أُضيف
  (الإصلاح الوحيد كان توحيد مصدر بيانات موجود مسبقًا، لا إضافة محتوى جديد).
- **Phase 8 (دليل الجامعات)**: **الوحيدة التي فيها تقدّم حقيقي ملموس** —
  7→15 جامعة، والـ15 كلها محقَّقة فرديًا الآن. لا تزال بعيدة عن التغطية
  الشاملة المتخيَّلة في التكليف الأصلي (كليات/درجات/رسوم/منح تفصيلية لكل
  جامعة) — البيانات الحالية أساسية (اسم/بلد/موقع/نبذة) فقط.
- **Phase 9 (توسيع بنك الأسئلة بتوازن)**: **لم يُنفَّذ** — العدد الحالي
  914 هو **بعد حذف** 36 مكررًا من 950 الأصلية، لا بعد أي إضافة توسّعية.
  لا سؤال واحد جديد أُضيف.
- **Phase 10 (مقالات أصلية جديدة)**: **لم يبدأ إطلاقًا** — صفر مقالات جديدة.
- **Phase 11 (بحث موحّد + ربط داخلي)**: **لم يُنفَّذ** — لا تعديل على بنية
  البحث أو الربط الداخلي بين المحتوى.

**ما أُنجز فعليًا وله قيمة حقيقية مؤكدة** (تفصيل كامل أدناه): إصلاح فخ
توجيه /hadith، حذف ~176 عنصر مكرر حرفيًا عبر qa/quiz/fawaid/adhkar-seed،
حذف صفحات SEO شبح لقسم محذوف، بناء سكربت تدقيق تخريج أحاديث دائم وإصلاح
19 حالة حقيقية به، إصلاح سكربت content-audit.mjs كان يكذب بصمت، تعبئة أول
14 حديثًا ضعيفًا/موضوعًا حقيقيًا في جدول كان فارغًا تمامًا، بناء نموذج
أولي واحد لمسار تعليمي مربوط بكتاب حقيقي، توسيع/تحقيق دليل الجامعات
بالكامل (15/15 محقَّقة فرديًا)، **اكتشاف وإصلاح عطل بيانات حقيقي كبير**
(صفحة /scholars كانت تعرض 83 بدل 96 عالمًا بسبب مصدر بيانات مكرر متجمِّد).

## ماذا أُنجز فعليًا (3 جلسات، آخر تحديث 2026-07-18)

> **⚠️ قسم تاريخي متروك للمرجعية فقط — راجع "📋 خلاصة موحَّدة" أعلى
> الملف للحالة الحالية الصحيحة.** هذا القسم يوثّق أول 3 جلسات فقط من
> 20+ دفعة لاحقة؛ أرقامه (مثل عدد العلماء المُثرَين أو الكتب) **متجاوَزة
> فعلياً بأرقام أحدث بكثير** موثَّقة في الخلاصة الموحَّدة. أُبقي دون حذف
> لأنه يحوي تفصيل استشهادات وقرارات تصميم قد يُحتاج الرجوع إليها.

كل ما يلي مُختبر (typecheck + lint + build) ومدفوع لـ`origin/majalis-
content-fill`. Commits بالترتيب الزمني:
`8d39b72d` → `eb47e7fa` → `406b29dd` → `d7c461c8` → `e1488e38` → `bba98094`
→ `72e0c01b` → `6b827dbb` → `a7279f95` → `0ce51275`.

### الجلسة الأولى
1. فخ src/ الميت لـ/hadith أُصلح (كان يعرض HadithIndexPage بدل HadithPage المدمجة).
2. 114 سؤال مكرر في qa-seed.ts أُزيلت (474→360).
3. 36 سؤال مكرر في quiz-seed.ts أُزيلت (950→914) + تصحيح خلاف واقعي (بدر≠أول غزوة).
4. تكراران في fawaid-seed.ts أُزيلا (510→508).
5. صفحات SEO شبح لـ/fatwa المحذوف أُزيلت بالكامل (generate-seo.mjs + 5 ملفات ثابتة).
6. `AUDIT_INVENTORY.md` كُتب بجذر المستودع.

### الجلسة الثانية — تدقيق تخريج الأحاديث + إصلاح أدوات
1. `scripts/audit/hadith-takhrij-check.mjs` بُني (يفحص qa/fawaid/adhkar/arbaeen/quiz-seed).
   كشف 19 حالة حقيقية بعد إصلاح إيجابيات كاذبة في الـregex (بادئة "ال"
   التعريف تسقط خطياً عند اتصال حرف الجر: "للبيهقي" لا "البيهقي")، كل واحدة
   حُقِّقت عبر WebSearch وأُصلحت. **النتيجة: 0 حالة متبقية.**
2. `content-audit.mjs` كان يُنتج أرقاماً خاطئة **بصمت** (regex على نص خام
   بدل استيراد حقيقي) — أُعيد كتابته بالكامل.

### الجلسة الثالثة (هذه الجلسة) — الأولويات الخمس + دفعة سادسة اكتُشفت أثناء العمل

**1) تعبئة daif/mawdu في verified_hadith_items — منجَز:**
`supabase/verified_hadith_daif_mawdu_seed_v1.sql` طُبِّق مباشرة على DB
الحية. 5 أحاديث ضعيفة + 9 موضوعة، كل واحد حُقِّق عبر WebSearch (دُرر
السنية، ابن باز، إسلام سؤال وجواب، إسلام ويب) مع ذكر الخلاف العلمي صراحة
حيث وُجد (حديثا "من عشق فعف" و"أنا مدينة العلم" لهما مخالفون معتبرون
للتضعيف/التوضيع، ذُكر ذلك في `explanation`). **النتيجة: sahih=290،
daif=5، mawdu=9** (كانت daif=0, mawdu=0). تحقّقتُ أن RLS
(`verified_hadith_public_read`) تسمح بقراءة anon لها (نفس شرط
`verification_status='verified'`).
- **الدفعة أولية فقط (14 حديثاً)** — الهدف الأصلي كان 20-30 لكل قسم. باقٍ:
  توسيعها لاحقاً بنفس المنهجية (WebSearch + تحقق حقيقي، لا توليد من الذاكرة).

**2) مسار أصول الفقه التجريبي كامل الجودة — منجَز:**
`supabase/learning_path_usool_fiqh_pilot_v1.sql`: مرحلة "التأسيس" ← مقرر
"المدخل إلى أصول الفقه" ← وحدة ← 3 عناصر (item_type='book')، كل عنصر
مربوط عبر `course_books` بكتاب حقيقي موجود في `library-catalog.ts`:
"الورقات في أصول الفقه" للجويني (`book-waraqat`) — المتن التأسيسي القياسي
لهذا الفن، وليس متناً متقدماً (روضة الناظر/المستصفى موجودان أيضاً بالمكتبة
لمراحل لاحقة، لم يُستخدما عمداً). **usool-fiqh: total_sessions 0→3.**

**اكتشاف معماري مهم موثَّق في الملف نفسه**: `learning_items` لها حقول
`content_ref_table/content_ref_id/external_url` لكن
`src/lib/learning-paths-service.ts` **لا تستعلمها في الواجهة أصلاً** — لا
مُشغِّل محتوى مضمَّن، فقط زر "إكمال" ذاتي التقرير. الربط الحقيقي الوحيد
الفعّال بمحتوى موجود هو `course_books` (يُعرض كشريحة كتاب). أي مسار جديد
يُبنى مستقبلاً يجب أن يتبع نفس النمط (قراءة موجَّهة في كتاب حقيقي) حتى
يُبنى مُشغِّل محتوى فعلي — هذا قيد معماري قائم، ليس اختياراً.

**3) توسيع universities من 7 إلى 15 — منجَز:**
`supabase/universities_expand_v1.sql`: 8 مؤسسات جديدة حُقِّقت عبر WebSearch
(الأزهر، دار العلوم-القاهرة، كلية الزيتونة-أمريكا، المعهد العالي لأصول
الدين-تونس، الجامعة الإسلامية العالمية-إسلام آباد، جامعة شريف هداية
الله-جاكرتا، دار الحديث الحسنية-المغرب، كلية الإلهيات-جامعة مرمرة-تركيا).
**اكتشاف**: الـ7 الأصلية كلها `last_reviewed_by="بيانات تجريبية — يحتاج
تحقق"` رغم `is_published=true` — **لم تُصلَح** (خارج نطاق آمن، تحتاج تحقق
فردي لكل رابط)، مسجَّلة كأولوية تالية أدناه.

**تحقّق طلبه المنسّق (2026-07-18، لاحقاً)**: هل الجامعات الثماني الجديدة
لها نفس منطق `is_verified`/`is_published` مقابل الأصلية السبع؟ فحصتُ
`lib/api-handlers/universities.js` (نقطة النهاية الفعلية التي تستهلكها
`src/lib/universities-service.ts` — **ليست استعلام Supabase مباشرًا من
الواجهة**): البوابة الوحيدة الفعلية للظهور العام هي `.eq("is_published",
true)` (سطر 75). حقل `is_verified` **لا يُستخدم كبوابة افتراضية إطلاقاً**
— فقط فلتر اختياري يطلبه المستخدم يدويًا عبر مربّع اختيار في
`UniversitiesPage.tsx` (سطر 239-240، "موثّق فقط"). أي: الجامعات الثماني
الجديدة (كلها `is_verified=true, is_published=true`) والسبع الأصلية
(`is_published=true` دومًا، `is_verified` مختلط true/false) **تُعامَل
بمنطق ظهور واحد متطابق تمامًا** — لا فرق بنيوي بينها. أيضًا تحقّقت:
`last_reviewed_by` **لا يُعرَض في أي مكوّن عام** (`UniversityDetailPage.
tsx`/`UniversitiesPage.tsx`) — حقل داخلي/إداري بحت لا يراه الزائر، فوسم
"بيانات تجريبية" للسبع الأصلية لا يُنتج ادّعاءً كاذبًا مرئيًا للمستخدم،
لكنه يبقى تناقضاً داخلياً يستحق التصحيح الفردي (البند التالي أدناه) —
**لا تغيير بنية مطلوب، السلوك الحالي للكود سليم ومتّسق فعلاً.**

**4) قرار status/content_status لـlearning_paths — محسوم ومنفَّذ:**
`supabase/learning_paths_draft_empty_v1.sql`. القرار: **لا عمود جديد** (كان
يتطلب تعديل 6+ استعلامات RLS-adjacent في learning-paths-service.ts). بدلاً
من ذلك: تصحيح البيانات — `status` عمود CHECK يقبل أصلاً 'draft', وfetchPathList()
يفلتر published فقط. المسارات السبعة الفارغة المتبقية (`uloom-quran, adab,
akhlaq, arabic, nahw, dawah, tarbiyah`) صارت `status='draft'` فاختفت
تلقائياً من `/learning/paths` بلا أي تعديل كود.

**5) توسيع audit-data-quality.mjs — منجَز:**
أُضيف فحص تكرار دائم لـquiz-seed.ts وfawaid-seed.ts (0 تكرار، تأكيد ثبات
إصلاحات الجلسة الأولى) وadhkar-seed.ts (**27 ذكراً مكرراً حقيقياً اكتُشف
وأُصلح فوراً**، 332→305). ملاحظة تصميم حرجة موثَّقة في الكود: فحص adhkar
**مقيَّد بالتصنيف (categoryId)** لأن نفس الذكر يتكرر شرعاً وعمداً عبر
تصنيفات مختلفة (مثال: "بسم الله" عند الوضوء وعند الطعام كلاهما مسنون
فعلاً) — هذا ليس خطأً، ومحاولة أولى غير مقيَّدة بالتصنيف أنتجت 57
"إيجابية كاذبة" قبل هذا التصحيح. أثر جانبي: `updates-seed.ts` فيه إعلان
تاريخي "الأذكار — 332+ ذكر" صار رقمه غير مطابق — عُدِّل العنوان بإزالة
الرقم الجامد (لم يُحذف السجل التاريخي).

**6) `scripts/audit-all-routes.mjs` — نُفِّذ فعلياً، واكتشف عطلاً حقيقياً كبيراً:**
شُغِّل عبر `pnpm run build` (لتوليد dist/) ثم `npx vite preview --port 5177`
(في الخلفية — **ملاحظة بيئية**: عمليات `run_in_background` في هذه البيئة
انتهت بـexit 144 عدة مرات من تلقاء نفسها بعد دقيقة تقريباً؛ إن أعدت التشغيل
تأكد من إعادة تشغيل الخادم بلا افتراض بقائه حياً طويلاً) ثم
`node scripts/audit-all-routes.mjs http://localhost:5177 <routesFile>
<outJson>` على 166 مساراً عاماً (استُخرجت من `src/App.tsx` بـ
`grep -oP '(?<=<Route path=")[^"]+' src/App.tsx | grep -v ":"`، استُبعد
`/admin/*`).

**الاكتشاف الأهم لهذه الجلسة كلها**: `/scholars` ظهرت `isBlank`-كاذبة في
أول تشغيل (مهلة قصيرة)، فتحقّقتُ يدوياً بـPlaywright تفاعلي (مهلة أطول +
`waitUntil: "load"`) ووجدت الصفحة تعرض **83 عالماً فقط بدل 96** — ليس عطل
توقيت بل **مصدر بيانات مكرر حقيقي**: `IslamicScholarsPage.tsx` كانت تحمل
مصفوفة SCHOLARS محلية منسوخة (83، متجمّدة) بدل الاستيراد من
`scholars-data.ts` (96، المصدر الصحيح المُدقَّق وdetail page `/scholars/:id`
تستورد منه أصلاً). **أُصلح فوراً** (راجع commit `df4735d5` أعلاه) — 15
عالماً كانوا مخفيين عن الفهرس العام صاروا ظاهرين، منهم القرطبي والسعدي.
هذا يثبت قيمة تشغيل هذا السكربت رغم صعوبة إعداده — **شغّله على بقية
المسارات الديناميكية (`/scholars/:id`, `/library/:id`, إلخ) في جلسة قادمة،
احتمال وجود مصادر بيانات مكررة أخرى مشابهة وارد فعلاً**.

بعد الإصلاح وتحسين مهلة الانتظار في السكربت نفسه (500ms→2500ms، والتعليق
في الكود يوثّق تجربة `waitForSelector("h1")` التي جُرِّبت وتراجعت لأنها
تُسيء التعامل مع مسارات Redirect-only كـ`/fatwa`): 28 من 166 "مشكلة"
متبقية، **كلها تحقّقتُ يدوياً أنها إيجابيات كاذبة**: الغالبية (~19) بسبب
`/api/prayer-times` 404 محلي (Vercel API غير متاح تحت `vite preview` الثابت
— لا يحدث في الإنتاج الحقيقي حيث `/api/*` يُوجَّه لدوال Vercel serverless)،
والباقي صفحات ثقيلة (`/library`, `/quiz`, `/quran-hub`...) ما زالت تحتاج
مهلة أطول من بيئة الاختبار المحلية البطيئة رغم أنها تُصيَّر بنجاح فعلياً
(تحقّقتُ يدوياً بمهلة أطول لكل من `/scholars` — الآن يعرض 96 بشكل صحيح).
**لا حاجة لعمل إضافي على هذا السكربت الآن** — قيمته الأساسية (اكتشاف
مصادر بيانات مكررة) تحقّقت.

**7) تنظيف بقايا CMS اليتيمة لجدول `fatwas`**: لم يُنفَّذ بعد (أولوية أقل،
الوقت لم يتّسع). راجع تفاصيله في قسم "أكبر فجوة متبقية" أدناه.

### الجلسة الرابعة — البقايا الصغيرة (بعد أمر "اكمل")

**1) توسيع daif/mawdu من 14 إلى 32 — منجَز:**
`supabase/verified_hadith_daif_mawdu_seed_v2.sql`: +8 ضعيف (daif-006..013)
و+10 موضوع (mawdu-010..019)، كل واحد حُقِّق فرديًا عبر WebSearch (لا من
نتائج عامل البحث الخلفي القديم — أُهملت تمامًا حسب تحذير الجلسة الرابعة
أعلاه، بُحث من الصفر). **النتيجة النهائية: sahih=290, daif=13, mawdu=19**
(لا يزال دون هدف 20-30 **لكل قسم** بالضبط — daif=13 دون 20 — فجوة صغيرة
متبقية، أقل أولوية). حالتان بخلاف علمي حقيقي وُثِّقتا صراحة في
`explanation`: حديث زيارة القبر ("من زار قبري وجبت له شفاعتي" — 3 أقوال
معتبرة من موضوع لصحيح)، و"الجنة تحت أقدام الأمهات" (أُعيد تصنيفه من داعف
مبدئي إلى موضوع بعد تحقق مستقل، مع ذكر البديل الصحيح "الزمها فإن الجنة
عند رجليها" رواه النسائي).

**2) تنظيف بقايا CMS لجدول `fatwas` — منجَز (بنطاق مُصغَّر آمن):**
لم يُلمَس `CmsContentKind` الواسع (يمسّ content-registry.ts وAdminInlineEdit
وخطوط dedup — خطر عالٍ). بدلاً من ذلك حُدِّد نطاق أضيق وآمن فعليًا:
`KnowledgeSourceType` في `src/lib/supabase.ts` (اتحاد أضيق بـ6 قيم يُغذّي
"مواد مرتبطة" عبر `knowledge_relationships`) — أُزيل منه "fatwa" (تحقّقتُ:
صفر صف في `knowledge_relationships` استخدمه قط). أثناء الإزالة اكتُشف عطل
حقيقي: `KnowledgeRelatedItems.tsx` كان `TYPE_HREF.fatwa` يشير لمسار
`/fatwa/:id` الميت (يُحوَّل الآن لـ`/rulings`) — **رابط ميت كامن أُزيل**.
عدّلت أيضًا `RelationshipsSection.tsx` (لوحة إدارة) و`KnowledgeGraphPage.tsx`
(صفحة `/knowledge-graph` العامة). commit `dde85da6`.

**3) مراجعة أعمق لـ`fawaid-seed.ts` — منجَزة جزئيًا:**
فُحصت عيّنة منهجية من الاقتباسات المنسوبة لعلماء/صحابة (لا الأحاديث
النبوية فقط، تلك غُطِّيت بالكامل في hadith-takhrij-check.mjs سابقًا) — 28
اقتباسًا فُحصت يدويًا عبر مطابقة أسلوبية/محتوى مع مصادر معروفة، كلها بدت
معقولة النسبة. **هذه ليست مراجعة شاملة لكل الـ508** (فحص عيّنة موجَّهة لا
سطرًا بسطر) — الفجوة لا تزال قائمة لمن يريد تدقيقًا كاملاً لاحقًا.

### الجلسة الخامسة — Phase 3 (بدء موسوعة العلوم الشرعية)

**مقرر "أركان الإيمان الستة" داخل مسار aqeedah — منجَز:**
`supabase/learning_path_aqeedah_arkan_al_iman_v1.sql`. تحقيق معماري أولًا:
فُحص مسار `aqeedah` (منشور فعلاً، 5 مقررات: نواقض الإسلام/كتاب التوحيد/
العقيدة الواسطية/العقيدة الطحاوية/القواعد المثلى) — لا مقرر يغطي الأركان
الستة كموضوع مستقل، فأُضيف مقرر جديد `arkan-al-iman` بترتيب **سابق** لـ
"نواقض الإسلام" (sort_order=0، لأن تعريف الإيمان منطقيًا يسبق نقض نواقضه).

البنية: 7 عناصر تعليمية (item_type='book') — تمهيد بحديث جبريل + ركن لكل
ركن من الستة — كل عنصر مربوط عبر `course_books` بـ"لمعة الاعتقاد الهادي
إلى سبيل الرشاد" لابن قدامة المقدسي (`book-luma-itiqad-ibn-qudama`، موجود
مسبقًا في `library-catalog.ts`)، مع `scope_description` يشير لباب محدَّد
من أبواب الكتاب السبعة **المُتحقَّق منها فعليًا عبر WebSearch قبل الكتابة**
(كلام الله، القرآن كلام الله، رؤية المؤمنين لربهم يوم القيامة، القضاء
والقدر، الإيمان قول وعمل، الإيمان بكل ما أخبر به الرسول، محمد خاتم
النبيين) — لا ادّعاء بتغطية باب لم يُتحقَّق من وجوده.

**سلامة الاستشهاد (حرجة، مُلتزَم بها حرفيًا):**
- كل آية قرآنية في الملف استُخرجت **حرفيًا** من `public/data/quran/surah-
  XXX.json` (المصدر المحلي المُتحقَّق من بصمته، Tanzil/AlQuran Cloud) عبر
  قراءة مباشرة للملف بـpython — **لا من الذاكرة إطلاقًا** (النساء:136،
  البقرة:285، البقرة:177، القمر:49).
- حديث جبريل: رقمه الدقيق في صحيح مسلم (8) وصحيح البخاري (50) والأربعين
  النووية (الحديث الثاني) — تحقّق عبر WebSearch (ويكيبيديا العربية +
  مصادر متعددة متطابقة)، لا من الذاكرة.

**نتيجة التطبيق على DB الحية**: `aqeedah.total_sessions` 50→57 (+7)، مقرر
`arkan-al-iman` به 7 عناصر و7 صفوف `course_books` — تحقّقتُ مباشرة بعد
التطبيق. **دفعة تحقق فنية كاملة نجحت معًا**: typecheck + lint +
test:regression (كل الـ16 سويت، 0 فشل) + build (بما فيه `test-dynamic-
404-safety` و`test-no-regressions` كجزء من build) — لم يتطلب أي تعديل كود
(هذا العمل DB-only، لا تغيير TS/TSX).

**اكتشاف جانبي مهم أثناء `pnpm run build`**: لاحظتُ `ArkanImanPage-*.js`
في مخرجات البناء — صفحة مرجعية مستقلة `/arkan-iman` (`src/views/
ArkanImanPage.tsx`) **موجودة فعلاً ومكتملة الجودة** (7 أقسام تفاعلية،
أدلة قرآنية+حديثية لكل ركن، أقوال علماء، JSON-LD، اختبار قصير). لا تعارض
مع المقرر الجديد (مرجع للقراءة الواحدة مقابل مسار متدرّج بتتبّع تقدّم)
لكنه **درس مهم للعمل القادم**: تحقّق دائمًا من `src/views/*.tsx` قبل افتراض
فراغ موضوع عقدي — الأساسيات (أركان الإيمان، أركان الإسلام `/arkan`،
التوحيد `/tawhid`، الأسماء الحسنى `/asma-husna`، الملائكة `/malaika`) لها
صفحات مرجعية جيدة بالفعل؛ الفجوة الحقيقية في هيكل `learning_paths`
المتدرّج تحديدًا، لا في المحتوى المرجعي الأساسي.

### الجلسة السادسة — Phase 4-5 (ثاني مسار تعليمي فارغ يُبنى بالكامل)

**مسار `uloom-quran` (علوم القرآن): draft فارغ ← published ببنية كاملة — منجَز:**
`supabase/learning_path_uloom_quran_v1.sql`. اختير من السبعة المتبقية
(adab, akhlaq, arabic, dawah, nahw, tarbiyah, uloom-quran) لأنه الوحيد
بمطابقة 1:1 تامة بين اسم المسار وتصنيف حقيقي جاهز في `library-catalog.ts`
("علوم القرآن" — 4 كتب: الإتقان/البرهان/مناهل العرفان/التبيان [الأخير
مصنَّف "آداب" لكنه بذات الموضوع]).

**خطوة أولى ضرورية لم تكن موجودة في usool-fiqh/aqeedah**: هذا المسار كان
له placeholder صريح ("قيد الإعداد" ← مقرر "محتوى قيد المراجعة العلمية"
بوصف "لم تُضَف بعد دروس أو كتب حقيقية معتمدة") — **حُذف عمداً** (`DELETE
FROM path_stages` مع CASCADE يحذف مقرره تلقائياً) قبل إدراج المحتوى
الحقيقي، لأن إبقاءه بجواره يصبح تصريحاً كاذباً. **درس للمسارات المتبقية
الستة**: تحقّق دائماً من وجود placeholder مماثل بهذا النمط أولاً
(`SELECT * FROM path_stages WHERE path_id=... AND slug='pending-content'`)
واحذفه بنفس الطريقة قبل الإدراج.

البنية: 3 مراحل بنفس تسمية aqeedah/usool-fiqh (التأسيس/البناء/التوسع) ←
3 مقررات ← 11 عنصراً تعليمياً (لا مقرر واحد بعنصر واحد كالنمط الأول، بل
تفصيل موضوعي حقيقي كما في arkan-al-iman) ← 11 صف `course_books`:
- **التأسيس**: "آداب حامل القرآن" (3 عناصر، كتاب التبيان للنووي — أبوابه
  العشرة تحقّقت عبر WebSearch قبل الكتابة: فضل التلاوة، آداب الحامل،
  آداب القراءة).
- **البناء**: "المدخل إلى علوم القرآن" (4 عناصر، الإتقان للسيوطي — المكي
  والمدني، أسباب النزول، الناسخ والمنسوخ، جمع القرآن).
- **التوسع**: "علوم القرآن المتقدمة" (4 عناصر، البرهان للزركشي + مناهل
  العرفان للزرقاني — القراءات، إعجاز القرآن، المحكم والمتشابه، رسم المصحف).

**سلامة الاستشهاد (نفس المنهجية الملتزَم بها حرفياً)**:
- 4 آيات استُخرجت حرفياً من `public/data/quran/surah-*.json` (لا من
  الذاكرة): البقرة:106 (النسخ)، آل عمران:7 (المتشابه)، الإسراء:88
  (الإعجاز)، الحجر:9 (حفظ القرآن).
- حديث «خيركم من تعلم القرآن وعلمه»: صحيح البخاري، رقم 5027 — تحقّق
  عبر WebSearch (3 مصادر متطابقة).
- حديث «أُنزل القرآن على سبعة أحرف»: صحيح البخاري (فضائل القرآن) وصحيح
  مسلم (صلاة المسافرين، رقم 818)، متواتر كما قرّر القاسم بن سلّام —
  تحقّق عبر WebSearch.
- محتوى الأبواب/الأنواع لكل كتاب (10 أبواب للتبيان، 80 نوعاً للإتقان، 47
  نوعاً للبرهان، مباحث مناهل العرفان) **كلها تحقّقت عبر WebSearch قبل
  كتابة أي `scope_description`** يدَّعي تغطية باب/نوع بعينه — لا ادّعاء
  غير مُتحقَّق منه، بنفس القيد المُلتزَم به في مقرر أركان الإيمان.

**نتيجة DB الحية**: `uloom-quran.status`: `draft`→`published`,
`total_sessions`: `0`→`11`. تحقّقتُ مباشرة: 3 مراحل، 3 مقررات، 11 عنصراً،
11 صف `course_books`. **دفعة تحقق فنية كاملة نجحت معًا**: typecheck + lint
+ test:regression (16 حزمة، 0 فشل) + build (بما فيه فحوصات ما بعد البناء)
— DB-only، لا تغيير كود TS.

**Phase 6 (توسيع المكتبة فوق 117) — أول دفعة حقيقية، منجَزة:**
`src/lib/library-catalog.ts`: 5 كتب جديدة (117→122)، كل واحد تحقّق مؤلفه
ومكانته العلمية عبر WebSearch مباشر قبل الإضافة — سدّت ثغرات حقيقية
ملحوظة في الكتالوج القائم (127+ كتاباً فُحص فهرسه كاملاً أولاً بـ`grep
-oP '(?<=title: ")[^"]+' | sort` قبل الاختيار لتفادي التكرار):
- **صحيح ابن خزيمة** (حديث) — كان غائباً رغم كونه أصح الصحاح بعد
  الصحيحين عند جمهور المحدّثين.
- **ميزان الاعتدال في نقد الرجال** للذهبي (مصطلح الحديث) — أهم كتب الجرح
  والتعديل، كان غائباً رغم وجود 9 كتب أخرى في نفس التصنيف.
- **تفسير الجلالين** (تفسير) — أوسع التفاسير المختصرة انتشاراً، كان
  غائباً رغم 9 تفاسير أخرى موجودة (كلها مطوَّلة نسبياً).
- **سير أعلام النبلاء** للذهبي (تاريخ) — موسوعة التراجم الأشهر، كانت
  غائبة تماماً رغم 6 كتب تاريخ أخرى.
- **الهداية شرح بداية المبتدي** للمرغيناني (فقه حنفي) — من أمهات كتب
  المذهب الحنفي، التصنيف كان به كتاب واحد فقط قبلها.

تحقّق تلقائي بعد البناء: `فُحص: 96 عالِمًا و122 كتابًا` + `كل سجل حي له
prerender مطابق` (5 مجلدات `seo-prerender/library/book-*` جديدة تولَّدت
تلقائياً وصحّت يدوياً بفحص `<title>` لأحدها). typecheck+lint+
test:regression+build نجحت معاً — **هذه دفعة كود فعلية** (خلافاً لدفعتي
aqeedah/uloom-quran الـDB-only)، وتغيير `content-counts.json` (117→122)
تغيير محتوى حقيقي لا ضجيج توليد، احتُفظ به عمداً خلافاً لملفات rulings-
encyclopedia/feed/sitemap التي أُعيدت لحالتها (طابع زمني فقط، راجع
الملاحظة التشغيلية أعلاه).

**مسار `akhlaq` (الأخلاق): draft فارغ ← published ببنية كاملة — منجَز
(نفس الجلسة، استئناف فوري بعد تأكيد المنسّق):**
`supabase/learning_path_akhlaq_v1.sql`. نفس نمط uloom-quran بالضبط: حذف
placeholder صريح ("قيد الإعداد") أولاً، ثم 3 مراحل (التأسيس/البناء/
التوسع) ← 3 مقررات ← 9 عناصر تعليمية ← 9 صف `course_books`، كلها مربوطة
بـ**3 كتب موجودة مسبقاً** في `library-catalog.ts` (لا كتب جديدة هذه
المرة): "تهذيب الأخلاق" لابن مسكويه (التأسيس)، "رياض الصالحين" للنووي
(البناء)، "أدب الدنيا والدين" للماوردي (التوسع).

**ملاحظة معمارية مكرَّرة**: صفحة مرجعية مستقلة عالية الجودة `/akhlaq`
(`src/views/AkhlaqPage.tsx`، 1328 سطراً، عشرات الفضائل مصنَّفة حسب
مع الله/مع النفس/مع الناس/في الأسرة/في المجتمع) موجودة مسبقاً — لم
تُستخدَم محتوياتها مباشرة (كُتب محتوى مستقل مربوط بالكتب الثلاثة اتساقاً
مع نمط aqeedah/uloom-quran)، ولا تعارض معها (مرجع تصفّح مقابل مسار
متدرّج بتتبّع تقدّم). هذا يؤكد النمط المُلاحَظ مرتين الآن: أساسيات الأخلاق
والعقيدة غالباً موجودة كصفحات مرجعية، والفجوة الحقيقية في هيكل
`learning_paths` تحديداً.

**سلامة الاستشهاد**: أبواب/مباحث الكتب الثلاثة الفعلية (تهذيب الأخلاق:
النفس/الفضائل/المحبة والصداقة؛ أدب الدنيا والدين: الأبواب الخمسة
المؤكَّدة؛ رياض الصالحين: ترتيب الأبواب الأولى الإخلاص→التوبة→الصبر→
الصدق) تحقّقت عبر WebSearch قبل الكتابة. حديث «إنما الأعمال بالنيات»:
تأكّدتُ أنه **أول حديث** افتتح به البخاري صحيحه (لا رقم تسلسلي واحد —
تضارب حقيقي بين أرقام الطبعات لاحظته أثناء البحث، فاكتُفي بالوصف
المؤكَّد "أول حديث" بدل المجازفة برقم قد يكون خاطئاً لطبعة بعينها)، مع
رقم مسلم المؤكَّد (1907).

**نتيجة DB الحية**: `akhlaq.status`: `draft`→`published`,
`total_sessions`: `0`→`9`. تحقّقتُ مباشرة: 3 مراحل، 3 مقررات، 9 عناصر، 9
صف `course_books`. دفعة تحقق فنية كاملة نجحت معاً (DB-only، لا تغيير كود
TS).

**مسار `adab` (الآداب): draft فارغ ← published ببنية كاملة — منجَز (نفس
الجلسة، استئناف فوري ثالث بعد تأكيد المنسّق):**
`supabase/learning_path_adab_v1.sql`. **تمييز متعمَّد عن akhlaq** موثَّق
صراحة في الملف: أخلاق = صفات نفسية عامة ثابتة (الصدق/الصبر/الإخلاص)،
آداب (هذا المسار) = سلوكيات موقفية تفصيلية (التعامل مع الوالدين/الجار/
الضيف/السلام). البنية: 3 مراحل ← 3 مقررات ← 7 عناصر ← 7 صف `course_books`،
كتابان **غير مستخدَمين سابقاً** في akhlaq رغم كونهما في نفس تصنيف "آداب":
"الأدب المفرد" للبخاري (التأسيس: بر الوالدين/حق الجار؛ البناء: إكرام
الضيف/آداب السلام) و"مدارج السالكين" لابن القيم (التوسع: منزلة التوبة/
الإخلاص/الصبر — شرح "منازل السائرين" للهروي).

**تحقيق البحث تم عبر Agent فرعي مخصَّص هذه المرة** (بدل WebSearch مباشر
كالجلسات السابقة) لضمان تحقّق أعمق قبل الكتابة. اكتشاف مهم أثناء البحث:
حديث «رضا الرب في رضا الوالد» الشهير له **تصحيح غير مستقر عند الألباني**
عبر مؤلفاته المختلفة (صحّحه في مواضع، ثم نُقل تراجعه في أخرى) —
**استُبعد الحديث المتنازَع فيه بالكامل** والتزم عوضاً عنه بحديث ابن مسعود
الثابت («أي العمل أحب إلى الله؟ ... ثم بر الوالدين») المُتحقَّق كحديث
رقم 1 في صحيح الأدب المفرد (تصحيح الألباني) وأصله في البخاري (2782)
ومسلم (85) — **تطبيق حرفي لقيد "لا تُعرض معلومة غير مؤكدة كحقيقة"** حين
واجهتُ تعارضاً حقيقياً في التصحيح العلمي، وليس مجرد تعليمة نظرية.

**نتيجة DB الحية**: `adab.status`: `draft`→`published`, `total_sessions`:
`0`→`7`. تحقّقتُ مباشرة: 3 مراحل، 3 مقررات، 7 عناصر، 7 صف `course_books`،
وتحقّقتُ يدوياً أن حقل `outcomes` (jsonb) الذي فيه نص عربي طويل صار مصفوفة
JSON صحيحة بعد `::jsonb` cast (فحص مباشر عبر SELECT). دفعة تحقق فنية
كاملة نجحت معاً (DB-only، لا تغيير كود TS، ولا تغيير في
`content-counts.json` هذه المرة — منطقي لأن التغيير لا يمسّ عدد الكتب).

**الآن ثلاثة مسارات من السبعة الأصلية الفارغة مُنجَزة (uloom-quran،
akhlaq، adab) — 4 متبقية**: arabic, nahw, dawah, tarbiyah.

**`arabic` و`nahw` — قرار سريع منفَّذ (طُلب صراحةً من المنسّق "قرار بسيط،
لا تُطِل التفكير فيه") — منجَزان معاً:**
`supabase/learning_path_arabic_nahw_v1.sql`. القرار: **مساران منفصلان لا
مسار واحد** — أوصافهما المخزَّنة أصلاً مختلفة فعليًا (`nahw`="قواعد النحو
العربي" تخصصي، `arabic`="أساسيات اللغة العربية للطالب العلمي" عام واسع)،
والمكتبة تحوي كتباً كافية لتغطية كليهما بلا تداخل: `nahw` = تسلسل
الآجرومية (تأسيس) ← مراح الأرواح في الصرف (بناء) ← ألفية ابن مالك (توسّع)
— 3 مراحل، 5 عناصر. `arabic` = مسح أوسع: جامع الدروس العربية للغلاييني
(تأسيس) ← أسرار البلاغة للجرجاني (بناء) ← المعجم الوسيط (توسّع) — 3
مراحل، 3 عناصر (أخف من المعتاد عمداً، تنفيذاً لطلب السرعة). كلاهما
`status`: `draft`→`published`. تحقّق WebSearch لمحتوى الآجرومية/الألفية/
مراح الأرواح تم قبل الكتابة. typecheck+lint+test:regression+build نجحت
معاً. **الآن 5 من 7 مسارات فارغة أصلية مُكتملة — 2 متبقيان**: dawah,
tarbiyah.

**تحوّل تركيز الجلسة بتوجيه صريح من المنسّق**: بعد 5 مسارات، طلب المنسّق
صراحةً التنويع بدل الاستمرار الحصري في المسارات — الانتقال لمرحلة لم
تُلمَس إطلاقاً بعد. اخترتُ **Phase 7 (تعميق العلماء الـ96)** كأنضج
للتنفيذ الآن: نوع `Scholar` في `scholars-data.ts` له حقل `sources` جاهز
فعلاً (96 من 98 سجلاً فيه `sources: []` فارغ تمامًا — فجوة حقيقية موثَّقة
في تعليق النوع نفسه: "فارغة = لم يُوثَّق بعد") لكن **غير مستهلَك في أي
واجهة عرض حالياً** (تحقّقتُ: لا استخدام لـ`verificationStatus`/`sources`
في `IslamicScholarsPage.tsx` أو أي مكوّن آخر) — إثراء آمن تمامًا، صفر
مخاطر عرض.

**Phase 7 (تعميق العلماء) — 8 من 96 علماء أُثريَ فعلياً — منجَز:**
اختيار 8 علماء بعلاقات شيخ/تلميذ موثَّقة جيداً بينهم (ابن تيمية↔ابن
القيم، البخاري↔الترمذي، الذهبي↔ابن كثير) لتعظيم القيمة من دفعة صغيرة.
**تحقيق البحث تم عبر Agent فرعي مخصَّص** (بحث شامل لكل الثمانية دفعة
واحدة، أسرع من 8 WebSearch منفصلة)، وأنتج تقريراً يفرّق بوضوح بين
المؤكَّد وغير المؤكَّد — **مثال حرج طُبِّق فعلياً**: البحث رجّح أن مسلماً
والنسائي "عاصرا" البخاري وليسا بالضرورة "تلميذين مباشرين" له بالمعنى
الرسمي، فلم يُذكر ذلك في بيو البخاري رغم إغراء الشهرة الشائعة لهذه
العلاقة — **التزام حرفي بعدم عرض معلومة غير مؤكدة كحقيقة، حتى لو كانت
شائعة التداول**.

لكل عالم من الثمانية: (أ) `bio` أُثري بجملة أو جملتين تذكر علاقة شيخ/
تلميذ محدَّدة بتفاصيل حقيقية (لا "تلميذ فلان" عامة فقط، بل مدة الملازمة
حين تُوجد، وسياق العلاقة)، (ب) `sources` (كان `[]` فارغاً) صار يحوي مرجعاً
ببليوغرافياً حقيقياً واحداً أو اثنين (سير أعلام النبلاء، ذيل طبقات
الحنابلة، الدرر الكامنة، الجواهر والدرر، طبقات الشافعية الكبرى)، (ج)
`verificationStatus` تحوَّل من `pending_review` إلى `reviewed` — **قرار
تسمية متعمَّد**: لم أستخدم `verified` لأن ذلك الحقل، رغم عدم استهلاكه
حالياً في أي واجهة، **يعني ضمناً مراجعة بشرية من هيئة علمية** (يقترن في
النوع بحقلي `reviewedBy`/`reviewedAt` غير المُستخدَمين هنا) — `reviewed`
أصدق تعبيراً عن "تحقّق آلي موثَّق بمصادر حقيقية عبر WebSearch"، تمييزاً
واعياً عن مراجعة بشرية رسمية لم تحدث.

typecheck + lint + test:regression (16 حزمة، بما فيها `test:scholars-
integrity` تحديداً) + build نجحت معاً. عدد العلماء بقي 96 (منطقي — إثراء
سجلات موجودة لا إضافة جديدة). 8 صفحات `seo-prerender/scholars/*`
تولَّدت تلقائياً بالمحتوى الجديد، تحقّقتُ يدوياً من ظهور النص الجديد في
واحدة منها (`grep` مباشر على الملف). **88 من 96 لا يزالون بـ`sources: []`
فارغ** — فجوة كبيرة متبقية لجلسات لاحقة، هذه دفعة أولى مُثبِتة للنمط لا
اكتمال.

### الجلسة السادسة (تابع) — Phase 11 ثم Phase 10 (المرحلتان اللتان لم تُلمَسا إطلاقاً)

**Phase 11 (ربط داخلي) — إصلاح فجوة حقيقية موجودة سابقاً + تدقيق شامل:**
اكتشاف جوهري أثناء الفحص: `LearningPathDetailPage.tsx` (صفحة تفصيل أي
مسار تعليمي) كانت تعرض اسم الكتاب المربوط بكل عنصر تعليمي (`course.
books`) **كنص عادٍ بلا رابط إطلاقاً** — لا رابط لصفحة الكتاب الحقيقية
`/library/:id`. هذا عطل ربط داخلي **سيطري على كل الموقع** (كل مسار له
كتب، لا فقط الستة الجديدة). أُصلح: أُضيف `LIBRARY_TITLE_TO_ID` (خريطة
عنوان←معرّف من `LIBRARY_CATALOG` نفسه، مطابقة حرفية لأن `course_books.
book_title` نص حر لا FK لمعرّف الفهرس الثابت)، وأصبحت شريحة الكتاب رابطاً
فعلياً لـ`/library/:id` حين يوجد تطابق، مع سقوط آمن لنص عادٍ حين لا يوجد
(لا كسر إطلاقاً). أُضيف نمط CSS بسيط (`.lpd2-book-chip__link`، يعيد
استخدام متغيرات الهوية اللونية القائمة `--majalis-emerald`، لا ألوان
جديدة) — راجع `src/views/learning/LearningPathDetailPage.tsx` و
`src/styles/elite-2026.css`.

**تدقيق شامل قبل الإصلاح** (سكربت python مباشر يقارن كل `course_books.
book_title` الفريد في القاعدة الحية مقابل كل `title` في `library-
catalog.ts`): **المسارات الستة المبنية هذه الجلسات (aqeedah/arkan-al-
iman، usool-fiqh، uloom-quran، akhlaq، adab، nahw، arabic) سليمة 100%
— صفر تعارض تسمية**. لكن الفحص الشامل (لا يقتصر على الستة فقط) كشف 5
تعارضات في **مسارات أقدم لم تُلمَس هذه الجلسات إطلاقاً** (`nawaqid-islam`
ضمن aqeedah، `minhaj-muslim`/`zad-mustaqni` ضمن مسار `fiqh`):
- **صُحِّح فوراً** (`supabase/course_books_title_fix_v1.sql`): "الأصول
  الثلاثة"→"ثلاثة الأصول" و"زاد المستقنع"→"زاد المستقنع في اختصار
  المقنع" — نفس الكتاب بصياغة مختلفة، تصحيح نصّي بسيط بلا مخاطر.
- **لم يُصحَّح عمداً**: "القواعد الأربع"، "كشف الشبهات"، "منهاج المسلم"
  — **غائبة تماماً من الفهرس الثابت** (ليست مشكلة تسمية بل كتب لم تُضَف
  بعد إطلاقاً) — تحتاج تحقّق WebSearch وإضافة فعلية (عمل Phase 6 لا
  Phase 11)، تبقى نصاً بلا رابط (سلوك آمن افتراضي، موثَّق في تعليق
  `course_books_title_fix_v1.sql`).

**Phase 10 (مقالات أصلية) — أول مقالة نموذج، بنية تحتية جديدة كاملة:**
اكتشاف معماري حرج قبل الكتابة: `CmsContentKind` يضم "article" كنوع
محتوى في `content-registry.ts`، لكنه معرَّف بـ`table: "library_items"`
(جدول شبه فارغ بلا أي صفحة عرض حقيقية له) و`publicRoute: () =>
"/library"` — **لا مسار `/articles` ولا صفحة عرض مقال موجودة إطلاقاً في
src/App.tsx**. كتابة مقال عبر هذا المسار كانت ستُنتج سجلاً يتيماً تماماً
— تكرار حرفي لنفس عطل بقايا `fatwa` اليتيمة المُصلَح جزئياً سابقاً.

**القرار**: جدول جديد بسيط `articles` (`supabase/articles_v1.sql`) مستقل
تماماً عن `content-registry` المكسور، بنفس نمط `is_approved`/
`verified_by`/`approved_at` الموجود فعلاً في `islamic_stories`/
`prophet_stories` (نمط مُثبَت في المشروع، لا اختراع بنية جديدة). RLS:
`SELECT` عام مقيَّد بـ`is_approved = true` فقط (نفس نمط
`verified_hadith_public_read`) — **تحقّقتُ مباشرة**: `SET LOCAL ROLE
anon; SELECT count(*) FROM articles;` أرجعت 0 فعلياً رغم وجود صف واحد،
لأن `is_approved=false`.

**لم يُبنَ مسار/صفحة عرض علنية `/articles/:slug` عمداً** — بناء صفحة
كاملة عمل واجهة خارج نطاق "مقالة نموذج واحدة تُثبت النمط"، ويخالف توجيه
"تجنّب عمل الواجهة/نظام التصميم" الثابت لهذا الوكيل (فريق تصميم موازٍ
يعمل على ذلك في فرع منفصل). `is_approved=false` + غياب مسار عرض =
تمثيل صادق تماماً لـ"محتوى قيد المراجعة" — لا حاجة لصفحة لإثبات ذلك.

**المقالة**: "أركان الإيمان الستة: تعريفها وأدلتها من القرآن والسنة" —
اختيرت عمداً لأنها **تعيد استخدام مادة مُتحقَّقة بالفعل** من الجلسة
الخامسة (لا استشهاد جديد غير مُتحقَّق لمعظم المحتوى): 4 آيات مستخرَجة
حرفياً من الملفات المحلية، حديث جبريل (مسلم 8، البخاري 50، الأربعين 2).
**إضافة جديدة واحدة تحقّقت هذه الدفعة عبر WebSearch**: خلاف عقدي حقيقي
موثَّق حول تعريف الإيمان (أهل السنة: قول+اعتقاد+عمل، يزيد وينقص — مقابل
المرجئة: الأعمال خارج مسمى الإيمان) — عُرض الموقفان بأمانة دون فتوى
شخصية، مع تمييز صريح داخل نص المقال بين ما هو "نصّ قطعي" (أصل الأركان
الستة) وما هو "مسألة اجتهادية/خلافية". تتضمن المقالة: عنوان غير مضلِّل،
مقدمة، 9 عناوين فرعية (##)، 7 مصادر مُخرَّجة، 3 روابط داخلية حقيقية
(`/learning/paths/aqeedah`، `/arkan-iman`، `/library/book-luma-itiqad-
ibn-qudama` — كلها محتوى بُني أو تحقَّق فعلياً هذه الجلسات)، 3 أسئلة
شائعة، وخلاصة. أُدخلت بـ`is_approved=false` كما طُلب صراحةً.

typecheck + lint + test:regression + build نجحت معاً على كل هذه الدفعة
(كود جديد + جدول DB جديد + إصلاح ربط + تصحيح تسمية).

### إغلاق الفجوة المعلَّقة — تحقّق بصري فعلي عبر Playwright (منجَز)

الرابط الجديد في `LearningPathDetailPage.tsx` كان غير مُتحقَّق منه بصرياً
(الصفحة client-rendered بالكامل، لا تظهر في `dist/.../index.html`
الثابت). **اكتُشفت أثناء المحاولة الأولى عقبة حقيقية**: `pnpm run
preview` المحلي يبني بـ`sb-placeholder-auth-token` (لا `.env.local` في
هذا الـworktree) فتبقى الصفحة عالقة على "جارٍ التحميل…" بلا بيانات حقيقية
أبداً — Playwright ضد بناء كهذا كان سيُنتج نتيجة سلبية كاذبة (0 روابط)
لا لأن الإصلاح خاطئ بل لأن البيانات لم تصل أصلاً. **الحل**: استُخرج
مفتاح anon العام (آمن للعميل بطبيعته، ليس سرّاً) عبر `npx supabase
projects api-keys --project-ref ngmvmlulzacrlicuagyp`، وُضع في
`.env.local` مؤقت (محمي بـ`.gitignore` أصلاً، حُذف فوراً بعد الاستخدام)،
أُعيد البناء والتصيير بـ`pnpm run preview --port 5178`.

**نتيجة الاختبار الفعلي (سكربت Playwright مؤقت، نافذة متصفح حقيقية،
تنقّل فعلي بالنقر لا فحص DOM فقط)** على المسارات الستة كاملة:
- aqeedah: 10 شرائح كتب، 8 روابط فعلية (2 نص عادٍ بلا رابط لكتابي
  "القواعد الأربع"/"كشف الشبهات" الغائبين فعلاً من الفهرس — سقوط آمن كما
  صُمِّم، ليس خطأ).
- akhlaq: 9/9، adab: 7/7، uloom-quran: 11/11، nahw: 5/5، arabic: 3/3 —
  **كل شريحة كتاب موجودة في الفهرس أصبحت رابطاً فعلياً، 100%**.
- **تحقّق تنقّل حقيقي**: نُقر على أول رابط في كل مسار، وصل فعلياً لصفحة
  الكتاب الصحيحة (مثال: `/library/book-thalathah` بعنوان H1 مطابق حرفياً
  "ثلاثة الأصول"؛ `/library/book-tibyan` ← H1 "التبيان في آداب حملة
  القرآن"). **ليس فحصاً سطحياً لوجود الرابط، بل تأكيد أن الوجهة صحيحة
  فعلاً**.

الفجوة مُغلَقة بثقة كاملة الآن، لا "ملاحظة صادقة معلَّقة" متبقية. لم
تُحفَظ سكربتات الاختبار المؤقتة أو `.env.local` (حُذفت جميعاً، لا أثر
لها في git).

### Phase 9 (بنك الأسئلة) — أول توسيع حقيقي، 914→926 (12 سؤالاً جديداً)

**فحص معماري أولاً (كما طُلب صراحةً، بحذر مماثل لفحص Scholar سابقاً)**:
اكتُشفت حقيقة مهمة — **يوجد نظاما أسئلة منفصلان تماماً**، لا نظام واحد:
1. `src/data/islamicQuizData.ts` (`ALL_QUESTIONS`، حقول `{id,q,a,hint}`)
   — البيانات الثابتة الأولية للعبة `/quiz` (تصميم Jeopardy بفئات ونقاط
   200/400/600)، تُستهلَك مباشرة عبر `IslamicQuizGame.tsx`.
2. `src/lib/quiz-seed.ts` (`DEMO_QUIZ_QUESTIONS`، 914→926 سؤالاً، حقول
   أغنى `{id,section,category,level,question,answer,status}`) — **هذا هو
   البنك الفعلي المُستهلَك حيّاً**: `getQuizQuestions()` في `supabase.ts`
   تستعلم أولاً جدول DB `quiz_questions` (يُزرَع من هذا الملف عبر
   `upsertQuizSeedToDb()`)، وعند فراغه أو تعطّل الاتصال تسقط تلقائياً على
   هذا الملف مباشرة — ثم `IslamicQuizGame.tsx` يستبدل `poolRef.current`
   الابتدائي (`ALL_QUESTIONS`) بالنتيجة عبر `mergeSupabaseQuestions()`
   بمجرد وصولها. **تأكّد أن دفعة 950→914 السابقة كانت فعلاً تعمل على
   البنك الحي الصحيح، لا ملفاً يتيماً — طمأنة مهمة لعمل جلسة سابقة**.

**اكتشاف الخطر المعماري الحقيقي (طُبِّق الحذر المطلوب)**: `mergeSupabaseQuestions()`
تُحوِّل كل صف DB إلى `{id, q, a, hint}` **فقط** — أي حقل جديد (كـ
`explanation`/`reference`) **لن يصل إطلاقاً لواجهة اللعبة الحية** ما لم
تُعدَّل ثلاث طبقات معاً: مخطط جدول `quiz_questions` في DB، دالة الدمج،
وواجهة عرض السؤال نفسها في `IslamicQuizGame.tsx` — بالضبط نمط الخطر الذي
حذَّر منه المنسّق. **القرار المطابق لتوجيهه صراحة**: أُضيف الحقلان
`explanation`/`reference` **كحقلين اختياريين فقط في نوع `QuizQuestion` في
quiz-seed.ts** (لا تعديل لأي طبقة أخرى) — بيانات جاهزة موثَّقة لكن غير
مُستهلَكة بعد في اللعبة الحية، **نفس نمط `Scholar.sources` المُثبَت هذه
الجلسة بالضبط** (بيانات آمنة تماماً، صفر مخاطرة معمارية، جاهزة لأي عمل
واجهة مستقبلي يريد عرضها).

**12 سؤالاً جديداً موزَّعة على أقسام كانت فعلاً ناقصة** (فُحص التوزيع
الكامل عبر 41 قسماً أولاً بسكربت python مباشر لتحديد الأقسام الحقيقية
الناقصة، لا تخمين): "الآداب الإسلامية" (2→5)، "الحج والعمرة" (2→5)،
"الأنبياء والرسل" (3→6)، "الفرائض والمواريث" (17→20). كل سؤال له
`explanation` (شرح إضافي) و`reference` (مرجع مستقل عن نص الإجابة) —
تحقّق WebSearch مباشر لحقائق فقهية دقيقة قبل الكتابة (أركان الحج الأربعة
مع ذكر خلاف الحنفية صراحة بدل تعميم زائف؛ الفروض الستة المقدَّرة —
صُحِّح فهم أولي خاطئ أثناء البحث: "أصحاب الفروض" 12 شخصاً بخلاف "الفروض"
نفسها 6 كسور، والسؤال صيغ بدقة عن الأخيرة فقط)، وآيتان قرآنيتان
مستخرَجتان حرفياً من الملفات المحلية (العنكبوت:14 مدة دعوة نوح، الأحقاف:35
أولو العزم).

**ملاحظة تشغيلية مهمة اكتُشفت**: أول تشغيل لـ`test:regression` بعد
الإضافة **فشل فعلياً** (`test:counts`) — `content-counts.json` يحتوي رقم
`quizQuestions` ثابتاً يجب تحديثه يدوياً بـ`pnpm run generate:counts` بعد
أي تغيير في عدد الأسئلة، **ليس تلقائياً**. أُصلح فوراً، وأُعيد تشغيل
الدفعة كاملة فنجحت. **درس لأي عمل مستقبلي يغيّر عدد كتب/علماء/فوائد/أسئلة
مباشرة**: شغّل `generate:counts` قبل `test:regression` دائماً.

## أكبر فجوة متبقية الآن — بالأولوية (ابدأ من هنا)

1. **[منجَز]** ~~التحقق الفردي من الجامعات السبع القديمة~~: نُفِّذ
   (`supabase/universities_verify_original7_v1.sql`). اكتُشف وصُحِّح خطأ
   حقيقي: صف `jordan-islamic-sciences` كان `website_url`/`name_en` يشيران
   خطأً لمؤسسة مختلفة تماماً (aabu.edu.jo = جامعة آل البيت، لا صلة مباشرة
   بالاسم العربي المسجَّل) — صُحِّح لـwise.edu.jo (World Islamic Sciences
   and Education University، المؤسسة الحقيقية المقصودة). الست الأخرى
   تحقّقت أنها صحيحة كما هي (IIUM ماليزيا، الجامعة الإسلامية بالمدينة،
   جامعة الإمام محمد بن سعود، جامعة قطر-الشريعة، جامعة أم درمان، جامعة أم
   القرى). **صفر صفوف متبقية بوسم "بيانات تجريبية" الآن من أصل 15.**

2. **[منجَز جزئيًا]** ~~توسيع دفعة daif/mawdu~~: نُفِّذ عبر
   `verified_hadith_daif_mawdu_seed_v2.sql` — الآن daif=13, mawdu=19 (كان
   5/9). لا يزال daif دون هدف 20 بالضبط — فجوة صغيرة متبقية إن أُريد
   استكمالها لاحقًا (أولوية منخفضة الآن).

3. **[منجَز 7/7 — مكتمل بالكامل]** ~~بناء محتوى حقيقي لبقية المسارات
   التعليمية الفارغة~~: كل السبعة مسارات التي كانت `status='draft'` فارغة
   تمامًا بُنيت الآن بمحتوى حقيقي كامل (3 مراحل/مقررات/عناصر مربوطة بكتب
   حقيقية عبر `course_books` لكل مسار): `uloom-quran` (11 عنصراً)،
   `akhlaq` (9)، `adab` (7)، `nahw` (5)، `arabic` (3)، `dawah` (5)،
   `tarbiyah` (6) — إضافة لمقرر `arkan-al-iman` (7 عناصر) داخل مسار
   `aqeedah` الذي كان منشوراً أصلاً. **قرار arabic/nahw**: مساران منفصلان
   (لا مسار واحد) — نُفِّذ وطُبِّق، راجع `supabase/learning_path_arabic_
   nahw_v1.sql`. **قرار dawah/tarbiyah**: لا تصنيف "دعوة"/"تربية" مخصَّص
   في المكتبة — بُنيا بربط كتب من تصنيفات قريبة (سيرة لـdawah، رقائق/آداب
   لـtarbiyah) بدل إضافة كتب جديدة، راجع `supabase/learning_path_dawah_
   tarbiyah_v1.sql` (يوثّق أيضاً استخدام منازل مختلفة تماماً من "مدارج
   السالكين" عمّا استُخدم في adab، لتفادي تكرار المحتوى — تحقَّق عبر
   WebSearch قبل الكتابة).
   **لا عمل متبقٍ في هذا البند** — 8 مسارات تعليمية حقيقية الآن (usool-
   fiqh من جلسة سابقة + 7 هذه الجلسات) من أصل ~19 مطلوبة أصلاً في التكليف
   الكامل.

3ب. **Phase 3 (الموسوعة الهرمية) — لا يزال المتبقي الأكبر**: بعد usool-
   fiqh وaqeedah/arkan-al-iman والمسارات السبعة أعلاه، الأبواب الكبرى ما
   زالت بلا محتوى هرمي عميق (تفصيل درس-فدرس، لا مجرد ربط بكتاب واحد):
   القرآن وعلومه (له الآن مسار uloom-quran لكنه 11 عنصراً فقط عبر 3
   كتب، ليس موسوعة كاملة)، الحديث وعلومه (لا مسار مخصَّص إطلاقاً)، السيرة
   والتاريخ (لا مسار)، الفقه بكل أبوابه (فقط usool-fiqh التمهيدي). هذا
   أكبر فجوة فعلية متبقية من التكليف الأصلي — يحتاج جلسات مخصَّصة متعددة،
   ليس دفعة واحدة. **مرشَّح تالٍ منطقي**: مسار `hadith` أو `fiqh` إن وُجدا
   فارغين (تحقّق `SELECT slug,status,total_sessions FROM learning_paths
   WHERE slug IN ('hadith','fiqh','seerah','tafsir');` أولاً).

4. **[فُحص جزئياً هذه الجلسة] مصادر بيانات مكررة أخرى بنمط /scholars**:
   تحقّقتُ من `LibraryPage.tsx` — **سليمة**، تستورد `LIBRARY_CATEGORIES` من
   `library-catalog.ts` وتستهلك `getLibrary()` التي تدمج DB مع الفهرس
   الثابت بشكل صحيح (`mergeLibraryWithCatalog`/fallback عند فراغ DB) — لا
   تكرار. مسح نطاقي أوسع (`src/views/*.tsx` بحثاً عن مصفوفات محلية كبيرة
   >30 عنصراً، ثم مطابقة الموضوع مع أي `src/lib/*-data.ts`/`*-seed.ts`
   مناظر) لم يجد ملف lib مطابقاً موضوعياً لأي صفحة كبيرة أخرى (HikamSalafPage،
   SahabahPage، DuasPage، IslamicSectsPage، HadithSciencePage وغيرها — كل
   واحدة محتواها الوحيد، لا نسخة lib منافسة). **خلاصة**: حالة /scholars
   على الأرجح كانت استثنائية (نشأت لأن للعلماء صفحتي تفصيل وفهرسة معاً)،
   لا نمطاً متكرراً واسعاً — لكن **لم يُفحص بعد المسارات الديناميكية نفسها**
   (`/scholars/:id`, `/library/:id`) عبر `audit-all-routes.mjs` (يحتاج ملف
   مسارات محقونة القيم الفعلية بدل الأنماط `:id`)، افعل ذلك إن اتّسع الوقت.

5. **[منجَز بنطاق مُصغَّر، مرحلتان]** ~~تنظيف بقايا CMS اليتيمة لـ`fatwas`~~:
   المرحلة الأولى (`dde85da6`): أُزيل "fatwa" من `KnowledgeSourceType` +
   إصلاح رابط ميت كامن. المرحلة الثانية (`a3f05e8d`، بطلب صريح لاحق):
   أُزيل الفرع الميت `case "fatwa"` من `AdminInlineEdit.tsx` (لم يستدعِه
   أي مكوّن بـ`contentType="fatwa"` إطلاقاً — تحقّق بحث شامل قبل الحذف) +
   3 دوال إدارة يتيمة في `platform-supabase.ts`
   (adminGetAllFatwas/adminUpsertFatwa/adminDeleteFatwa). **لم يُلمَس
   عمداً** `CmsContentKind`/`content-registry.ts` الأوسع — يخدم 14 نوع
   محتوى حي آخر، مخاطرة أعلى من فائدتها لجدول فارغ. **تحقّقتُ**:
   `FiqhCouncilStatsPage.tsx` يستخدم `"fatwas"` شرعياً (إحصائية فتاوى
   المجمع الفقهي المؤسسي الحي `/fiqh-council/fatwas`، غير متعلقة بالميزة
   العامة المحذوفة `/fatwa`) — تُرك بلا مساس، استخدام صحيح.

6. **[فُحص جزئيًا]** ~~مراجعة عامة لبقية `fawaid-seed.ts`~~: عيّنة من 28
   اقتباسًا منسوبًا لعلماء/صحابة فُحصت يدويًا (بخلاف الأحاديث النبوية،
   مُغطاة بالكامل سابقًا) — كلها معقولة. **ليست مراجعة شاملة لكل الـ508**،
   الفجوة قائمة لتدقيق كامل لاحق إن أُريد.

7. **[منجَز جزئياً — 64/96]** ~~مراجعة `scholars-data.ts` بعمق أكبر~~:
   أُثري 64 عالماً عبر ثماني دفعات — (1) ابن تيمية، ابن القيم، البخاري،
   الترمذي، النووي، ابن حجر، الذهبي، ابن كثير؛ (2) shafi، ahmad،
   abu-dawud، tabari، ibn-jawzi، ibn-rajab، bayhaki، daraqutni؛ (3)
   muslim، nasai، ibn-majah، hakim، ibn-khuzayma، ibn-hibban،
   sufyan-thawri، ibn-mubarak (commit `ac8aaae3`)؛ (4) abu-hanifa،
   malik، awzai، abu-yusuf، shaybani، ghazali، juwayny، ibn-qudama
   (commit `97ef8b3e`)؛ (5) shawkani، ibn-baz، ibn-uthaymeen، albani،
   muhammad-abdu، rashid-rida، ibn-hazm، ibn-rushd (commit `a66e6598`)؛
   (6) al-saadi، ibn-jibreen، fawzan، ibn-abd-al-wahhab، qaradawi،
   muhammad-ghazali، zuhayli، al-suyuti (commit `4c7dd993`)؛ (7)
   ibn-khaldun، ibn-ashur، shatbi، shanqiti، mubarakfuri، ibn-abidin،
   muzani، afifi (commit `c8658a5c`)؛ (8) ibn-salah، al-mizzi،
   khatib-baghdadi، al-qurtubi، ibn-ghudiyan، shaarawi، muqbil،
   ibn-abd-al-barr (commit `8084dcc0` — المزي يربط بالذهبي/ابن تيمية
   المُثرَيين مسبقاً، ابن غديان يربط ابن باز/الشنقيطي/عفيفي الثلاثة
   المُثرَين مسبقاً، ابن عبد البر شارك نفس شيخ ابن حزم بالضبط [أحمد بن
   محمد بن الجسور] — تلمذة مشتركة تكشف تداخل حلقات قرطبة). **صُحِّح
   أيضاً ادّعاء خاطئ موجود مسبقاً** («عفيفي شيخ ابن باز المفضَّل» —
   لم يثبته البحث، في الدفعة السابعة). **32 من 96 لا يزالون بلا
   `sources`/تفاصيل شيخ-تلميذ (ثلثا العلماء مُثرَون الآن)**. المنهجية
   المُثبَتة (كرِّرها): اختر دفعة 8 علماء بعلاقات متبادلة، ابحث عبر
   WebSearch متزامن مباشر (لا Agent خلفي معلَّق)، `verificationStatus`
   يصبح `"reviewed"` لا `"verified"`. **عند البحث عن سجلات ذات
   `sources: []` فارغة بالضبط بـregex/python: قسّم الملف لمقاطع بحدود
   `id: "..."` أولاً**. **⚠️ عند تحرير `sources: []` بـEdit: تأكَّد أن
   `old_string` يشمل سطر `verificationStatus` التالي أيضاً، وإلا يبقى
   السطر القديم مكرَّراً بعد الاستبدال** (وقع فعلياً هذه الجلسة، التُقِط
   بـtypecheck قبل commit — لا تكرِّر الخطأ). **دروس متراكمة من الدفعات
   4-8**: (أ) عند ورود ادّعاء شيخ/تلميذ متعارض زمنياً مع تواريخ الوفاة/
   الميلاد — اترك الادّعاء. (ب) ثناء متبادل موثَّق بين معاصرين دون تتلمذ
   مباشر — صِفه بدقة كـ"ثناء" لا "تلمذة". (ج) لمصادر معاصرين تجاوزت
   شهرتهم وفاة الزركلي (1976): تجنَّب "الأعلام" واستخدم مواقع رسمية.
   (د) تجنَّب أيضاً الاستشهاد بـ"سير أعلام النبلاء" للذهبي (توفي 748هـ)
   لمن توفي بعده بعقود (ابن خلدون 808هـ، الشاطبي 790هـ) — استخدم مصادر
   معاصرة فعلية (كالإحاطة لابن الخطيب) أو الأعلام للمتأخرين قبل 1976.
   (هـ) عند العثور على ادّعاء موجود مسبقاً يبدو غير مؤكَّد أثناء بحثك عن
   عالم آخر مرتبط — تحقّق منه وصحِّحه فوراً. دفعة تاسعة مرشَّحة: افحص
   الباقين عبر `sources: []` (40 كانوا متبقين قبل الدفعة الثامنة، الآن
   32).

8. **[منجَز جزئياً]** ~~مراجعة `library-catalog.ts` بعمق أكبر~~: 127
   كتاباً الآن (122→123 الرسالة الفقهية، commit `5f09d2e1`؛ 123→124
   المنهاج شرح صحيح مسلم، commit `f16381af`؛ 124→125 حصن المسلم، commit
   `865ba07e`؛ 125→126 القواعد المثلى لابن عثيمين، commit `516d97f8`؛
   126→127 دعوى تعارض السنة مع العلم، commit `16f6ecc9` — آخر ثلاثة
   أُضيفت خصيصاً لربط عناصر تعليمية موجودة مسبقاً بلا كتب مطابقة، نمط
   ناجح مُكرَّر ثلاث مرات: "ابحث عن عناوين عناصر learning_items تطابق
   كتباً غائبة من الفهرس"). **مرشَّح رابع فوري**: "تلخيص مختصر المقنع"
   (مقرر talkhees-muqni في fiqh) — اكتُشف بنفس النمط لكن لم يُتحقَّق
   منه بعد (رصيد WebSearch نفد). لا تزال تحتاج مراجعة تفصيلية أعمق —
   فُحص فقط عدم تكرار id/title وتوزيع الفئات سطحياً. فئات أرق متبقية:
   منطق وفلسفة (2)، علوم القرآن (4 فقط رغم مسار uloom-quran الكامل).

## عوائق مسجَّلة (سطر واحد لكل بند)

- `src/data/library-catalog.json` و`src/data/scholars-list.json` يتيمان
  تمامًا (غير مستوردَين من أي كود) — لا خطر عرض، قرار حذفهما معلَّق.
- `scripts/platform-seed.snapshot.json` لا يزال يحتفظ بمفتاح `"fatwas"`
  (5 سجلات) رغم توقف استهلاكه — بيانات خاملة، إزالتها اختيارية.
- تصنيفان متبقيان من `audit-qa-categories.mjs` (إيجابيات كاذبة للـregex
  غالباً) — لم يُصلحا، مخاطرة منخفضة.
- جدول `library_items` وجدول `books` (DB) شبه فارغان (5 و0 صفوف) وغير
  مستخدَمين فعلياً — المصدر الحقيقي الوحيد لصفحة المكتبة هو
  `src/lib/library-catalog.ts` (117 كتاباً، TS ثابت). لا تفترض أن أي
  عملية DB على `books`/`library_items` تؤثر على ما يُعرض للمستخدم.
- `verified_hadith_items` الـ290 صف الأصلية (sahih) كلها `quality_score=0`
  رغم `trust_level=90` — استيراد آلي دفعة واحدة من fawazahmed0/hadith-api،
  لم تُراجَع فردياً. الحقول غير مستهلَكة بالواجهة حالياً (تحقّقتُ) فلا خطر
  عرض، لكن يستحق تنظيفاً لو استُخدمت لاحقاً في لوحة إدارة.

## الحالة الفعلية عند آخر تحديث (2026-07-18)

### ⚠️ تصحيح مهم لفهم سابق — اقرأ قبل أي عمل على Phase 3

وُثِّق سابقاً في هذا الملف أن "لا مسار حديث مخصَّص إطلاقاً" وأن العقيدة
لها "باب واحد فقط". **هذا خطأ فحص لا حقيقة**: `SELECT slug, status,
total_sessions FROM learning_paths ORDER BY slug;` يُظهر أن مسارات
**hadith (102 جلسة)، fiqh (106)، tafseer (35)، seerah (30)، tawheed
(16)، mustalah-hadith (10→14)** كلها موجودة فعلاً ومنشورة — الفحص
السابق اقتصر خطأً على السبعة المُعلَمة "فارغة تماماً" (`status='draft'`)
فقط، ولم يفحص الجدول كاملاً. **الفحص الصحيح دائماً**: استعلم `learning_
paths` بلا شرط `status` أولاً قبل الحكم بوجود فجوة.

**لكن**: هذه المسارات **ضحلة فعلياً** رغم أرقام `total_sessions` الكبيرة
— فحص `mustalah-hadith` قبل هذه الجلسة كشف مرحلتين ومقررين لكن **عنصر
تعليمي واحد فقط** بينهما (نمط "عنصر عام واحد لكل مقرر" نفسه الموجود سابقاً
في aqeedah قبل إضافة arkan-al-iman). **الفجوة الحقيقية إذن ليست "مسارات
غائبة" بل "عمق ضحل داخل مسارات موجودة"** — تحقّق دائماً من عدد
`learning_items` الفعلي لكل مسار قبل افتراض عمق أو غياب.

**منجَز بالكامل الآن** (تحقّق مباشر عبر git log + DB queries، لا افتراض):
- **7/7 مسارات تعليمية كانت فارغة تماماً** الآن كاملة ومنشورة: uloom-quran،
  akhlaq، adab، arabic، nahw، dawah، tarbiyah — إضافة لمقرر arkan-al-iman
  داخل aqeedah ومسار usool-fiqh التجريبي الأول.
- **Phase 3**: ستة أبواب مفصَّلة الآن — "أركان الإيمان الستة" (aqeedah)،
  "أقسام الحديث: الصحيح والحسن والضعيف" (mustalah-hadith، مرحلة "البناء"
  عبر مقدمة ابن الصلاح — `supabase/learning_path_mustalah_hadith_
  building_v1.sql`)، "أقسام التوحيد الثلاثة" (tawheed، مرحلة "التأسيس"
  جديدة **قبل** المرحلة الموجودة، عبر العقيدة الواسطية لابن تيمية —
  `supabase/learning_path_tawheed_aqsam_v1.sql`، commit `d3eda3da`)،
  "الغزوات الكبرى" (seerah، مرحلة ثالثة جديدة بعد "التوسع"، 4 غزوات عبر
  الرحيق المختوم للمباركفوري — `supabase/learning_path_seerah_maghazi_
  v1.sql`، commit `523afbd0`)، "تفسير سورة يوسف" (tafseer، مقرر ثالث
  داخل مرحلة "تفسير سور مختارة" الموجودة، 4 عناصر تتبع بناء القصة الدرامي
  عبر تفسير ابن كثير — `supabase/learning_path_tafseer_yusuf_v1.sql`،
  commit `254a72a4`)، و"الفقه الحنفي بعمق" (fiqh، مرحلة رابعة جديدة بعد
  "فقه مقارن وتخصصي"، 4 أبواب أساسية — طهارة/صلاة/زكاة/بيوع — عبر الهداية
  شرح بداية المبتدي للمرغيناني — `supabase/learning_path_fiqh_hanafi_
  deep_v1.sql`، commit `bf099d15`). **بهذا اكتمل تعميق كل مسارات دفعة
  الاكتشاف الأصلية الخمسة (hadith/fiqh/tafseer/seerah/tawheed) — كل واحد
  منها له الآن باب واحد على الأقل مربوط فعلياً بكتاب حقيقي**.
  `tawheed.total_sessions`: 16→19، `seerah.total_sessions`: 30→34،
  `tafseer.total_sessions`: 35→39، `fiqh.total_sessions`: 106→110.
- **Phase 6**: 117→127 كتاب (12 كتاباً مُتحقَّقة فردياً؛ آخرها "دعوى
  تعارض السنة مع العلم التجريبي" للهاجري، commit `16f6ecc9`، أضيف
  لربط عنصر تعليمي موجود مسبقاً في مسار hadith بلا كتاب مطابق؛ قبله
  "القواعد المثلى" لابن عثيمين، commit `516d97f8`؛ وقبله "حصن المسلم"
  للقحطاني، commit `865ba07e`).
- **Phase 7**: **64/96 عالم** أُثري (شيخ/تلميذ + مصادر) عبر ثماني دفعات —
  الدفعة الثامنة (ibn-salah، al-mizzi، khatib-baghdadi، al-qurtubi،
  ibn-ghudiyan، shaarawi، muqbil، ibn-abd-al-barr، commit `8084dcc0`)
  أثرت المزي (يربط بالذهبي وابن تيمية المُثرَيين مسبقاً)، ابن غديان
  (يربط ابن باز/الشنقيطي/عفيفي — ثلاثتهم مُثرَون مسبقاً)، وابن عبد البر
  (نفس شيخ ابن حزم بالضبط: أحمد بن محمد بن الجسور — تلمذة مشتركة تكشف
  تداخل حلقات قرطبة). **دقة موثَّقة**: مدخل ابن الصلاح يوضّح صراحة أن
  النووي لم يدركه شخصياً رغم الربط الشائع بينهما. الدفعة السابعة (commit
  `c8658a5c`) أثرت الشنقيطي والمباركفوري والمزني، وصحَّحت ادّعاءً خاطئاً
  موجوداً مسبقاً في مدخل عفيفي («شيخ ابن باز المفضَّل» — لم يثبته البحث).
  **32 عالماً متبقٍ فقط (ثلثا العلماء مُثرَون الآن)**.
- **اكتشاف فجوات جديدة عبر منهجية الاستعلام الشامل (بطلب صريح من
  المنسّق، مُكرَّرة عبر دفعتين متتاليتين)**: الدفعة الأولى كشفت مقرر
  "نخبة الفكر" داخل `mustalah-hadith` فارغاً تماماً (0 عناصر، حالة
  `needs_review` صادقة) ومقرري "صحيح البخاري"/"صحيح مسلم" داخل `hadith`
  بعناصر تطابق الكتابين حرفياً بلا ربط — عولجا معاً (commit `4feca6cf`):
  نخبة الفكر مُلئ بـ4 عناصر (الشاذ/المنكر، المعلَّل، المدرج، المقلوب/
  المضطرب) ورُفعت حالته لـpublished. `mustalah-hadith.total_sessions`:
  14→18. الدفعة الثانية كشفت مقرري "دعوى تعارض السنة مع العلم" (hadith)
  و"تلخيص مختصر المقنع" (fiqh) بنفس النمط — الأول عولج (كتاب أُضيف
  وربط، commit `16f6ecc9`)، **الثاني تُرك بلا مساس عمداً** لأن رصيد
  WebSearch نفد للجلسة قبل التحقق من الكتاب المذكور في عنوان عنصره —
  **لا تخمين بلا تحقق، حتى مع نمط ناجح مُكرَّر**. مرشَّح أول لأي جلسة
  قادمة عند توفر رصيد بحث. **هذا النمط (فحص شامل لكل courses بلا
  course_books دورياً) أثبت فعاليته مرتين متتاليتين** — استمر في تكراره.
- **Phase 9**: 914→943 سؤال. **فُحص طلب المنسّق بتطبيع أسماء الأقسام
  المتجزئة، فتبيَّن أن التجزّؤ الظاهري في `section` الخام (41 اسماً) لا
  يسبّب ضرراً وظيفياً** — طبقة `SECTION_TO_CATEGORY` في
  `src/data/islamicQuizData.ts` توحّدها بالفعل إلى 8 فئات فعلية تُستهلَك
  في اللعبة الحية (quran/hadith/sira/anbiya/fiqh/aqeeda/tarikh/akhlaq)،
  فلم تُعَد التسمية الخام بلا فائدة وظيفية. **لكن الفحص الدقيق لهذه
  الطبقة كشف خللاً حقيقياً**: قسم "أخلاق إسلامية" (بلا "ال") لا يطابق
  المفتاح المسجَّل "الأخلاق الإسلامية"، فكان سؤاله الوحيد **يُسقَط
  صامتاً** من اللعبة الحية عند المزامنة من Supabase — صُحِّح (commit
  `e7bb1c0d`). **توسيع مستهدَف بدقة بعدها**: فحص توزيع الأسئلة عبر
  الفئات الفعلية الثمانية × 3 مستويات كشف أن "صعب" في sira (2) وanbiya
  (4) أرقّ خلية فعلية — أُضيفت 4 أسئلة "صعب" لكل منهما (8 إجمالاً)، كل
  حقيقة تحقّقت عبر WebSearch قبل الكتابة. quizQuestions: 935→943.
  **درس منهجي**: عند فحص "تجزّؤ تسمية" مستقبلاً، افحص دائماً طبقة
  الاستهلاك (mergeSupabaseQuestions/SECTION_TO_CATEGORY) قبل الحكم
  بوجود ضرر وظيفي — الفحص السطحي لـ`section` الخام وحده مضلِّل.
- **Phase 10**: جدول `articles` + **مقالتان**، كلتاهما `is_approved=false`،
  تربطان نطاقين مختلفين عمداً. RLS مُتحقَّق مباشرة: anon يرى 0 صف.
- **Phase 11**: إصلاح فجوة ربط داخلي سيطرية، مُتحقَّق منها بصرياً عبر
  Playwright فعلياً (43/45 رابط يعمل ويتنقّل فعلاً). **بحث موحّد لم يُلمَس بعد**.
- تنظيف بقايا CMS يتيمة لـ`fatwas` (مرحلتان مكتملتان).
- **جلسة سابعة (2026-07-18، بلا WebSearch — رصيد نفد، عمل داخلي بحت
  بطلب صريح من المنسّق)**:
  - **`knowledge_relationships` كان فارغاً كلياً (0 صف)** رغم أن
    `KnowledgeRelatedItems.tsx` مبنيّ بالكامل ومُستهلَك فعلياً في
    `LibraryDetailPage.tsx`/`LessonDetailPage.tsx` — ميزة "مواد مرتبطة"
    كانت ميتة صامتة تماماً (`if (items.length===0) return null`) منذ
    إنشائها. عُبِّئ بـ70 علاقة **مُستمَدة بالكامل من بيانات مُتحقَّقة
    مسبقاً هذه الجلسات** (لا WebSearch جديد): 32 مؤلف_كتاب (مطابقة صارمة
    key_works↔عناوين المكتبة)، 33 شيخ_تلميذ (كل اتجاه مقروء حرفياً من
    نص bio الفعلي)، 5 مرتبط (ثناء/زمالة موثَّقة، ليست تلمذة مباشرة).
    الملف: `supabase/knowledge_relationships_populate_v1.sql`، commit
    `c0fe550d`. **تحذير تقني وقع فعلاً أثناء الكتابة**: صف مكرر واحد
    (`ibn-jawzi`→`ibn-qudama`) كُتب مرتين بالخطأ فرفضه قيد UNIQUE على DB
    (وليس على typecheck) — اكتُشف من رسالة خطأ `supabase db query`
    وأُصلح قبل إعادة المحاولة؛ **تحقّق دائماً من التكرارات برمجياً قبل
    تطبيق أي INSERT جماعي كبير، لا تعتمد على قيد DB وحده كشبكة أمان**.
  - **عطل صامت حقيقي ثانٍ من نفس عائلة "أخلاق إسلامية"** اكتُشف أثناء
    تفعيل `knowledge_relationships` لأول مرة: `TYPE_HREF.scholar` في
    `KnowledgeRelatedItems.tsx` كان يبني
    `` `/lessons?sheikh=${id}` `` — لكن `/lessons` يفلتر عبر
    `sheikhName` (اسم عربي من بيانات الدروس)، بينما `id` هنا هو مُعرِّف
    `scholars-data.ts` الإنجليزي (مثل `ibn-baz`) — لا يتطابقان أبداً،
    فكان الرابط سيُنتج صفحة نتائج فارغة دوماً لا 404 ظاهرة. صُحِّح إلى
    `` `/scholars/${id}` `` (المسار الصحيح المُستهلِك لنفس المعرِّف عبر
    `ScholarProfilePage.tsx`)، نفس commit `c0fe550d`.
  - **بقايا CMS يتيمة إضافية اكتُشفت (لم تُعالَج — صفر تأثير حي)**: حقل
    `publicRoute` في `CONTENT_REGISTRY` (`src/lib/cms/content-registry.ts`)
    مُعرَّف لكل نوع محتوى (بما فيه نفس النمط المعطوب
    `` `/lessons?sheikh=${id}` `` لنوع `sheikh`) لكنه **غير مُستدعى في أي
    مكان بالكود إطلاقاً** (تحقَّق عبر `grep -rn "\.publicRoute(" src/`) —
    سقالة CMS ميتة بالكامل، على الأرجح ميزة "معاينة الصفحة الحية" مخطَّطة
    ولم تُوصَل قط. لا خطر حالياً (غير مستدعاة)، لكن من يوصلها مستقبلاً
    سيرث نفس عطل `sheikh` الصامت — **وثِّق هذا هنا كي لا يُكتشَف من
    الصفر**.
  - **فوائد فقهية جديدة (3) تُعيد استخدام استشهادات مُتحقَّقة سابقاً**:
    آيات المائدة:6 (الوضوء)، النساء:103 (مواقيت الصلاة)، البقرة:275
    (حل البيع وحرمة الربا) — نفس النص العربي المُتحقَّق حرفياً لمسار
    `fiqh-hanafi-deep` هذه الجلسات، قُورن byte-for-byte برمجياً قبل
    الإدراج لاستبعاد أي انحراف كتابي. `src/lib/fawaid-seed.ts`
    (508→511)، commit `54e7ae4f`.
  - **مراجعة اتساق داخلية شاملة** (استشهادات SQL الخمسة + أسئلة المسابقة
    الثمانية المضافة سابقاً + بنية `scholars-data.ts`/`library-catalog.ts`
    كاملة): **لا أخطاء جديدة** — فقط تأكيد أن عطل "أخلاق إسلامية" (مُصلَح
    سابقاً) لم يتكرر، وأن 3 عناوين كتب غير مطابقة في `course_books` هي
    فجوات موثَّقة مسبقاً (سطر 973 أدناه) لا أخطاء جديدة.
  - **تحقّق المنسّق لاحقاً من تجدُّد رصيد WebSearch (نفس الجلسة)**: اختُبر
    فعلياً باستعلام حقيقي (ابن جزي الكلبي الغرناطي) — **لا يزال منتهياً
    بالكامل (0/200)**، لم يتجدَّد. تابع العمل بلا بحث كما طُلب.
  - **9 روابط صامتة إضافية من نفس عائلة `TYPE_HREF.scholar`** اكتُشفت
    بفحص منهجي (`grep` لكل `href`/`navigate()` يبني رابط `?param=قيمة` ثم
    تحقّق مباشر هل الصفحة المستقبِلة تقرأ ذلك الـparam فعلاً): **`/rulings
    ?category=`** (من `RulingDetailPage`/`FiqhPage`)، **`/library?category=
    /?cat=`** (من `LibraryDetailPage` + JSON-LD)، **`/qa?cat=`** (من
    `FiqhPage`)، **`/lessons?search=`** (من بطاقات "مواد ذات صلة" المعروضة
    فعلياً في كل صفحة حكم شرعي عبر `rulings-relations.ts`)، **`/miracles
    /stories/fawaid?cat=`** (JSON-LD ذاتي لكل صفحة)، و**`/arbaeen-nawawi
    ?h=<id>` + `#hadith-<id>`** (كلاهما معطوب معاً — لا قراءة param ولا
    عنصر DOM بـid مطابق). في كل حالة: الرابط يُبنى صحيحاً ويُنقَل إليه
    المستخدم، لكن الصفحة الهدف كانت تُهيِّئ حالة الفلتر من
    `usePersistedState`/`useState` فقط بلا أي مزامنة مع query string
    الفعلي — فيهبط المستخدم على الفلتر الافتراضي/السابق بصمت تام، بلا 404
    وبلا أي خطأ ظاهر. **حالة إضافية مختلفة قليلاً**: أزرار "استعراض حسب
    النوع" في `KnowledgeGraphPage` كانت تستدعي `navigate('/knowledge-graph
    ?type=...')` على صفحة لا تقرأ أي query param إطلاقاً — زر معطَّل
    بالكامل بلا أي أثر عند الضغط؛ صُحِّح بربطه بآلية `setFilterType`
    المحلية العاملة فعلاً (بدل إضافة مسار قراءة URL ثانٍ غير ضروري).
    جميعها صُحِّحت بإضافة `useEffect` بسيط يقرأ الـparam عند التحميل
    ويطبّقه على حالة الفلتر الموجودة أصلاً — لا بنية جديدة. **تبقّى
    عنصران غير معالَجين عمداً**: `UniversitiesPage` (`?country=`) و
    `OccasionsPage` (`?month=`) يحتاجان منطقاً خاصاً (مطابقة قائمة/تمييز
    نطاق تاريخ) لا مجرد نسخ النمط — مرشَّحان لدفعة قادمة. commit
    `730c251e`.
  - **7 فوائد فقهية/عقدية إضافية** تُعيد استخدام استشهادات قرآنية
    مُتحقَّقة سابقاً هذه الجلسات (المائدة:6، النساء:103، البقرة:275،
    الفاتحة:2، النحل:36، الأعراف:180، التوبة:103) — كل نص قُورن
    byte-for-byte برمجياً بمصدره في ملفات SQL قبل الإدراج. `fawaid`:
    508→515 (commits `54e7ae4f`، `952d373c`).

**لم يُلمَس بعد إطلاقاً** (فجوات حقيقية كبيرة متبقية):
- ~~6 تصنيفات فارغة في موسوعة الأحكام~~ **[مُغلَقة — الجلسة التاسعة]**.
  **فجوة أصغر مشابهة لا تزال قائمة**: تصنيف "النوازل المعاصرة" في المجمع
  الفقهي (`FIQH_COUNCIL_CATEGORIES`) لا يزال بلا محتوى (تحقَّق من DB
  الحية مباشرة سابقًا)، أقل إلحاحاً (تصنيف واحد فقط، محتوى مؤسسي أعقد
  مصدراً من موسوعة الأحكام).
- موسوعة العلوم الشرعية العميقة — سبعة أبواب فقط من عشرات الأبواب
  الممكنة داخل مسارات موجودة فعلاً. كل مسار من الخمسة الأصليين له باب
  عميق واحد على الأقل الآن (mustalah-hadith صار له اثنان)، لكن بقية
  مقرراته لا تزال ضحلة — راجع القائمة الكاملة غير المعالَجة في بند 3
  أسفل "المهمة التالية بدقة".
- بحث موحّد عبر كل أنواع المحتوى (جزء من Phase 11).
- ~~32 من 96 عالماً بلا مصادر/تفاصيل شيخ-تلميذ~~ **[مُغلَقة بالكامل —
  الجلسة التاسعة، 96/96 الآن]**.
- مراجعة عميقة لـ127 كتاباً و515 فائدة (فُحص فقط سطحياً/عيّنة).
- `knowledge_relationships` عُبِّئ الآن بـ153 علاقة عبر 5 من 6 أنواع
  (مؤلف_كتاب 32، شيخ_تلميذ 56، شرح_لكتاب 5، درس_عن_كتاب 6، فائدة↔كتاب 49،
  مرتبط عالم↔عالم 5؛ فتوى_في_باب متروك عمداً — الفتوى محذوفة من التطبيق).
  **مؤلف_كتاب وfawaid↔book استُنفِدت بالفحص المباشر** (سكربتا مطابقة
  صارمة أُعيدا تشغيلهما فلم ينتجا مرشحاً جديداً). **شيخ_تلميذ ثبت أنه
  غير مستنفَد فعلياً** (خلافاً لما كان مُفترضاً سابقاً) — كل دفعة إثراء
  علماء جديدة (بيو جديد يُكتَب) كشفت علاقات إضافية حقيقية؛ 17 علاقة جديدة
  الجلسة التاسعة وحدها رغم "استنفاد" مزعوم سابق. **درس**: الاستنفاد كان
  محصوراً بالبيانات الثابتة وقت الفحص، لا مطلقاً — إعادة الفحص بعد أي
  تحديث بيو تستحق العناء. **لا يزال غير مُغطّى**:
  `question`↔`fawaid` — يحتاج تصميم مطابقة جديد (لا حقل نصي مباشر يربط
  سؤالاً بفائدة بنفس سهولة حقل `source` في fawaid-seed.ts).
- **مقرر "تلخيص مختصر المقنع" (fiqh)**: عنصر تعليمي موجود يُسمّي كتاباً
  بعينه («تلخيص مختصر المقنع») غير موجود في `library-catalog.ts`، بنفس
  نمط دعوى تعارض السنة/القواعد المثلى الناجح — **لم يُتحقَّق منه بعد**
  لأن رصيد WebSearch نفد للجلسة (200/200) قبل الوصول إليه. مرشَّح أول
  فوري لأي جلسة قادمة، لا حاجة لإعادة اكتشافه.

## المهمة التالية بدقة

بالأولوية (اختر الأنضج حسب فحصك عند الاستئناف، لا حاجة لالتزام صارم
بالترتيب):

1. **[منجَز]** ~~مقالة أصلية ثانية~~ — "منهج النبي ﷺ في الدعوة إلى الله".
   **مقالة ثالثة مرشَّحة إن أُريد**: "آداب طلب العلم" تربط
   `/adab-talab-ilm` الموجودة مسبقاً + مسار `adab`.

2. **[منجَز بالكامل]** ~~دفعة ثامنة إثراء علماء~~ ثم ~~دفعات تاسعة-عاشرة-
   حادية عشرة-ثانية عشرة~~ — **96/96 عالماً الآن مُثرى بالكامل**
   (`sources` غير فارغ للجميع). آخر 4 دفعات (32 عالماً) عبر `WebFetch`
   من ويكيبيديا العربية (لا WebSearch، كان منتهيًا)، commits `b6b65564`→
   `c062408a`→`5f7bcea1`→`b3b4da2a`. **لا حاجة لأي دفعة إثراء علماء أخرى
   لهذا الهدف** — الأولوية التالية إن أُريد التعمُّق أكثر هي مراجعة جودة
   الـ32 المُثراة حديثًا فردياً (لا إعادة اكتشاف من هو فارغ).
   **تذكَّر أثناء التحرير**: عند استبدال `sources: []` بمصفوفة، تأكَّد
   أن `old_string` يشمل سطر `verificationStatus` التالي أيضاً (وليس
   فقط سطر `sources`) لتفادي تكرار السطر عن طريق الخطأ — وقع هذا فعلاً
   هذه الدفعة والتُقِط بواسطة `pnpm run typecheck` قبل أي commit، لكن
   الأسلم عدم وقوعه أصلاً.

3. **[منجَز شبه كامل — نمط ناجح مُكرَّر 3 مرات]** ~~اكتشاف فجوات جديدة~~:
   استعلام شامل (`SELECT lp.slug, c.slug, count(li.id) AS items,
   count(cb.id) AS books FROM learning_paths lp JOIN path_stages ps ON
   ps.path_id=lp.id JOIN courses c ON c.stage_id=ps.id LEFT JOIN
   course_units cu ON cu.course_id=c.id LEFT JOIN learning_items li ON
   li.unit_id=cu.id LEFT JOIN course_books cb ON cb.learning_item_id=
   li.id GROUP BY lp.slug, c.slug HAVING count(cb.id)=0`) طُبِّق 3 مرات
   وكشف: نخبة الفكر (فارغ تماماً، عُولج)، صحيح البخاري/مسلم (عُولجا)،
   دعوى تعارض السنة (عُولج)، **الجلسة التاسعة (تكملة) عالجت 8 من آخر 10**
   (`fiqh-ibadat`/`fiqh-shafii`/`fiqh-hanafi`، `seerah-intro`/`seerah-
   deep`، `tafsir-general`/`tafsir-kahf`/`tafsir-nahl` — رُبطت جميعها
   بكتب حقيقية موجودة أو مُتحقَّق منها حديثاً عبر `course_books`، commit
   `43149f58`). **2 متبقيان عمداً بلا معالجة** (ثقة منخفضة، لا تخمين):
   `fiqh-women` (لم يُعثر على كتاب فقه نسائي معروف بثقة كافية) و
   `talkhees-muqni` (نتيجة WebFetch وحيدة لمؤلف غير مؤكَّد، رُفضت مرتين
   الآن). **الاستعلام الآن يعيد صفين فقط** — إن ظهر كتاب فقه نسائي موثوق
   الهوية لاحقاً (مؤلف معروف، تحقُّق مصدرين مستقلين على الأقل) يمكن إغلاق
   fiqh-women، لكن لا تخمين تحت أي ضغط لإكمال العدد.

4. **[منجَز — قرار مدروس، لا تسرّع]** ~~توحيد أقسام بنك الأسئلة~~: فُحصت
   طبقة `SECTION_TO_CATEGORY` في `islamicQuizData.ts` فتبيَّن أن تجزّؤ
   `section` الخام لا ضرر وظيفي منه (مُوحَّد فعلياً لـ8 فئات في اللعبة
   الحية). **لا حاجة لعمل توحيد تسمية إضافي** — الفحص اكتمل والنتيجة:
   لا فجوة حقيقية هنا. ما بقي فعلياً مفيداً: مراقبة أي `section` جديد
   يُضاف مستقبلاً للتأكد أنه موجود في `SECTION_TO_CATEGORY` (وإلا
   سيُسقَط صامتاً من اللعبة الحية كما حدث مع "أخلاق إسلامية" المُصحَّحة).

5. **دفعة رابعة أسئلة مسابقة (توسيع مستهدَف بدقة)**: بعد إضافة 8 أسئلة
   "صعب" لـsira/anbiya هذه الجلسة، أعد حساب توزيع الفئات الثمانية ×
   المستويات الثلاثة (`SELECT section→catId عبر SECTION_TO_CATEGORY ثم
   count بـ level`) لتحديد الخلية الأرقّ التالية. لا تفحص `section`
   الخام وحده — افحص عبر طبقة التوحيد دائماً.

**⚠️ ملاحظة تشغيلية (جلسة سادسة)**: رصيد WebSearch نفد بالكامل (200/200)
قرب نهاية الجلسة — أي عمل يحتاج تحقّقاً جديداً (علماء/كتب/حقائق تاريخية)
يجب تأجيله لجلسة جديدة برصيد بحث متجدِّد، لا محاولة الاستمرار بلا تحقّق.

**✅ جلسة سابعة (2026-07-18) نُفِّذت بالكامل بلا WebSearch** كما طُلب
صراحةً من المنسّق، عبر 4 مهام بديلة لا تحتاج مصادر جديدة: (1) مراجعة
اتساق داخلية — لا أخطاء جديدة، (2) تعبئة `knowledge_relationships` (كان
فارغاً، 70 علاقة الآن) + إصلاح عطل `TYPE_HREF.scholar` الصامت، (3) رصد
بقايا CMS يتيمة (`publicRoute` غير المُستدعى في `content-registry.ts`)،
(4) 3 فوائد فقهية جديدة تُعيد استخدام استشهادات مُتحقَّقة سابقاً. راجع
تفصيل كامل أعلاه في قسم "جلسة سابعة".

**✅ تحقّق المنسّق من تجدُّد WebSearch لاحقاً في نفس الجلسة السابعة —
اختُبر فعلياً، لا يزال منتهياً (0/200)**. تابع العمل التلقائي بلا بحث:
اكتُشفت وأُصلحت 9 روابط صامتة إضافية من نفس عائلة `TYPE_HREF.scholar`
(تفصيل كامل أعلاه، commit `730c251e`)، وأُضيفت 4 فوائد أخرى تُعيد
استخدام استشهادات مُتحقَّقة (511→515، commit `952d373c`). **افحص رصيد
WebSearch من جديد أولاً عند الاستئناف التالي** — قد يتجدَّد بنافذة زمنية
مختلفة عن حد الجلسة الحالي؛ إن تجدَّد، المرشَّح الأول الفوري هو "تلخيص
مختصر المقنع" (بند "لم يُلمَس بعد" أعلاه) ثم الدفعة التاسعة من إثراء
العلماء (32 متبقٍ، القائمة الكاملة بالأسماء متوفرة عبر فحص `sources: []`
في `scholars-data.ts`). إن كان لا يزال منتهياً: `UniversitiesPage`
(`?country=`) و`OccasionsPage` (`?month=`) هما آخر رابطين صامتَين
معروفَين لم يُعالَجا بعد (يحتاجان منطق مطابقة/نطاق تاريخ مخصَّصاً)،
ومراجعة 127 كتاباً و515 فائدة فرداً فرداً لم تبدأ بعد.

**✅ المنسّق طلب صراحةً: عدم إعادة اختبار WebSearch كل دفعة (يستهلك وقتاً
بلا فائدة إن كان لا يتجدد بسرعة) — فحصه مرة كل عدة دفعات فقط من الآن
فصاعداً.** تابع تلقائياً بلا بحث خارجي، أُنجز في نفس الجلسة السابعة:
- **`UniversitiesPage`/`OccasionsPage` أُكملا** بنفس منهجية الروابط
  الصامتة (`?country=`/`?month=` من JSON-LD كل صفحة يُقرآن الآن عند
  التحميل). لا رابط صامت معروف متبقٍ من قائمة الفحص الأصلية.
- **اكتشاف أكبر من نفس عائلة "أخلاق إسلامية" لكن على نطاق أوسع بكثير**:
  فحص كل تصنيف مُعرَّف (`XXX_CATEGORIES`) في كل صفحة فلترة مقابل القيم
  الفعلية المستخدَمة في بيانات المحتوى كشف أن **142 من 515 فائدة (~27%)**
  في `fawaid-seed.ts` تحمل قيم `category` لا تطابق أياً من 19 شريحة
  فلترة معروضة فعلياً في `FawaidPage.tsx` (تُبنى من `FAWAID_CATEGORIES`
  المستورَدة من `fawaid-curated-seed.ts` + `LEGACY_CATEGORIES` محليّة —
  **وليس** من `FAWAID_CATEGORIES` المُصدَّرة من `fawaid-seed.ts` نفسها،
  وهي مُصدَّرة لكن غير مُستخدَمة إطلاقاً في الصفحة الحية!). و**11 من 127
  كتاباً** في `library-catalog.ts` بنفس النمط (تصنيفات فرعية مثل "فقه
  حنفي/شافعي/مالكي" لا تطابق أياً من 14 شريحة `LibraryPage`). كلاهما
  صُحِّح: 126 فائدة أُعيدت تسميتها لتصنيف قائم، 16 حصلت على شريحة جديدة
  أصيلة ("فوائد تاريخية") + توحيد صيغتَي تكرار ("الرقائق"، "الذكر
  والدعاء")؛ الكتب الـ11 أُعيدت تسميتها بالكامل لتصنيفات قائمة (لا شرائح
  جديدة — 2-3 كتب لكل مذهب لا تبرر شريحة مستقلة). **تحقّق برمجي: 0
  تصنيف غير مرئي متبقٍ في كلا الملفين.** commit `19ac1015`.
  **حالة مشابهة اكتُشفت ولم تُعالَج**: `qa-seed.ts` (31 سؤالاً بـ
  `category_id: "seed-cat-fiqh"` غير موجود في `QA_CATEGORIES` المحلية
  بالملف نفسه) — **لم تُعالَج عمداً** لأن الملف auto-generated صراحة
  ("do not edit manually")، وتحقَّق مباشرة من DB الحية أن جدول
  `qa_categories` يحوي فعلاً فئة "الفقه" — فالعطل يمس فقط مسار fallback
  المحلي/offline (عند تعطّل Supabase)، لا التجربة الحية الفعلية.
  **درس منهجي لأي جلسة قادمة**: عند فحص أي صفحة فلترة، قارن دائماً بين
  مصدر شرائح الفلتر الفعلي (الذي يستورده المكوّن حرفياً، قد يكون ملفاً
  مختلفاً كلياً عن ملف بيانات المحتوى نفسه كما حدث هنا) وبين القيم
  الفعلية في بيانات المحتوى — لا تفترض تطابقهما، وتحقَّق برمجياً لا
  بالعين فقط، فالمقياس هنا (515 فائدة) أكبر من أن يُفحَص يدوياً.
- **5 علاقات "شرح_لكتاب" (كتاب↔كتاب) جديدة** في `knowledge_relationships`
  — أول استخدام لنوع علاقة كان معرَّفاً في `RelationshipsSection.tsx`
  لكن لم يُستخدَم قط (فتح الباري→صحيح البخاري، المنهاج شرح مسلم→صحيح
  مسلم، فتح المجيد→كتاب التوحيد، شرح الأربعين لابن دقيق→الأربعون
  النووية، شرح العقيدة الطحاوية→العقيدة الطحاوية) — كل زوج مُستخرَج من
  عنوان الكتاب نفسه في `library-catalog.ts`، مُتحقَّق برمجياً أن كلا
  المعرِّفين (شرح ومتن) موجودان فعلاً. 5 من أصل 10 عناوين "شرح" في
  الفهرس لها متن مُفهرَس مستقل؛ الباقي (المهذب، تقريب النواوي، بداية
  المبتدي، الدرة المضية) غير مُفهرَس كسجل مستقل فلا هدف صالح له حالياً.
  **تصحيح**: "زاد المستقنع" *كان* يُظَن غير مُفهرَس، لكن تبيَّن لاحقاً
  (جلسة درس_عن_كتاب أدناه) أنه مفهرَس فعلاً بعنوان أطول ("زاد المستقنع
  في اختصار المقنع"، `book-zaad-mustaqni`) — درس منهجي: تحقَّق من عناوين
  جزئية/بادئة قبل الحكم بغياب كتاب من الفهرس، لا مطابقة حرفية كاملة فقط.
  commit `9e00909e`.
- **جلسة ثامنة (2026-07-18، تكملة، بلا WebSearch)**: بطلب المنسّق، طُبِّقت
  منهجية "مصدر الفلتر الفعلي مقابل بيانات المحتوى" على صفحات إضافية —
  الدروس (نظيف، تحقَّق مباشرة من DB الحية: 5 تصنيفات فعلية تطابق
  KUWAIT_CATEGORIES بالكامل)، الحكم والاقتباسات (`HikamSalafPage.tsx`،
  نظيف)، خرائط ذهنية (`mind-maps-data.ts`، نظيف)، متشابهات القرآن
  (`mutashabihat-data.ts`، نظيف)، الطب النبوي (`prophetic-medicine-seed.
  ts`، نظيف)، أسماء الله الحسنى (`asma-husna-data.ts`، نظيف).
  **الأذكار**: 10 من 305 ذكر (categoryId "adh-salawat"×5 و"adh-general"×5)
  غير موجودة في `ADHKAR_CATEGORIES` فتختفي صامتاً من فلاتر `AdhkarPage`
  — أُضيفت فئة أصيلة جديدة "الصلاة على النبي ﷺ" (نظير الاستغفار: جنس
  ذكر مستقل يستحق فئة خاصة)، وطُوي "adh-general" في "adh-misc" القائمة.
  **موسوعة الأحكام (اكتشاف أعمق من نفس العائلة)**: تتبُّع جذر المشكلة
  كشف نظامَي تصنيف متوازيين تماماً لموسوعة الأحكام — `RULINGS_CATEGORY_
  TREE` الحديث (20 تصنيفاً، `rulings-categories.ts`، المصدر الفعلي
  لفلاتر `RulingsPage` الحية) و`RulingCategory`/`RULING_CATEGORIES`
  القديم (13 تصنيفاً بأسماء أقصر، `platform-types.ts`، يُقيِّد نوع
  `rulings-seed.ts` حرفياً عبر TypeScript). 19 حكماً في `rulings-seed.ts`
  استخدمت الأسماء القديمة القصيرة ("الحج"، "المواريث"، "النوازل" بدل
  "الحج والعمرة"/"المواريث والوصايا"/"النوازل المعاصرة") فاختفت صامتاً
  من كل فلاتر `RulingsPage` الحية. **اكتُشف أيضاً أثناء التتبُّع**: نموذج
  تحرير حكم في لوحة الإدارة (`RulingsSection.tsx`) كان يعرض القائمتين
  معاً (13 قديمة + 20 حديثة = 33 خياراً متكرراً) في نفس `<select>` —
  فبإمكان أي إداري اختيار تصنيف قديم عن غير قصد وإعادة إنتاج نفس العطل
  لأي حكم جديد. أُصلح الجذر بالكامل: 19 حكماً أُعيد تصنيفها، `RulingCategory`
  حُدِّث ليطابق التصنيف الحالي حرفياً (20 قيمة)، وأُزيلت القائمة القديمة
  من نموذج الإدارة (خيار واحد صحيح الآن لا 33 متكرراً). **تحقَّق محلياً**
  (14 chunk صحيح بدل 16 مع 3 تكرارات يتيمة) ثم **أُرجِع الناتج المُولَّد**
  (`public/data/rulings-encyclopedia/*`) حسب الانضباط القياسي — يحمل
  `generated_at`/`updated_at` يتغيّران كل بناء، الإصلاح الدائم في ملفات
  المصدر فقط، سيتولَّد صحيحاً تلقائياً في أي بناء فعلي قادم. **اكتشاف
  إضافي (فجوة محتوى لا بق كود)**: 6 تصنيفات في الشجرة الحالية بلا أي حكم
  إطلاقاً (القضاء والحدود، الأطعمة والأشربة، الجنائز، الجهاد والسياسة
  الشرعية، اللباس والزينة، الأيمان والنذور) — يحتاج محتوى فقهي جديد
  مُتحقَّق منه، لا إصلاح كود؛ مرشَّح لجلسة WebSearch قادمة.
  `FATWA_CATEGORIES` (قائمة شقيقة قديمة أخرى) تحقَّقتُ أنها **بلا أي
  مُستهلِك إطلاقاً** — بقية ميتة موثَّقة من إزالة الفتاوى سابقاً، لا حاجة
  لمسّها. commit `2dcddead`.
  **6 علاقات "درس_عن_كتاب" (درس↔كتاب) جديدة**: خامس نوع علاقة من أصل 6
  في `RelationshipsSection.tsx` يُستخدَم لأول مرة — كل زوج مُطابَق مباشرة
  بين عنوان درس حقيقي في جدول `lessons` الحي (استعلام مباشر) وعنوان كتاب
  حقيقي في `library-catalog.ts` (رياض الصالحين، كتاب التوحيد، صحيح
  البخاري، صحيح مسلم ×2، زاد المستقنع). **النوع السادس والأخير
  "فتوى_في_باب" مُتروك عمداً بلا استخدام** لأن الفتوى حُذفت من التطبيق
  كلياً. `knowledge_relationships`: 70→81 علاقة إجمالاً. commit `7982fe9b`.
  **رصيد WebSearch أُعيد اختباره مرة واحدة بطلب المنسّق (لا كل دفعة) —
  لا يزال منتهياً (0/200)، تحقَّق باستعلام حي فعلي.**
- **جلسة ثامنة (تكملة أخيرة)**: وسِّع فحص التصنيفات لصفحات إضافية
  (الأخلاق/الأدعية القرآنية/الأدعية العامة/فضائل الأعمال/علوم الحديث/
  الفرق الإسلامية/التجويد/الصحابة/السنن اليومية/البحث الأكاديمي — كلها
  نظيفة، بعضها بأمان النوع TS الصارم مثل `SahabiCategory`/`DuaCategory`
  فلا خطر مطابقة أصلاً)، والمجمع الفقهي (`fiqh_council_issues`/
  `fiqh_council_items` الحيّتان، تحقَّق مباشر من DB: 0 تصنيف غير مرئي؛
  "النوازل المعاصرة" فقط بلا محتوى — فجوة محتوى صغيرة أخرى، ليست بقاً).
  **صفحة الحديث (`HadithPage.tsx`) لها معمارية مختلفة عمداً**: تصنيفاتها
  تُطابَق بكلمات مفتاحية (`keys`) داخل نص الحديث لا بحقل category حرفي —
  نمط أكثر تسامحاً بطبيعته (لا عطل "اختفاء صامت" من نفس النوع)، تُرك
  بلا تعديل بحكم اختلاف معماريتها لا إهمالاً.
  **إعادة تعدين knowledge_relationships**: أُعيد تشغيل سكربت المطابقة
  الصارم لـ"مؤلف_كتاب" على الحالة الحالية لـ`scholars-data.ts` — **صفر
  مرشّح جديد** (الدفعة الأصلية 32 استنفدت كل التطابقات الواثقة فعلاً).
  فحص يدوي إضافي لنصوص bio كل الـ96 عالماً بحثاً عن عبارات "تتلمذ على"/
  "من شيوخه"/"أخذ عنه" غير المُستغَلة كشف **6 علاقات شيخ_تلميذ جديدة**
  (كل واحدة تحقَّق أنها غير موجودة مسبقاً عبر استعلام SQL قبل الإدراج):
  الذهبي→ابن كثير، مسلم→الترمذي، أبو داود→الترمذي، أبو حنيفة→أبو يوسف،
  سفيان بن عيينة→الشافعي، سفيان بن عيينة→أحمد. **استُبعد عمداً**: ابن عبد
  الوهاب/ابن تيمية (تأثر فكري عبر قرون لا تلمذة مباشرة)، والأوزاعي/مالك
  (قائمة رواة عامة بلا سرد تلمذة واضح، بخلاف علاقة الأوزاعي/سفيان الثوري
  الموجودة فعلاً التي لها قصة لقاء محدَّدة). `knowledge_relationships`:
  81→87 علاقة إجمالاً. commit `ce60125b`.

5. **مراجعة عميقة لـ127 كتاباً و515 فائدة** — لم تُفحص فرداً فرداً بعد،
   فقط تكرار سطحي. **فحص سريع للفئات الأرق المتبقية** (`grep -oP
   'category: "[^"]+"'`) يُظهر: منطق وفلسفة (2)، علوم القرآن (4 فقط رغم
   مسار uloom-quran الكامل) — مرشحات لإضافات فردية مُتحقَّقة إن أُريدت
   المتابعة على نمط حصن المسلم/القواعد المثلى.

6. **[منجَز بالكامل — تصحيح توثيق قديم]** ~~تعبئة `knowledge_relationships`~~:
   تحقَّق مباشرة من DB الحية (بطلب المنسّق ضمن جولة "أي فجوة تكتشفها"):
   153 علاقة إجمالاً عبر **5 من 6 أنواع فعلية** — شيخ_تلميذ (56)،
   **fawaid↔book "مرتبط" (49، منجَز فعلاً خلافاً لما وثَّقته سطور سابقة
   من هذا الملف بأنه "لم يُملأ بعد" — كان ذلك توثيقاً قديماً غير محدَّث،
   صُحِّح الآن)**، مؤلف_كتاب (32)، **lesson↔book "درس_عن_كتاب" (6، منجَز
   أيضاً)**، scholar↔scholar "مرتبط" (5)، شرح_لكتاب (5). النوع السادس
   والأخير "فتوى_في_باب" متروك عمداً بلا استخدام (الفتوى محذوفة من
   التطبيق كلياً). لا عمل متبقٍّ حقيقي هنا.

- **جلسة تاسعة (تكملة، 2026-07-18)**: بعد إغلاق مقرري "تفسير الكهف"
  و"الأربعون النووية"، رجع التركيز لبنك الأسئلة (Phase 9) بنفس منهجية
  "لا تفترض، تحقق مباشرة من DB الحية" — بدل افتراض أن sira/anbiya لا
  تزال أرقّ خلية صعب (كما في الدفعة الثالثة الموثَّقة أعلاه)، شُغِّل
  استعلام حي فعلي (`SELECT section, level, count(*) FROM quiz_questions
  WHERE is_published=true GROUP BY section, level`) ومُرِّر عبر إعادة
  تطبيق بايثون لدالة `SECTION_TO_CATEGORY` الحقيقية من
  `islamicQuizData.ts` لحساب التوزيع الفعلي لكل فئة لعبة × مستوى. تبيَّن
  أن sira/anbiya عند صعب **استُهدفتا فعلاً** في الدفعة الثالثة
  (demo-quiz-972..979، غير موثَّقة بوضوح كافٍ سابقاً)، بينما aqeeda/صعب
  بقيت الأرقّ فعلياً (8 فقط، سؤال واحد قديم فقط: demo-quiz-943). تفادى
  هذا الفحص المباشر تكرار محتوى موجود فعلاً — بالضبط الخطر الذي حذَّر منه
  المنسّق.
  أُضيفت **5 أسئلة جديدة** (`demo-quiz-980`..`984`) بمستوى صعب/العقيدة:
  الفرق بين توحيد الأسماء والصفات والألوهية، ضلال الجهمية في الصفات،
  مراتب الإيمان بالقدر الأربع، حكم جحد ركن من أركان الإيمان الستة، معنى
  الولاء والبراء — مستندة لمراجع عقدية سلفية قياسية (شرح الواسطية لابن
  عثيمين، شرح الطحاوية لابن أبي العز، شفاء العليل لابن القيم) لحقائق
  تصنيفية مستقرة غير خلافية، فلم تتطلب تحقق WebFetch حديثي جديد (خلافاً
  لمحتوى الأحكام/السير السابق الذي يتضمن استشهادات نصية مباشرة).
  `quiz-seed.ts`: 943→948 سؤالاً. طُبِّق التزامن على القاعدة الحية عبر
  `scripts/gen-quiz-sync-sql.mjs` (يُعيد توليد SQL دفعي كامل بأمان
  `ON CONFLICT (question) DO NOTHING`، آمن لإعادة التشغيل الكامل) —
  **`quiz_questions`: 953→958 صفاً**، تحقَّق العدد مباشرة قبل وبعد
  التطبيق (لا افتراض). `content-counts.json` حُدِّث
  (`quizQuestions: 943→948`) عبر `pnpm run generate:counts` **قبل**
  `test:regression` كالمعتاد. كل البوابات الأربع (typecheck/lint/
  test:regression 16 حزمة/build) نجحت معًا. commit `47e3cb82`، مدفوع
  فعلياً لـ`origin/majalis-content-fill`.

- **جلسة تاسعة (تكملة ثانية، 2026-07-18)**: بطلب المنسّق، استُعلِم مباشرة
  عن كل `course_units` المنشورة التي تحوي ≤2 عنصر (`learning_items`) عبر
  `courses.stage_id → path_stages.path_id → learning_paths` (الربط
  الصحيح، لا `courses.path_id` المباشر الذي لا وجود له فعلياً — خطأ
  استعلام أول صُحِّح فوراً). كشف الاستعلام **21 مقرراً ضحلاً** بعنصر واحد
  أو اثنين عبر aqeedah/fiqh/اللغة العربية/النحو/مصطلح الحديث/السيرة —
  مخزون كبير من عمل مماثل متبقٍّ.
  اختير مقرر "كتاب التوحيد" (aqeedah، وحدة `d17b0e0f...`، كان عنصراً
  واحداً "شرح كتاب التوحيد" بوزن 100) لأن الكتاب المرجعي (`book-tawhid`)
  موجود مسبقاً في الفهرس بعنوان مطابق حرفياً لآلية الربط
  `LIBRARY_TITLE_TO_ID`. أُضيفت 4 عناصر لأبواب حقيقية من متن الكتاب
  الأصلي لمحمد بن عبد الوهاب — كل استشهاد تحقَّق فردياً عبر WebFetch (بعد
  فشل الوصول لـ`sunnah.com` بـ403، استُخدم البحث داخل ويكي مصدر العربية
  كبديل ناجح):
  1. باب تفسير التوحيد وشهادة أن لا إله إلا الله — حديث معاذ بن جبل «فإن
     حق الله على العباد أن يعبدوه ولا يشركوا به شيئاً» (رواه مسلم، كتاب
     الإيمان، حديث 30 — رقم مؤكَّد من النص الكامل بالسند).
  2. باب من حقق التوحيد دخل الجنة بغير حساب — حديث السبعين ألفاً (متفق
     عليه عن ابن عباس/عمران بن حصين/أبي هريرة، بلا رقم مؤكَّد — لم يُقدَّم
     رقم غير موثوق).
  3. باب الخوف من الشرك — آية إبراهيم:35 (تحقَّقت حرفياً من
     `public/data/quran/surah-014.json`، `numberInSurah: 35`) + حديث
     الشرك الأصغر (رواه أحمد عن محمود بن لبيد) — **ذُكر صراحة في الوصف
     أن في سنده كلاماً بين أهل العلم** بدل تصحيحه بلا تحفظ، بنفس انضباط
     عرض الخلاف الحديثي الصريح المُتَّبع سابقاً مع حديث "طلب العلم
     فريضة".
  4. باب من الشرك: الذبح لغير الله — حديث «لعن الله من ذبح لغير الله»
     (رواه مسلم عن علي بن أبي طالب، كتاب الأضاحي).
  أُعيد توازن الوزن (5×20=100)، `learning_paths.total_sessions` لـaqeedah:
  58→62 (تحقَّق مباشرة من DB الحية قبل وبعد التطبيق). كل البوابات الأربع
  نجحت معًا. commit `9e8a4856`، مدفوع فعلياً. **20 مقرراً ضحلاً آخر لا
  يزال مرشحاً لنفس المعالجة** (قائمة كاملة في نتيجة الاستعلام، لم تُحفَظ
  كملف منفصل — يُعاد الاستعلام بسهولة عند الحاجة).

- **جلسة تاسعة (تكملة ثالثة، 2026-07-18)**: بطلب المنسّق ("بأولوية لأهمها
  علميًا")، اختير مقرر "صحيح البخاري" (hadith path، وحدة `c6a272b8...`،
  كان عنصراً واحداً عاماً بوزن 100) من قائمة الـ20 مقرراً السطحياً
  المتبقية — لأن صحيح البخاري أصح كتاب بعد كتاب الله عند جمهور أهل العلم،
  الأولوية العلمية الأوضح. قبل الاختيار، قُورنت الأحاديث المرشَّحة بكل
  عناوين ونصوص الـ42 حديثاً في `arbaeen-nawawi-seed.ts` (نفس مسار الحديث)
  لتفادي التكرار — استُبعد مرشّح أولي (قصة الثلاثة أصحاب الغار) لأن
  التحقق عبر WebFetch أكَّد وجوده في صحيح مسلم فقط بثقة كافية دون تأكيد
  مباشر لموضعه في صحيح البخاري تحديداً، فاستُبدل بأربعة أحاديث أخرى
  تحقَّق موضعها في صحيح البخاري تحديداً بثقة كاملة:
  1. حديث السبعة الذين يظلهم الله في ظله — الأصناف السبعة الكاملة
     تحقَّقت عبر 3 عمليات WebFetch منفصلة تدريجية (لم تظهر دفعة واحدة في
     نتيجة بحث واحدة) — متفق عليه عن أبي هريرة.
  2. الرحمة بالحيوان في السنة — حديثا المرأة والهرة + الرجل والكلب
     مقترنين، متفق عليهما (لم يُذكر اسم راوي حديث الهرة تحديداً لعدم
     توفره في نتائج البحث المتاحة).
  3. خيركم من تعلم القرآن وعلمه — عثمان بن عفان، صحيح البخاري، كتاب
     فضائل القرآن، الباب 21 (موضع دقيق مؤكَّد).
  4. أدب الطعام (التسمية والأكل باليمين) — عمر بن أبي سلمة، صحيح البخاري،
     كتاب الأطعمة.
  أُعيد توازن الوزن (5×20=100)، `learning_paths.total_sessions` لـhadith:
  106→110 (تحقَّق مباشرة من DB الحية قبل وبعد). كل البوابات الأربع نجحت
  معًا. commit `3f7e87d3`، مدفوع فعلياً. **19 مقرراً ضحلاً آخر لا يزال
  مرشحاً** — من أبرزها علمياً: صحيح مسلم (2)، رياض الصالحين (1)، العقيدة
  الواسطية (1)، شرح الطحاوية (1)، زاد المستقنع (1)، بلوغ المرام (1).

- **جلسة تاسعة (تكملة رابعة، 2026-07-18)**: تابع أولوية "أهمها علميًا"
  (بطلب المنسّق) إلى "صحيح مسلم" (hadith path، وحدة `82c0d01d...`، كان
  عنصرين: قراءة عامة 70% + حديث 989 منفرد 30%). أُضيفت 4 أحاديث حقيقية
  أخرى، قُورنت أولاً بكل الـ42 حديثاً في `arbaeen-nawawi-seed.ts` وبالأربعة
  عناصر المُضافة حديثاً لصحيح البخاري (نفس مسار الحديث) لتفادي التكرار:
  1. أول من تُسعَّر بهم النار (القارئ/المجاهد/المتصدق رياءً) — صحيح مسلم،
     كتاب الإمارة، عن أبي هريرة — يكمّل موضوعياً حديث "خيركم من تعلم
     القرآن" المُضاف للبخاري (فضل التعلّم مقابل خطر الرياء فيه).
  2. أفشوا السلام بينكم — صحيح مسلم، كتاب الإيمان، عن أبي هريرة.
  3. فضل عيادة المريض («خُرفة الجنة») — صحيح مسلم، عن ثوبان رضي الله عنه.
  4. الدنيا سجن المؤمن وجنة الكافر — صحيح مسلم، عن أبي هريرة.
  كل استشهاد تحقَّق عبر WebFetch. أُعيد توازن الوزن على 6 عناصر
  (17+16+17+17+17+16=100، تحقَّق SUM(weight) مباشرة من DB الحية = 100.00
  تماماً). `learning_paths.total_sessions` لـhadith: 110→114 (تحقَّق
  مباشرة قبل وبعد). كل البوابات الأربع نجحت معًا. commit `52620aaf`،
  مدفوع فعلياً.
  **بلا توقف**، تابع نفس النمط لمقرر "رياض الصالحين" (وحدة
  `d700b47a...`، كان عنصراً عاماً واحداً). أُضيفت 4 أبواب مشهورة من
  الكتاب، قُورنت أولاً بالثمانية عناصر المُضافة حديثاً للبخاري ومسلم
  وبالأربعين النووية لتفادي التكرار:
  1. باب الصبر: «عجباً لأمر المؤمن» — صحيح مسلم، كتاب الزهد، عن صهيب.
  2. باب التوبة: فرح الله بتوبة عبده — متفق عليه، عن أنس بن مالك.
  3. باب بر الوالدين: «رغم أنف من أدرك أبويه فلم يدخل الجنة» — صحيح
     مسلم، عن أبي هريرة.
  4. باب حسن الخلق: «أكمل المؤمنين إيماناً أحسنهم خلقاً» — رواه الترمذي
     وأبو داود وصحَّحه الحاكم — **ليس في الصحيحين**، ذُكر مصدره الحقيقي
     صراحة بدل نسبته خطأً للبخاري/مسلم (انضباط دقة النسبة، لا مجرد صحة
     المتن).
  أُعيد توازن الوزن (5×20=100)، `learning_paths.total_sessions` لـhadith:
  114→118 (تحقَّق مباشرة). كل البوابات نجحت معًا. commit `8336a700`،
  مدفوع فعلياً.
  **بلا توقف**، تابع نفس النمط لمقرري "العقيدة الواسطية" (وحدة
  `7527bfb6...`) و"العقيدة الطحاوية" (وحدة `b8fcae81...`)، كلاهما كان
  عنصراً عاماً واحداً. كل نص من الرسالتين تحقَّق حرفياً عبر WebFetch من
  المتن نفسه على ويكي مصدر (لا من الذاكرة)، والآيات القرآنية المستشهَد
  بها في الواسطية تحقَّقت مباشرة من الملفات المحلية:
  **الواسطية** (ابن تيمية، commit `f9c07987`، aqeedah total_sessions
  62→66):
  1. منهج أهل السنة في أسماء الله وصفاته — «من غير تحريف ولا تعطيل، ومن
     غير تكييف ولا تمثيل» + الشورى: 11.
  2. الإيمان بالقدر خيره وشره — «العباد فاعلون حقيقة والله خالق أفعالهم»
     + التكوير: 28-29.
  3. ترتيب الخلفاء الراشدين — «خير هذه الأمة بعد نبيها أبو بكر ثم عمر،
     ويثلثون بعثمان ويربعون بعلي».
  4. اليوم الآخر: فتنة القبر والميزان — المؤمنون: 102.
  **الطحاوية** (الإمام الطحاوي، commit `74adbc42`، aqeedah
  total_sessions 66→70) — مواضيع مختلفة تماماً عن الواسطية عمداً لتجنّب
  التكرار داخل نفس المسار:
  1. القرآن كلام الله غير مخلوق — «ليس بمخلوق ككلام البرية» (ردًّا على
     محنة خلق القرآن المعتزلية).
  2. عدم تكفير أهل القبلة بالذنوب — «ولا نكفر أحداً من أهل القبلة بذنب
     ما لم يستحله».
  3. الإسراء والمعراج حق — «وعرج بشخصه في اليقظة إلى السماء».
  4. رؤية أهل الجنة لله تعالى — «بغير إحاطة ولا كيفية».
  كل البوابات نجحت معًا في كلا الدفعتين، ومدفوعتان فعلياً.
  **محاولة "زاد المستقنع" تعذَّرت**: متنه غير متوفر على ويكي مصدر
  العربية (بحث مباشر لعدة عبارات مرجَّحة أعاد صفر نتائج)، وshamela.ws
  رفض الوصول (403 Forbidden). بدل اختلاق محتوى فقهي حنبلي دقيق (نُصُب
  زكاة، شروط عقود) بلا مصدر يمكن التحقق منه مباشرة، أُجِّل هذا المقرر
  ريثما يتوفر مصدر موثوق — قرار متعمَّد يتوافق مع انضباط الجلسة الكامل
  في رفض تقديم معلومة غير مؤكَّدة كحقيقة.
  **تابع بدلاً منه إلى "بلوغ المرام من أدلة الأحكام"** (fiqh path، وحدة
  `13c3ca00...`، كان عنصراً عاماً واحداً) — مختلف عن الكتب السابقة لأنه
  تحديداً أحاديث أحكام فقهية (لا عقيدة/أخلاق عامة)، فاختيرت 4 أحاديث
  أحكام مباشرة، تحقَّق كل منها عبر WebFetch:
  1. المسح على الخفين — المغيرة بن شعبة، غزوة تبوك، رواه أبو داود
     والنسائي.
  2. تحريم كل مسكر — «كل مسكر خمر، وكل خمر حرام» — متفق عليه، ابن عمر.
  3. الشفعة في كل ما لم يقسم — صحيح البخاري، جابر بن عبدالله.
  4. النهي عن بيع الغرر وبيع الحصاة — صحيح مسلم، أبو هريرة.
  أُعيد توازن الوزن (5×20=100). `learning_paths.total_sessions` لـfiqh:
  110→114 (تحقَّق مباشرة قبل وبعد). كل البوابات نجحت معًا. commit
  `d50e988f`، مدفوع فعلياً. **14 مقرراً ضحلاً آخر لا يزال مرشحاً** (زاد
  المستقنع من بينها، مؤجَّل لا مُستبعَد).
  **بلا توقف، بطلب المنسّق**، تابع عبر مقررين إضافيين من نفس القائمة:
  **"مصطلح الحديث" العام** (mustalah-hadith path، وحدة `01553d1e...`،
  كان عنصراً واحداً مرتبطاً بالبيقونية) — مختلف عن مقرري "أقسام الحديث"
  و"نخبة الفكر" الموجودين مسبقاً في نفس المسار (لا تكرار)، فأُضيفت 4
  مصطلحات مختلفة: المرفوع/الموقوف/المقطوع (تصنيف بحسب القائل)،
  المسند/المتصل/المنقطع/المرسل (تصنيف بحسب اتصال السند)، زيادة الثقة،
  والحديث الموضوع وعلاماته — محتوى تعريفي/تصنيفي مستقر من "مقدمة ابن
  الصلاح في علوم الحديث" (نفس المرجع المنسوب إليه العنصر المجاور "الحديث
  الصحيح وشروطه الخمسة")، لا يحتاج تخريجاً فردياً. `total_sessions`
  لـmustalah-hadith: 18→22. commit `6bda7e0e`.
  **"نواقض الإسلام ومدخل العقيدة"** (aqeedah path، وحدة `d6e667c3...`،
  كان محاضرة عامة وزن 60 + اختبار وزن 0) — القائمة الكاملة لنواقض
  الإسلام العشرة لمحمد بن عبد الوهاب تحقَّقت حرفياً عبر WebFetch من نص
  الرسالة نفسها (لا من الذاكرة)، ثم جُمِّعت في 4 عناصر موضوعية: (1) الشرك
  في العبادة/الوسائط/السحر — نواقض 1،2،7؛ (2) عدم تكفير المشركين وتفضيل
  غير حكم النبي ﷺ — 3،4؛ (3) بغض دين الرسول ﷺ أو الاستهزاء به — 5،6؛
  (4) معاونة المشركين والخروج عن الشريعة والإعراض عن الدين — 8،9،10.
  رُبطت بـ"ثلاثة الأصول" (لا مدخل مستقل لرسالة النواقض في الفهرس، نفس
  مرجع العنصر العام الموجود). أُعيد توازن الوزن (40+15×4+0=100، تحقَّق
  SUM=100.00 مباشرة). `total_sessions` لـaqeedah: 70→74. commit
  `139b5228`. كل البوابات نجحت معًا في كلا الدفعتين، مدفوعتان فعلياً.
  **بلا توقف (بطلب المنسّق: "حتى تستنفد ما هو قابل للتحقق منها")**، تابع
  إلى "السيرة النبوية" العام (seerah path، وحدة `2594ba33...`، مرتبط
  بالرحيق المختوم، كان عنصراً واحداً) — 4 أحداث حقيقية من الفترة المكية
  المبكرة (قبل الهجرة)، مختارة عمداً لتفادي التداخل مع "الغزوات الكبرى"
  (فترة مدنية، مقرر آخر مبني سابقاً هذه الجلسة):
  1. بدء الوحي وحادثة غار حراء — العلق 1-5 (تحقَّقت حرفياً من
     surah-096.json) + تطمين خديجة رضي الله عنها، صحيح البخاري.
  2. الهجرة الأولى إلى الحبشة — خطاب جعفر بن أبي طالب أمام النجاشي.
  3. عام الحزن ورحلة الطائف — دعاء النبي ﷺ المشهور، سيرة ابن هشام.
  4. بيعة العقبة الثانية — 73 رجلاً وامرأتان، البداية والنهاية لابن كثير.
  كل نص تحقَّق عبر WebFetch. أُعيد توازن الوزن (5×20=100).
  `learning_paths.total_sessions` لـseerah: 34→38 (تحقَّق مباشرة). كل
  البوابات نجحت معًا. commit `239edf6e`، مدفوع فعلياً. **11 مقرراً ضحلاً
  آخر لا يزال مرشحاً** (زاد المستقنع من بينها، مؤجَّل).
  **بلا توقف (بطلب المنسّق)**، تابع عبر مقررين إضافيين:
  **"تفسير سورة النحل"** (tafseer path، وحدة `53ca670b...`، كان عنصرين:
  عام + "من الآية 40 فما بعدها") — أُضيفت 4 عناصر تغطي موضوعات من
  الآيات 1-39 (النطاق غير المُغطَّى صراحة)، كل آية تحقَّقت حرفياً من
  surah-016.json: (1) آيات نعم الله (5-18)، (2) حجة التوحيد وإبطال الشرك
  (17، 20-22)، (3) حال المتقين والظالمين عند الوفاة (28-32)، (4) إرسال
  الرسل لتحقيق التوحيد (36-37). `total_sessions` لـtafseer: 43→47.
  commit `65bca303`.
  **"كتاب التوحيد كاملًا"** (مسار "التوحيد" tawheed — **مسار منفصل
  تماماً عن مسار العقيدة aqeedah** الذي عُمِّق فيه مقرر آخر بنفس الكتاب
  سابقاً هذه الجلسة؛ نفس الكتاب، مساران مختلفان) — اختيرت عمداً 4 أبواب
  مختلفة تماماً عن الأربعة المُستخدَمة في مقرر aqeedah لتفادي التكرار حتى
  عبر مسارين: (1) إتيان الكهان والعرافين، (2) التبرك بشجر أو حجر (قصة
  ذات أنواط)، (3) «وما قدروا الله حق قدره» (الزمر: 67، تحقَّقت حرفياً من
  surah-039.json)، (4) النذر عبادة لا تصرف لغير الله (حديث عائشة، صحيح
  البخاري). `total_sessions` لـtawheed: 19→23. commit `3a4c432d`. كل
  البوابات نجحت معًا في كلا الدفعتين، مدفوعتان فعلياً.
  **بلا توقف (بطلب المنسّق)**، تابع إلى "منهج الحوار والتدرج" (dawah
  path، وحدة `d6ca471f...`، كان عنصراً واحداً عن الحكمة والموعظة الحسنة،
  النحل: 125) — أُضيفت 4 أمثلة حقيقية لمبدأ "التدرج" تحديداً (الجانب
  غير المُغطَّى):
  1. التدرج في الدعوة — حديث بعث معاذ إلى اليمن (شهادة، ثم صلاة، ثم
     زكاة) — متفق عليه.
  2. التدرج في التشريع: تحريم الخمر — ثلاث مراحل قرآنية (البقرة: 219،
     النساء: 43، المائدة: 90-91)، كل آية تحقَّقت حرفياً محلياً.
  3. الرفق في الدعوة — «إن الرفق لا يكون في شيء إلا زانه» — صحيح مسلم،
     عائشة.
  4. مراتب تغيير المنكر — «فليغيره بيده، فإن لم يستطع فبلسانه...» —
     صحيح مسلم، أبو سعيد الخدري.
  `learning_paths.total_sessions` لـdawah: 5→9 (تحقَّق مباشرة). كل
  البوابات نجحت معًا. commit `c0ad0121`، مدفوع فعلياً.
  **بلا توقف (بطلب المنسّق)**، تابع إلى مقررين مكتشَفين حديثاً في مسار
  "تربية" (tarbiyah):
  **"منزلتا الرضا والشكر"** (وحدة `869a7689...`، كان عنصرين عامّين وزن
  50/50) — أُضيف استشهاد قرآني وحديثي محدد لكل منزلة: آية الرضا المتبادل
  (البينة: 8، تحقَّقت حرفياً من surah-098.json)، حديث «ذاق طعم الإيمان
  من رضي بالله رباً» (صحيح مسلم، العباس بن عبد المطلب)، آية الشكر ووعد
  الزيادة (إبراهيم: 7، تحقَّقت حرفياً من surah-014.json)، حديث «من لا
  يشكر الناس لا يشكر الله» (الترمذي، حسن صحيح، أبو هريرة).
  **خطأ ذاتي اكتُشف وصُحِّح فوراً**: أول محاولة لإعادة توازن الوزن
  افترضت 4 عناصر إجمالاً (25% لكل عنصر) بينما الواقع 6 عناصر (2 موجودان
  + 4 جديدة) — تحقّق SUM(weight) المباشر من DB الحية بعد التطبيق كشف
  المجموع 150 بدل 100، فصُحِّح فوراً إلى 17×4+16×2=100 (تحقَّق SUM=100.00
  لاحقاً)، وصُحِّح ملف SQL نفسه ليوثِّق الواقع الصحيح لا الخطأ الأولي —
  مثال إضافي على قيمة التحقق المباشر من DB بعد كل تطبيق بدل افتراض نجاح
  الحسبة.
  **"آفات النفس"** (وحدة `a9d37169...`، كان عنصرين: الغضب والشح) —
  أُضيفت آفتان جديدتان: الحسد («يأكل الحسنات كما تأكل النار الحطب»،
  أبو داود) والكِبر («لا يدخل الجنة من كان في قلبه مثقال ذرة من كِبر»،
  صحيح مسلم، ابن مسعود).
  `learning_paths.total_sessions` لـtarbiyah: 6→12. commit `90b333fd`.
  كل البوابات نجحت معًا. مدفوع فعلياً.
  **بلا توقف (بطلب المنسّق)**، تابع إلى "آداب المجتمع" (adab path، وحدة
  `dca26054...`) و"الفضائل المنجية" (tarbiyah، وحدة `0185b970...`)،
  كلاهما كان عنصرين عامّين وزن 50/50:
  **آداب المجتمع**: حق الجار («ما زال جبريل يوصيني بالجار حتى ظننت أنه
  سيورثه»، متفق عليه، ابن عمر)، وآداب المجلس («إذا انتهى أحدكم إلى
  المجلس فليسلم»، أبو داود، أبو هريرة). adab total_sessions: 7→9.
  **الفضائل المنجية**: قبل الاختيار، تحقَّق أن حديث "ازهد في الدنيا
  يحبك الله" هو **بالضبط** حديث الأربعين النووية رقم 31 (المُستخدَم
  سابقاً هذه الجلسة) فاستُبعد فوراً لتفادي التكرار، واختير بدلاً منه:
  حسن الظن بالله (حديث قدسي «أنا عند ظن عبدي بي»، متفق عليه، أبو هريرة)،
  والتوكل على الله («لو أنكم تتوكلون على الله حق توكله لرزقكم كما يرزق
  الطير»، الترمذي، عمر بن الخطاب). tarbiyah total_sessions: 12→14.
  **تعلُّم من خطأ الدفعة السابقة**: هذه المرة أُعيد التحقق من عدد
  العناصر الإجمالي (4، لا 6) قبل توزيع الوزن (4×25=100) وتحقَّق
  SUM(weight)=100.00 لكلا المقررين مباشرة من DB الحية فور التطبيق —
  صحيحان من أول محاولة. commit `bbba9372`، مدفوع فعلياً. كل البوابات
  نجحت معًا. **4 مقررات ضحلة أخرى لا تزال مرشحة** (زاد المستقنع مؤجَّل).
  **بلا توقف (بطلب المنسّق: "نحو/عربية وفقه matn إن توفر مصدر")**، فُحص
  مسار نحو (nahw):
  **"متن الآجرومية"** (وحدة `33330646...`، كان عنصرين: الكلام وأقسامه،
  الإعراب — تحقَّق أنهما لا يغطيان علامات الاسم/الفعل) — متن الآجرومية
  متوفر كاملاً على ويكي مصدر (بخلاف زاد المستقنع)، فأُضيف: علامات الاسم
  («الخفض والتنوين ودخول الألف واللام وحروف الخفض») وعلامات الفعل («قد
  والسين وسوف وتاء التأنيث الساكنة») — 2→4 عناصر، تحقَّق SUM=100.00.
  nahw total_sessions: 5→7. commit `8009bab8`.
  **"ألفية ابن مالك"** (وحدة `eae5b2db...`، كان عنصرين وصفيين عامّين لا
  نصوصاً حقيقية من المنظومة) — أُضيف البيت الافتتاحي الفعلي «كَلَامُنَا
  لَفْظٌ مُفِيدٌ كَاسْتَقِمْ، وَاسْمٌ وَفِعْلٌ ثُمَّ حَرْفٌ الكَلِمْ»
  (تحقَّق حرفياً عبر WebFetch من نسختين مستقلتين). **اقتُصر على عنصر
  واحد فقط لا أربعة**: محاولة تأكيد بيت ثانٍ (باب المبتدأ والخبر) أعادت
  صفر نتائج فلم يُختلَق نص غير مؤكَّد — 2→3 عناصر (33+33+34=100).
  nahw total_sessions: 7→8. commit `30a3c8fe`.
  **"المدخل إلى الصرف — مراح الأرواح" فُحص وتعذَّر**: بحث WebFetch عن
  متنه على ويكي مصدر أعاد صفر نتائج، بنفس نمط زاد المستقنع تماماً —
  أُجِّل أيضاً بدل اختلاق محتوى صرفي بلا مصدر يمكن التحقق منه.
  كل البوابات نجحت معًا في كلا دفعتي النحو، مدفوعتان فعلياً.

  **بلا توقف (بطلب المنسّق: "أكمل المقررات السطحية القليلة المتبقية إن
  وُجد مصدر موثوق لها")**، عُمِّق مقرران في مسار الدعوة (dawah) بنفس
  انضباط "عنصر واحد مؤكَّد لا أربعة مُختلَقة" المُتَّبع في ألفية ابن مالك:
  **"دعوة القبائل والهجرة"** (وحدة `d51f414c...`، عنصران وصفيان عامّان)
  — أُضيف عنصر واحد: بيعة العقبة الأولى (12 رجلاً من الأنصار، بيعة
  النساء: «بايعناه على أن لا نشرك بالله شيئاً، ولا نسرق، ولا نزني»،
  متفق عليه). محاولة تأكيد عنصر ثانٍ (بعث مصعب بن عمير) أعادت صفر نتائج
  فلم يُختلَق. 2→3 عناصر (33+33+34=100). dawah total_sessions: 9→10.
  commit `dfa7df67`.
  **"مراحل الدعوة النبوية الأولى"** (وحدة `10a3e9d4...`، عنصران وصفيان
  عامّان) — أُضيف عنصر واحد: نداء الصفا «يا صباحاه» وتحدي النبي ﷺ لقريش
  «أرأيتكم لو أخبرتكم أن خيلاً بالوادي تريد أن تغير عليكم أكنتم
  مصدِّقيّ؟» — متفق عليه، تنفيذاً لآية الشعراء (214) «وأنذر عشيرتك
  الأقربين» (تحقَّقت حرفياً من surah-026.json). تحقَّق أن النص المحدد لا
  يكرر وصف العنصر الموجود العام. 2→3 عناصر (33+33+34=100). dawah
  total_sessions: 10→11. commit `5d3eb8cc`.
  كل البوابات نجحت معًا في كلا الدفعتين، مدفوعتان فعلياً.

  **بهذا اكتملت جولة "استنفاد ما هو قابل للتحقق" من المقررات السطحية
  المكتشَفة هذه الجلسة** — المتبقي (الفقه الحنفي، الفقه الشافعي، تلخيص
  مختصر المقنع، منهاج المسلم، فقه المرأة المسلمة، فقه العبادات، البلاغة،
  مسح قواعد العربية، المعجم العربي، دعوى تعارض السنة مع العلم التجريبي،
  السيرة النبوية المعمقة، تفسير القرآن الكريم) لم يُفحص فردياً بعد
  للتأكد من توفر مصدر — مرشح لدفعة قادمة إن أُريدت المتابعة، بنفس نمط
  الفحص أولاً ثم البناء فقط عند توفر مصدر موثوق.

  **تصحيح استنتاج iOS (بطلب المنسّق)**: تبيَّن أن اكتشافي السابق (عطل
  iOS WebSocket عالق على فرع معزول) استنتاج خاطئ — المنسّق فحص
  `https://www.majlisilm.com` الحي مباشرة وتأكَّد أن `wss://*.supabase.co`
  موجود بالفعل في CSP الإنتاج المنشور فعلياً (الفرع الأصلي الذي حمل هذا
  الإصلاح دُمج ونُشر في جلسة سابقة، وليس هذا الفرع). **درس موثَّق**: فحص
  فرع Git لا يُغني إطلاقاً عن فحص الإنتاج المنشور فعلياً مباشرة. صُحِّح
  في CONTINUATION_PLAN.md وREADY_FOR_MERGE.md (commit `881d9e81`).

  **🔧 بق جديد مُكتشَف وأُصلح عبر الفحص المباشر (بطلب المنسّق: "أي
  فجوة/بق جديد تكتشفه")**: تتبُّع بقايا "15 صفاً يتيماً" الموثَّقة سابقاً
  في quiz_questions (section بالإنجليزية الصغيرة: ahkam×3، anbiya×5،
  sahaba×3، seera×4) كشف أنها **غير مُعرَّفة إطلاقاً** في خريجة
  `SECTION_TO_CATEGORY` الفعلية (islamicQuizData.ts) — تختفي صامتاً من
  لعبة "الأسئلة الجاهزة" الحية رغم كونها أسئلة صحيحة تماماً (تحقَّقت
  يدوياً من كل الـ15: أركان الإسلام، نصاب زكاة الذهب، أول من أسلم،
  ميلاد النبي ﷺ...). أُصلح بتحديث عمود section للقيم العربية المكافئة
  (الأحكام/الأنبياء/الصحابة/السيرة) — تحقَّق أولاً أنها مسموحة فعلاً في
  CHECK constraint الحي قبل التطبيق. تحقَّق مباشرة بعد التطبيق: صفر صفوف
  متبقية بالقيم الإنجليزية القديمة، `quiz_questions` ثابت عند 958 (لا
  فقدان بيانات). كل البوابات نجحت معًا. commit `d3c326b4`، مدفوع فعلياً.

  **اكتشاف أثناء البحث عن عمل مفيد آخر (بطلب المنسّق: "فحص وصول iOS") —
  تصحيح لاحق مهم**: استعلام مباشر على `client_error_logs` الحية كشف 44
  خطأ iOS متعلق بـWebSocket/transport failure خلال آخر 30 يوماً. ظننتُ
  خطأً أن السبب هو أن إصلاح CSP (`commit 7a7d59e4`، إضافة
  `wss://*.supabase.co`) عالق على هذا الفرع المعزول غير مدموج للإنتاج —
  **استنتاج غير صحيح**، صحَّحه المنسّق مباشرة: فحص `https://www.majlisilm.com`
  الحي فعلياً وتأكَّد أن رأس CSP يحتوي `wss://*.supabase.co` **بالفعل في
  الإنتاج المنشور** (الفرع الأصلي الذي حمل هذا الإصلاح دُمج ونُشر في جلسة
  سابقة، وليس هذا الفرع تحديداً). خطئي كان فحص فرع Git معزول بدل التحقق
  من الإنتاج الفعلي المنشور مباشرة — **درس مهم موثَّق: التحقق من "الكود
  موجود على فرع ما" لا يُغني إطلاقاً عن التحقق من "ماذا يعرض الإنتاج
  المنشور فعلياً الآن"، الأخير وحده الحاسم**. أخطاء iOS المتبقية في
  السجلات على الأرجح من نسخة تطبيق iOS الأصلي (Capacitor) قديمة أو سبب
  مختلف تماماً — نُقل هذا للمالك مباشرة، وقرر الاستمرار بعمل المحتوى
  دون تحقيق إضافي بخصوص هذا العطل الآن.

  **🔧 بقان إضافيان مُكتشَفان وأُصلحا في المجمع الفقهي (فحص مباشر، بطلب
  المنسّق)**: تصنيف "الأطعمة والأشربة" كان بلا محتوى إطلاقاً (0 من 58
  صفاً منشوراً) رغم كونه مدرجاً وقابلاً للاختيار في 6 صفحات حية. أعمق
  من ذلك: تصنيف "النوازل المعاصرة" (مدرج أيضاً في `FIQH_COUNCIL_
  CATEGORIES` ومستخدَم في `FIQH_TOPIC_INDEX_SECTIONS`) كان **مستحيلاً
  بنيوياً** أن يحتوي أي محتوى — CHECK constraint الحي لعمود category لم
  يكن يسمح به إطلاقاً (10 قيم فقط بدل 11 في TypeScript). أُصلح بتوسيع
  القيد ليطابق TS بالضبط، وأُضيفت 4 قرارات حقيقية موثَّقة لمجمع الفقه
  الإسلامي الدولي (دورته 26، الدوحة، 4-8 مايو 2025)، تحقَّق كل قرار مباشرة
  عبر WebFetch من iifa-aifi.org الرسمي: اللحوم المستزرعة معملياً وGMO
  حيواني (إباحة بشروط)، وتناول الحشرات وصعق الحيوانات قبل الذبح (**تأجيل
  البتّ لمزيد من الدراسة** فعلياً — لم يُفرض حكم غير صادر، استُخدم
  context_disclaimer). `fiqh_council_issues` المنشور: 58→62. commit
  `68344bc1`، مدفوع فعلياً. كل البوابات نجحت معًا.

  **تصحيح توثيقي إضافي**: بند 6 أعلاه (تعبئة `knowledge_relationships`)
  كان يذكر أن أزواج fawaid↔book وlesson↔book "لم تُملأ بعد" — تحقُّق
  مباشر من DB الحية كشف أنهما **ممتلئان فعلاً** (49 و6 على الترتيب) منذ
  دفعة سابقة لم تُحدَّث في هذا التوثيق. صُحِّح البند مباشرة.

  **إكمال تعبئة "النوازل المعاصرة" (بطلب المنسّق: "قابلة للملء الآن بعد
  إصلاح القيد؟")**: بعد توسيع CHECK constraint، أُضيفت مسألتان ماليتان/
  تقنيتان ناشئتان حقيقيتان — اختيرتا عمداً لتفادي التداخل الموضوعي مع
  "القضايا المعاصرة" الموجودة (دعوة/إعلام) و"الاقتصاد الإسلامي" (اقتصاد
  كلي عام):
  1. القرار 237 (24/8) — العملات الرقمية المشفَّرة (كالبيتكوين): الدورة
     24، دبي (20 نوفمبر 2019). **أُجِّل البتّ** لعدم استقرار المعاملات
     وعدم حسم طبيعتها (سلعة/خدمة/أصل استثماري) وقيمتها النقدية شرعاً.
  2. القرار 230 (24/1) — العقود الذكية: نفس الدورة. **أُجِّل** أيضاً،
     مرتبطاً بحسم مسألة العملات المشفَّرة أولاً.
  كلاهما تحقَّق مباشرة عبر WebFetch من iifa-aifi.org الرسمي، وعُرضا
  بأمانة كقرارين مؤجَّلين (لا حكم قاطع مختلَق) — تطبيق كامل لقيد "لا
  ترجيح شخصي، اعرض الواقع كما هو" حتى في قرار تأجيل، لا حكماً حاسماً.
  `fiqh_council_issues` المنشور: 62→64. **الآن كل الـ11 تصنيفاً في
  FIQH_COUNCIL_CATEGORIES لها محتوى فعلي حي، لا استثناء**. commit
  `a06ba3d1`، مدفوع فعلياً. كل البوابات نجحت معًا.

  **🔧 فحص منهجي إضافي (بطلب المنسّق: "أي فجوة/بق جديد") كشف نفس البق
  بالضبط على جدول شقيق**: بحثت في كل قيود CHECK الحية التي اسمها يحوي
  "categor" — فوجدت `fiqh_council_items_category_check` (جدول منفصل
  تماماً عن fiqh_council_issues، لكنه **الجدول الأساسي** الذي تعتمد عليه
  الصفحة الرئيسية `/fiqh-council` وست صفحات حية أخرى عبر
  fiqh-council-service.ts) يحمل **نفس البق البنيوي بالضبط**: 10 قيم بدل
  11 (بلا "النوازل المعاصرة")، و"الأطعمة والأشربة" رقيقة جداً (صف واحد
  فقط من 30). أُصلح بنفس الطريقة: توسيع القيد، وإعادة استخدام نفس
  القرارات الأربعة المُتحقَّق منها مسبقاً (لا تحقق مكرر لازم، نفس
  النص المصدري، مُعاد صياغته لمخطط الجدول المختلف: ruling_text/evidence/
  session_number بدل ruling_summary/evidence_summary).
  `fiqh_council_items`: 30→34. commit `b3e66c12`، مدفوع فعلياً.
  فحصتُ أيضاً `islamic_stories_category_check` (3 قيم: صحابة/فتوحات/
  تاريخ) مقابل TS type المحلي — **مطابقان تماماً، سليم**. و`kn_tags_
  category_check` — لم أجد ثابت TS مقترن مكشوف لواجهة حية (على الأرجح
  وسم إداري داخلي لا فلتر مستخدم)، فلم يُتابَع. بهذا اكتمل الفحص المنهجي
  لهذا النمط من البق عبر القاعدة الحية.

  **متابعة الفجوة المرصودة أعلاه — تحقَّقت فعلياً عبر تتبُّع الكود، لا
  حاجة لمتصفح**: بعد الرصد الأولي، أعدت تتبُّع `onClick={() =>
  setCategorySlug(cat.slug)}` (زر التصنيف يضبط الحالة على السلاغ القياسي
  مثل "family") مقابل منطق `filteredItems` (يقارن مباشرة مع السلاغ الخام
  للسؤال مثل "nikah" بلا استدعاء resolveCategorySlug) — منطق مقارنة
  سلاسل نصية حتمي بسيط، لا حالة async مخفية تستلزم متصفحاً فعلياً
  لتفكيكها. **النتيجة حاسمة لا احتمالية**: زر "الأسرة" يعرض عدّاداً يشمل
  أسئلة nikah/talaq/tarbiyah (لأن countByCategorySlug يستدعي
  resolveCategorySlug فعلاً)، لكن النقر عليه فعلياً يستبعدها جميعاً —
  "العدّاد يقول كذا، النقر يُظهر أقل" مؤكَّد لا مُفتَرَض. أُصلح بإضافة
  فحص `resolveCategorySlug(slug) === categorySlug` ضمن شروط الفلترة، بنفس
  منطق التعيين المُستخدَم فعلاً في مكان آخر من نفس الملف — لا افتراض
  جديد، توحيد منطق موجود. `artifacts/majalis/src/views/QaPage.tsx`:
  استيراد resolveCategorySlug + توسيع شرط الفلترة (2 سطر تعديل + تعليق
  توضيحي). لا تغيير بيانات، إصلاح كود عرض فقط. commit `0c9f2c76`، مدفوع
  فعلياً. typecheck/lint/test:regression (16 حزمة)/build نجحت معاً.

  **المقالة الأصلية الرابعة**: "منازل القلب الثلاث: الصبر والشكر والرضا"
  — تربط ثلاثة مقررات بُنيت هذه الجلسة (منزلتا الرضا والشكر، الفضائل
  المنجية، آفات النفس — مسار التربية) بمقرر "باب الصبر" في رياض الصالحين
  (مُعمَّق سابقاً هذه الجلسة أيضاً) — تجميع تركيبي لمحتوى مُتحقَّق منه
  بالفعل هذه الجلسة (إبراهيم: 7، البينة: 8، أحاديث الرضا/الشكر/الحسد/الكِبر
  المُتحقَّقة سابقاً)، لا استشهادات جديدة. **أُفصِح صراحة (في المتن وفي
  FAQ) أن حديث الحسد (أبو داود) بلا تصحيح قاطع مؤكَّد**، بدل الادّعاء بصحته.
  **تصحيح ذاتي فوري**: تكرَّر نفس خطأ عمود tags (ARRAY بدل jsonb) من
  المقالة الثالثة سابقاً — اكتُشف عبر information_schema.columns وصُحِّح
  **قبل** التطبيق لا بعده هذه المرة. `articles`: 3→4، `is_approved: false`.
  commit `2c3eb274`، مدفوع فعلياً. كل البوابات نجحت معًا.
`npx supabase link --project-ref ngmvmlulzacrlicuagyp` أول كل جلسة جديدة
→ ابنِ المحتوى مع تحقّق WebSearch/Agent فرعي لكل استشهاد جديد (آيات من
الملفات المحلية حرفياً، أحاديث بتخريج مؤكَّد، واستبعاد أي حديث ذي تصحيح
متذبذب بين المصادر) → `pnpm run typecheck && pnpm run lint && pnpm run
test:regression && pnpm run build` معاً قبل أي commit → إن غيّرت عدد
كتب/علماء/فوائد/أسئلة شغّل `pnpm run generate:counts` **قبل**
test:regression (نُسي مرة، سبّب فشل `test:counts`) → بعد البناء
`git checkout --` لملفات الضجيج المُولَّدة (`public/data/rulings-
encyclopedia/*`, `feed.xml`, `sitemap.xml`,
`rulings-encyclopedia-seed.generated.ts`؛ أبقِ `content-counts.json`
و`data-quality-audit.json` فقط إن تغيّر رقم حقيقي فعلاً) → commit + push
→ حدِّث هذا الملف و`READY_FOR_MERGE.md` بنفس الأسلوب (قسم جلسة جديد،
تحديث "أكبر فجوة متبقية" و"المهمة التالية").
