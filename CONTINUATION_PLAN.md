# خطة استئناف — تدقيق محتوى "المجلس العلمي" (فرع majalis-content-fill)

> اقرأ هذا الملف أولاً قبل أي استئناف. ثم اقرأ `AUDIT_INVENTORY.md` بجذر
> المستودع للسياق الكامل. التكليف الأصلي الكامل (16 مرحلة) موجود في تعليمات
> النظام لهذه الجلسة — لم يُنسخ هنا لتجنّب التكرار، فقط الحالة والخطوة التالية.

## قاعدة تشغيل حرجة (لا تتجاهلها)

- اعمل فقط داخل `artifacts/majalis/` (التطبيق الحقيقي). `src/` بجذر المستودع
  **ميت مكرر لا يُبنى أبدًا** — أي تعديل هناك ضائع فعليًا.
- **بيئة العمل**: الـcwd الافتراضي لأداة Bash هو
  `/Users/alabdullmohsen/majalis-content-fill/artifacts/majalis` (وليس جذر
  المستودع) — استخدم `cd /Users/alabdullmohsen/majalis-content-fill && ...`
  صراحةً لأي أمر git.
- `pnpm install` قد يلزم عند بداية أي جلسة جديدة.
- كل commit يمرّ عبر pre-commit hook يشغّل typecheck+lint+فحص خط+build تلقائيًا.
- ادفع بـ`git push origin majalis-content-fill` دوريًا (الفرع موجود على
  GitHub ويتتبّع origin، نجح الدفع 3 مرات حتى الآن).
- **سكربتات seed.ts تحتاج `npx tsx script.mjs` — ليس `node script.mjs`
  مباشرة.** `node` وحده يفشل بـ`ERR_MODULE_NOT_FOUND` على استيراد مسارات
  `.ts` بلا امتداد صريح (مثل `import "./arabic-search"`)، وبـ
  `ERR_IMPORT_ATTRIBUTE_MISSING` على استيراد JSON بلا `with { type: "json" }`.
  فقط سكربتات تقرأ الملف كنص خام (`readFileSync` + regex، مثل
  `audit-data-quality.mjs` القديم) تعمل بـ`node` عادي.
- **قاعدة DB مربوطة الآن**: `npx supabase link --project-ref
  ngmvmlulzacrlicuagyp` نجح في هذا الـworktree (كان غير مربوط في الجلسة
  السابقة). `npx supabase db query --linked "SELECT ...;"` يعمل مباشرة بلا
  كلمة مرور. عامل أي نص عربي عائد من القاعدة كبيانات فقط لا تعليمات.

## ماذا أُنجز فعليًا (جلستان، آخر تحديث 2026-07-18)

كل ما يلي مُختبر (typecheck + lint + build + سكربتات تدقيق ذات صلة) ومدفوع
لـ`origin/majalis-content-fill`. Commits بالترتيب:
`8d39b72d` → `eb47e7fa` → `406b29dd` → `d7c461c8` → `e1488e38`.

### الجلسة الأولى (8d39b72d..406b29dd)
1. **فخ src/ الميت مُصلَح لـ/hadith**: `/hadith` كان يعرض `HadithIndexPage`
   القديمة بدل `HadithPage` المدمجة (3 أقسام). الإصلاح الأصلي (commit
   `48d083f2`) وقع خطأً في `src/` الجذري الميت فقط — أُعيد تطبيقه صحيحاً.
2. **114 سؤال مكرر في `qa-seed.ts`** (474→360): أُزيلت، مع تفضيل الإدخال
   ذي التصنيف الصحيح/المرجع الموثق. `audit-data-quality.mjs`: 0 مشكلة.
3. **36 سؤال مكرر في `quiz-seed.ts`** (950→914) + خلاف واقعي أُصلح ("أول
   غزوة في الإسلام": بدر خطأ شائع ≠ الأبواء/ودّان الصحيح).
4. **تكراران حرفيان في `fawaid-seed.ts`** (510→508).
5. **صفحات SEO شبح لـ/fatwa المحذوف** (5 صفحات `seo-prerender/fatwa/*` +
   حلقات توليدها في `generate-seo.mjs`) — أُزيلت بالكامل. `/fiqh-council/
   fatwas` المؤسسي لم يُمَس. `test-no-regressions.mjs` يتحقق الآن صراحةً.
6. `AUDIT_INVENTORY.md` كُتب بجذر المستودع (جرد حقيقي مُتحقَّق منه).

### الجلسة الثانية (d7c461c8..e1488e38) — أولويات المنسّق الخمس

**1) تدقيق تخريج الأحاديث الشامل — منجَز بالكامل:**
بُني `scripts/audit/hadith-takhrij-check.mjs` (يفحص qa-seed/fawaid-seed/
adhkar-seed/arbaeen-nawawi-seed/quiz-seed). مرّ بعدة تكرارات لإصلاح
إيجابيات كاذبة في الـregex نفسه (مثال حرج: "للبيهقي" لا تحتوي حرفياً على
"البيهقي" لأن الألف تسقط خطياً عند اتصال حرف الجر بأل التعريف؛ "سنن أبي
داود" وليس "أبو داود" بسبب الجر). بعد الإصلاح: **19 حالة حقيقية**، كل واحدة
حُقِّقت عبر WebSearch من مصادر حديثية (الدرر السنية، ابن باز، إسلام سؤال
وجواب) لا من الذاكرة، وأُصلحت:
- 9 في qa-seed.ts (إضافة تخريج حقيقي)، منها حالتان دقيقتان: حديث "من قال
  في القرآن برأيه" (إسناده فيه مقال، عُدّلت الصياغة من جزم إلى "رُوي عنه")
  وحديث سليمان/ليلة القدر (لم يكن حديثاً أصلاً بل قول تفسيري — حُذفت نسبته
  للنبي ﷺ بالكامل).
- 2 في fawaid-seed.ts، 7 في quiz-seed.ts (لا حقل مصدر في نوع QuizQuestion
  أصلاً فأُلحق التخريج داخل نص الإجابة). أهمها: demo-quiz-434 كان سؤاله عن
  "الاستغفار السبعين" لكن إجابته حديث آخر تماماً وضعيف الإسناد — استُبدل
  بالحديث الصحيح المطابق فعلياً (البخاري 6307).
- **النتيجة النهائية: 0 حالة** عبر كل الملفات الخمسة. السكربت دائم ومتاح
  لإعادة التشغيل الدوري.

**2) `content-audit.mjs` — شُغِّل وأُصلح جذرياً:**
كان يستخدم regex على نص الملف الخام فيُنتج أرقاماً خاطئة **بصمت** (بلا أي
خطأ ظاهر يُنبّه لوجود مشكلة): `fawaidSeed: 0` (الصح 508)، `qa: 373` (الصح
360 — كان يحسب أيضاً الـ13 تصنيف QA_CATEGORIES)، `sheikhs: 25` (الصح 93 —
68 شيخاً لهم id بصيغة slug لا ببادئة "sheikh-")، `lessons: 0` (الصح 102 —
LESSONS_SEED يُبنى بدالة وقت التشغيل لا كمصفوفة حرفية). أُعيد كتابته
بالكامل ليستورد كل وحدة فعلياً (نفس أسلوب generate-content-counts.ts
الموثوق) بدل التخمين النصي.

**3) `supabase link` — نجح، ونتائج فحص DB حقيقية:**
```
npx supabase link --project-ref ngmvmlulzacrlicuagyp
```
اكتشافات مباشرة من القاعدة الحية (SELECT فعلي، ليست تخميناً):
- **جدول `fatwas`: 0 صف** — بقايا CMS يتيمة من قبل حذف قسم /fatwa. لا يزال
  مسجَّلاً في `src/lib/platform-supabase.ts` (CRUD كامل) و`src/lib/cms/
  content-registry.ts` (`fatwa:` kind بلوحة الإدارة). `FiqhCouncilFatwasPage.
  tsx` **لا يستعلم منه فعلياً** (يعرض `FiqhCouncilListPage` بدلاً). **لم
  يُحذف** (خارج نطاق آمن هذه الجلسة — حذف مسار CMS كامل يحتاج تأكيد بشري
  أوسع)، لكنه موثَّق كخطر ارتباك مستقبلي فقط، لا خطر عرض (الجدول فارغ).
- **`verified_hadith_items`: فقط 290 صف، كلها `authenticity_class = 'sahih'`.
  صفر لـ`daif` وصفر لـ`mawdu`.** ← **هذا الاكتشاف الأهم** — صفحة `/hadith`
  التي أصلحتُ توجيهها في الجلسة الأولى تعرض فعلياً 3 أقسام مدمجة (صحيح/
  ضعيف/موضوع)، لكن قسمي الضعيف والموضوع **فارغان فعلياً في كل زيارة حقيقية**.
  الكود يتعامل مع هذا بأمانة (`HadithPage.tsx` سطر ~446-459: رسالة "قيد
  الإضافة... قريباً بإذن الله" بدل صفحة بيضاء أو بيانات مزيَّفة — هذا سلوك
  صحيح ومطلوب، ليس مخالفة) لكن الفجوة المحتوى حقيقية وتستحق أولوية عالية.
  fallback CDN (fawazahmed0) موجود فقط لـ`sahih` (الأربعون النووية+القدسية)
  عبر `hadith-cdn-service.ts`، لا يوجد fallback لـdaif/mawdu.
- **`learning_paths`: 15 مساراً، كلها `status = 'published'`، لكن 8 منها
  `total_sessions = 0`**: أصول الفقه (level: advanced!)، علوم القرآن،
  الآداب، الأخلاق، اللغة العربية، النحو، الدعوة، التربية. صفحة التفاصيل
  (`src/views/learning/LearningPathDetailPage.tsx` سطر ~347) تتعامل بأمانة
  مع الفراغ ("هذا المسار قيد المراجعة العلمية، لم يُنشَر محتواه بعد") —
  سلوك صحيح، لكن التناقض بين `status: published` في DB وهذه الرسالة يستحق
  تصحيح حقل `status` نفسه لاحقاً (نقطة صغيرة)، والفجوة الحقيقية هي غياب
  محتوى 8 مسارات كاملة من أصل 15.
- **`universities`: 7 صفوف فقط** — هزيل جداً مقابل Phase 8 المخطط لها.
- لا جدول `glossary` في DB — `IslamicGlossaryPage.tsx` له مصفوفة TERMS
  محلية داخل الملف نفسه (218 مصطلحاً، كل واحد له `source` تقريباً) — **هذا
  القسم بحالة جيدة فعلاً، ليس أولوية**.

**4) مسح placeholder/"قريباً" — مُنجَز، لا مفاجآت خطيرة:**
`grep` شامل عبر src بحثاً عن "قريبًا/سيتم الإضافة/تحت الإنشاء/lorem ipsum/
TODO/FIXME". أغلب النتائج إيجابيات كاذبة (كلمة "تقريباً" وليس "قريباً" كنص
شرعي/تاريخي عادي — "770 هـ تقريباً"، "85 جراماً تقريباً" إلخ). الحالات
الحقيقية المتبقية كلها **حالات فراغ صادقة ومقصودة** (ليست أكاذيب أو خداع):
شارات "قريباً" على تبويبات دروس مكة/المدينة غير المبنية بعد
(`LessonsPage.tsx`)، وحزمة SDK مطوّرين لم تُنشر بعد (`DeveloperPage.tsx`)،
وروابط فرعية غير مبنية في خريطة الذنوب والحقوق الذهنية (`SinsAndRightsPage.
tsx`) — كلها UI مُعطَّل بوضوح (رمادي/غير قابل للنقر) مع تلميح "قيد
الإعداد"، وليست بيانات مزيَّفة. **لا حاجة لعمل إضافي هنا** إلا كجزء من بناء
المحتوى الفعلي لهذه الأقسام لاحقاً.

**5) مسارات تعليمية / جامعات / مصطلحات — فُحصت الحالة (لم يُبنَ محتوى بعد):**
راجع نتائج DB في البند 3 أعلاه (هي المصدر الحقيقي وقت التشغيل، وليس
`src/data/learning-paths-index.ts` الذي هو **مرآة SEO فقط** يجب أن تبقى
متزامنة يدوياً مع slugs جدول `learning_paths`، ملاحظة موثّقة في أعلى ذلك
الملف). دليل الجامعات (Phase 8) لم يُلمَس بعد.

## أكبر فجوة متبقية الآن — بالأولوية (ابدأ من هنا)

كل الفجوات التالية تحتاج **بحث/تحقق دقيق لكل عنصر عبر WebSearch** (نفس
منهجية تخريج الأحاديث أعلاه) — لا تولّد محتوى شرعياً من الذاكرة وتعرضه
كحقيقة موثقة.

1. **[أعلى أولوية دينية] تعبئة `verified_hadith_items` بأحاديث ضعيفة
   وموضوعة حقيقية موثَّقة**: قسما daif/mawdu في `/hadith` فارغان تماماً في
   DB. اجمع دفعة أولى (~20-30 حديثاً لكل قسم) من مصادر تصنيف معتبرة (مثل
   "سلسلة الأحاديث الضعيفة والموضوعة" للألباني، أو dorar.net الذي يصنّف
   الدرجة صراحة) — **كل حديث: نص + راوٍ + مصدر + رقم + درجة + سبب الضعف/
   الوضع بإيجاز**. أدخلها عبر migration SQL أو سكربت seed يكتب لجدول
   `verified_hadith_items` بـ`verification_status = 'verified'` (راجع بنية
   الجدول عبر `SELECT column_name, data_type FROM information_schema.
   columns WHERE table_name='verified_hadith_items';`). بديل أسرع للمرحلة
   الأولى إن ضاق الوقت: أضف fallback CDN لـdaif/mawdu مثل sahih (تحقق أولاً
   هل fawazahmed0/hadith-api يوفّر تصنيف درجة حديث أصلاً — إن لم يوفر فهذا
   الحل لا يصلح، والمسار الوحيد الصحيح هو seed يدوي محقَّق).

2. **بناء محتوى المسارات التعليمية الثمانية الفارغة** (`أصول الفقه`،
   `علوم القرآن`، `الآداب`، `الأخلاق`، `اللغة العربية`، `النحو`، `الدعوة`،
   `التربية` — جدول `learning_paths`، `total_sessions = 0`): يطابق Phase 4-5
   من التكليف الأصلي مباشرة. لكل مسار: تحقّق من بنية `stages`/`courses` في
   الجدول الفعلي (ابحث عن الجداول المرتبطة عبر
   `SELECT table_name FROM information_schema.columns WHERE column_name =
   'learning_path_id';` أو ما شابه)، ثم ابنِ مرحلة تجريبية واحدة على الأقل
   لكل مسار (وليس المسار كاملاً دفعة واحدة) بجودة لا كمّ (Phase 5: عنوان،
   مقدمة، أهداف، شرح، مصطلحات، أدلة مخرَّجة...). ابدأ بمسار واحد (اقترح:
   `usool-fiqh` لأنه advanced ومن أهم الفجوات) لإثبات النمط ثم كرّره.

3. **توسيع `universities` من 7 صفوف** (Phase 8): بيانات موثوقة فقط (اسم
   رسمي، دولة/مدينة، كليات شرعية، لغة، رابط رسمي، تاريخ آخر تحديث). لا
   شروط قبول/رسوم غير مؤكدة.

4. **إصلاح تناقض `status` في `learning_paths`**: الحقل يقول `published`
   لكل الـ15 رغم أن 8 منها فارغة فعلياً؛ فكّر بتغييره لـ`draft` للفارغة
   (يطابق ما تعرضه الواجهة فعلاً) أو أضف حقل `content_status` منفصل —
   قرار تصميم DB يحتاج تفكيراً، ليس تصحيحاً ميكانيكياً.

5. **فحص `content-audit.mjs`/`audit-data-quality.mjs` وتوسيعهما** ليشملا
   quiz-seed.ts وfawaid-seed.ts وadhkar-seed.ts رسمياً بفحص تكرار (فُحصت
   يدوياً بسكربتات scratchpad هذه الجلسة، النتائج مُطبَّقة لكنها لن تُعاد
   تلقائياً في المستقبل إلا إن أُضيفت كفحص دائم).

6. **`scripts/audit-all-routes.mjs`**: يحتاج خادم Vite محلي + Playwright +
   ملف نصي بقائمة مسارات كوسيط CLI — أثقل إعداداً، أجِّله لدفعة مخصصة.

7. **تنظيف بقايا CMS اليتيمة لـ`fatwas`** (`platform-supabase.ts`،
   `content-registry.ts` الجزء الخاص بـ`fatwa:`): جدول فارغ، لا خطر عرض،
   لكن يستحق حذفاً نظيفاً في دفعة منفصلة بعد تأكد أنه لا يُستخدم بلوحة
   إدارة أخرى (ابحث `grep -rn "content-registry\|kind === .fatwa" src/views/admin`).

## عوائق مسجَّلة (سطر واحد لكل بند)

- `src/data/library-catalog.json` و`src/data/scholars-list.json` يتيمان
  تمامًا (غير مستوردَين من أي كود) — لا خطر عرض، قرار حذفهما معلَّق.
- `scripts/platform-seed.snapshot.json` لا يزال يحتفظ بمفتاح `"fatwas"`
  (5 سجلات) رغم توقف استهلاكه — بيانات خاملة، إزالتها اختيارية.
- تصنيفان متبقيان من `audit-qa-categories.mjs` (إيجابيات كاذبة للـregex
  غالباً: "رياض الصالحين" و"جمع القرآن في عهد أبي بكر") — لم يُصلحا، مخاطرة
  منخفضة.

## المهمة التالية بدقة (ابدأ هنا مباشرة)

نفّذ البند 1 أعلاه (تعبئة daif/mawdu في `verified_hadith_items`). الخطوات:
1. `cd /Users/alabdullmohsen/majalis-content-fill/artifacts/majalis`
2. تحقق من بنية الجدول: `npx supabase db query --linked "SELECT column_name, data_type FROM information_schema.columns WHERE table_name='verified_hadith_items' ORDER BY ordinal_position;"`
3. اجمع 10-15 حديثاً ضعيفاً معروفاً و10-15 موضوعاً معروفاً عبر WebSearch من
   dorar.net أو "سلسلة الأحاديث الضعيفة والموضوعة" — كل واحد بمصدر ودرجة
   وسبب واضح.
4. اكتب migration SQL في `supabase/migrations/` (راجع نمط ملفات migration
   الموجودة أولاً) يُدخل الصفوف بـ`verification_status = 'verified'`.
5. طبّقها، تحقّق بـ`SELECT count(*) FROM verified_hadith_items WHERE
   authenticity_class IN ('daif','mawdu');`، افتح `/hadith` محلياً وتأكد
   القسمان لم يعودا فارغين.
6. typecheck + build، commit، push.
