# بروتوكول التنفيذ — إلزامي في كل جلسة

## بداية الجلسة
- أنشئ فرعًا جديدًا من أحدث main باسم وصفي واضح، واعمل عليه حصرًا طوال الجلسة.
  **استثناء (2026-07-24):** داخل worktree يعمل على فرع `automation/content` أو `automation/tasks` تحديدًا، لا تُنشئ فرعًا جديدًا مطلقًا — ابقَ على نفس الفرع الثابت عبر كل المهام المتتالية؛ هذا هو صلب تصميم نافذتي العمل المتزامنتين (راجع بند 5 أدناه).
- **PR واحد لكل مهمة:** يُمنع فتح سلسلة Pull Requests متتابعة لنفس المهمة. حدّث الفرع/الـ PR الحالي بدل إنشاء phase2/phase3 منفصلة.
- حلّل المهام ورتّبها حسب الأولوية والتأثير، واختر أقصر مسار تنفيذي يحقق النتيجة كاملة.
- لا تقرأ ملفًا سبق تحليله إلا إذا تغيّر أو لزم للمهمة الحالية.

## تنفيذ كل مهمة
- تعديلات جزئية دقيقة؛ لا تعد كتابة ملفات كاملة لتعديل جزئي.
- أصلح السبب الجذري دائمًا؛ يُمنع الترقيع (هوامش سالبة، إخفاء عناصر، تعطيل وظائف).
- أعد استخدام المكونات الموجودة؛ لا مكونات مكررة ولا مكتبات جديدة إلا بمبرر متفوق.

## بوابة إغلاق المهمة — تسليم متوازٍ (2026-09-08)
**Implement → تحقق محلي سريع → push PR → فعّل auto-merge → انتقل فورًا.** لا تنتظر دمج/نشر Vercel أو انتهاء Actions إلا إذا اعتمدت المهمة التالية على نفس الملفات.

1. فرع مستقل باسم واضح لكل مهمة مستقلة؛ نفس الشاشة/الملفات → أكمل على نفس الفرع/الـPR.
2. تحقق محلي ضيق: اختبارات المسار المعدّل + `PORT=24216 BASE_PATH=/ pnpm --filter @workspace/majalis run build` (أو `pnpm run verify:ci -- --changed`/`--ui` عند الحاجة). عند الفشل: لا push.
3. ادفع → **PR Ready واحد** إلى `main` → `gh pr merge --auto --squash` (أو مسار `auto-merge-to-main.yml`).
4. **لا** تشغّل `gh pr checks --watch` ولا تنتظر Preview/Production بعد بدء الفحوصات — اترك الدمج والنشر في الخلفية.
5. استثناءات الانتظار: فشل CI على نفس الـPR (أصلح السبب الجذري هناك)، أو DB / iOS native / security بخطر مباشر، أو مهمة تالية تعتمد على نفس التغيير (تابع على نفس الفرع أو فرع فوق السابق ثم rebase بعد الدمج).
6. لا تعطّل CI ولا تعِد تشغيل عشوائي ولا تفتح phase2 لنفس المهمة.
   **استثناء (content-runner / automation):** كما هو — `scripts/content-runner.sh` ونافذتا `automation/*` عبر مساراتهما.

## صمامات الأمان وكفاءة الرصيد
- إذا اقترب انتهاء الوقت: أكمل المهمة الجارية فقط وارفعها، ولا تبدأ مهمة جديدة.
- لا تكرر أوامر نتائجها معروفة ولم تتغير البيئة.
- تحديثات مقتضبة أثناء التنفيذ؛ لا نسخ كاملة من الملفات في المخرجات.

## التقرير النهائي (عربي، مرة واحدة في النهاية)
- PRs المفتوحة: أيّها ينتظر CI، أيّها auto-merge مفعّل، روابطها؛ المهام المدفوعة دون انتظار الدمج؛ وأي فشل CI يحتاج إصلاحًا على نفس الـPR.
