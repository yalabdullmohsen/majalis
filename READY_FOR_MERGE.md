# READY_FOR_MERGE

> ملف حالة تقني فقط — **لا يعني اكتمال كل مراحل المحتوى (3-11)**. راجع
> `CONTINUATION_PLAN.md` للحالة الكاملة لما أُنجز وما تبقى من المحتوى،
> بما فيها قسم صريح "حالة المراحل 3-11" لا يدّعي اكتمالًا غير حقيقي.
> هذا الملف يوثّق فقط أن البوابات الفنية (typecheck/lint/tests/build)
> اجتازت معًا في دفعة واحدة نهائية، كما طلب المالك.

## ▶️ حالة الجلسة: جلسة سابعة — عمل داخلي بحت بلا WebSearch (بطلب صريح)

منذ آخر تحديث لهذا الملف (`516d97f8`): جلسة سادسة أضافت قرار مدروس بشأن
بنك الأسئلة (فحص `SECTION_TO_CATEGORY` + إصلاح خلل صامت + 8 أسئلة صعب
مستهدَفة)، دفعة ثامنة لإثراء العلماء (64/96)، وكتاب مكتبة جديد. **رصيد
WebSearch نفد بالكامل قرب نهاية تلك الجلسة.**

**الجلسة السابعة (2026-07-18)** نُفِّذت بالكامل **بلا أي WebSearch جديد**
بطلب صريح من المنسّق (الرصيد لم يتجدَّد بعد)، عبر 4 مهام بديلة: (1)
مراجعة اتساق داخلية شاملة لكل استشهادات SQL/أسئلة المسابقة المُضافة —
لا أخطاء جديدة؛ (2) تعبئة جدول `knowledge_relationships` الذي كان
فارغاً كلياً (70 علاقة: 32 مؤلف_كتاب + 33 شيخ_تلميذ + 5 مرتبط، كلها من
بيانات مُتحقَّقة مسبقاً) + إصلاح عطل صامت حقيقي (`TYPE_HREF.scholar`
كان يبني رابطاً لا يطابق أبداً فلتر `/lessons`)؛ (3) رصد بقايا CMS
يتيمة إضافية (`publicRoute` في `content-registry.ts` غير مُستدعى
إطلاقاً، نفس عطل الرابط كامن فيه لكن بلا تأثير حي)؛ (4) 3 فوائد فقهية
جديدة تُعيد استخدام استشهادات قرآنية مُتحقَّقة حرفياً سابقاً (508→511).

**تحقّق المنسّق من تجدُّد WebSearch لاحقاً في نفس الجلسة — اختُبر فعلياً
عبر استعلام حقيقي، لا يزال منتهياً (0/200).** تابع العمل التلقائي بلا
بحث: فحص منهجي لكل رابط `?param=` في الموقع كشف **9 روابط صامتة إضافية**
من نفس عائلة عطل `TYPE_HREF.scholar` (`/rulings?category=`,
`/library?category=`/`?cat=`, `/qa?cat=`, `/lessons?search=` [معروض
فعلياً على كل صفحة حكم شرعي], `/miracles`/`/stories`/`/fawaid?cat=`
[JSON-LD ذاتي], `/arbaeen-nawawi?h=`+`#hadith-` [معطوبان معاً]، وزر
"استعراض حسب النوع" في `KnowledgeGraphPage` [معطَّل بالكامل]) — كل رابط
يُبنى صحيحاً لكن الصفحة الهدف لا تقرأ الـquery param إطلاقاً فتهبط على
الفلتر الافتراضي بصمت. صُحِّحت جميعها بـ`useEffect` بسيط يقرأ الـparam
عند التحميل. ثم أُضيفت 4 فوائد أخرى تُعيد استخدام استشهادات مُتحقَّقة
(511→515).

**بطلب صريح من المنسّق: توقّف اختبار WebSearch كل دفعة من هنا فصاعداً
(يُفحص مرة كل عدة دفعات فقط).** أُكملت `UniversitiesPage`/`OccasionsPage`
بنفس منهجية الروابط الصامتة. ثم اكتُشف **عطل من نفس عائلة "أخلاق
إسلامية" لكن على نطاق أوسع بكثير**: 142 من 515 فائدة (~27%) في
`fawaid-seed.ts` و11 من 127 كتاباً في `library-catalog.ts` تحمل قيم
`category` لا تطابق أياً من شرائح الفلترة المعروضة فعلياً في
`FawaidPage`/`LibraryPage` — فتختفي صامتاً عند أي فلترة محدَّدة رغم
ظهورها تحت "الكل". صُحِّح الكل (126+11 إعادة تسمية لتصنيف قائم، 16
فائدة حصلت على شريحة جديدة أصيلة)؛ تحقّق برمجي: 0 تصنيف غير مرئي متبقٍ.
حالة مشابهة في `qa-seed.ts` (31 سؤالاً) وُثِّقت دون معالجة عمداً (ملف
auto-generated، والـDB الحية سليمة أصلاً). أُضيفت أيضاً أول 5 علاقات
"شرح_لكتاب" (كتاب↔كتاب) لـ`knowledge_relationships`.

**بطلب المنسّق، طُبِّقت نفس المنهجية على صفحات إضافية** (الدروس/الحكم/
الخرائط الذهنية/متشابهات القرآن/الطب النبوي/أسماء الله الحسنى — كلها
نظيفة). **الأذكار**: 10 من 305 ذكر بـcategoryId غير موجود، أُصلح
(فئة جديدة "الصلاة على النبي ﷺ" + طيّ فئة في "أذكار متنوعة"). **اكتشاف
أعمق من نفس العائلة في موسوعة الأحكام**: نظاما تصنيف متوازيان تماماً
(الحديث 20 تصنيفاً في `rulings-categories.ts` مقابل قديم 13 تصنيفاً في
`platform-types.ts` يُقيِّد نوع `rulings-seed.ts`) — 19 حكماً اختفت
صامتاً من فلاتر `RulingsPage`، **وأيضاً** نموذج تحرير حكم في لوحة
الإدارة كان يعرض القائمتين معاً (33 خياراً متكرراً) فيمكن لأي إداري
إعادة إنتاج نفس العطل لحكم جديد. أُصلح الجذر بالكامل. **6 علاقات
"درس_عن_كتاب" جديدة** (خامس نوع علاقة من 6 يُستخدَم لأول مرة). رصيد
WebSearch أُعيد اختباره مرة واحدة (بطلب المنسّق، لا كل دفعة) — لا يزال
منتهياً (0/200). تفصيل كامل في `CONTINUATION_PLAN.md` قسم "جلسة سابعة".

## الفرع

```
majalis-content-fill
```

**ممنوع الدمج لـ main. ممنوع النشر للإنتاج.** هذا الفرع للمراجعة فقط —
نافذة دمج خارجية ستتولى القرار النهائي.

## آخر commit مُتحقَّق منه (مدفوع فعلياً لـorigin)

```
7982fe9b...
7982fe9b  feat(knowledge-graph): إضافة 6 علاقات درس_عن_كتاب (درس↔كتاب)
2dcddead  fix(taxonomy): إصلاح تصنيفات غير مرئية — الأذكار + موسوعة الأحكام
4a97a2b3  docs: تحديث CONTINUATION_PLAN وREADY_FOR_MERGE — تصنيفات غير مرئية + شرح_لكتاب
9e00909e  feat(knowledge-graph): إضافة 5 علاقات شرح_لكتاب (كتاب↔كتاب)
19ac1015  fix(filters): إكمال UniversitiesPage/OccasionsPage + إصلاح تصنيفات غير مرئية (142 فائدة + 11 كتاباً)
07b84be2  docs: تحديث CONTINUATION_PLAN وREADY_FOR_MERGE — إصلاح 9 روابط صامتة + فوائد إضافية
952d373c  content(fawaid): إضافة 4 فوائد إضافية تُعيد استخدام استشهادات مُتحقَّقة سابقاً (511→515)
730c251e  fix(links): إصلاح 9 روابط صامتة إضافية (query params لا تُقرَأ في الصفحات الهدف)
3a103cc6  docs: تحديث CONTINUATION_PLAN وREADY_FOR_MERGE — جلسة سابعة (بلا WebSearch)
54e7ae4f  content(fawaid): إضافة 3 فوائد فقهية تُعيد استخدام استشهادات مُتحقَّقة سابقاً (508→511)
8084dcc0  feat(scholars): إثراء 8 علماء إضافيين — دفعة ثامنة Phase 7 (64/96 الآن)
```

سلسلة commits الجلسات كاملة (الأحدث أولاً):
`7982fe9b` ← `2dcddead` ← `4a97a2b3` ← `9e00909e` ← `19ac1015` ← `07b84be2` ← `952d373c` ← `730c251e` ← `3a103cc6` ← `54e7ae4f` ← `c0fe550d` ← `16f6ecc9` ← `8084dcc0` ← `e7bb1c0d` ← `9d094947` ← `516d97f8` ← `4feca6cf` ← `c8658a5c` ← `84de0d71` ← `865ba07e` ← `bf099d15` ← `4c7dd993` ← ...

`git status --short` فارغ وقت كتابة هذا التحديث. **مدفوع فعلياً**
لـ`origin/majalis-content-fill` (تحقّقتُ: `2dcddead..7982fe9b` ظهر في
مخرجات push الفعلية، وقبله `4a97a2b3..2dcddead` و`9e00909e..4a97a2b3`
و`19ac1015..9e00909e`).

## نتائج الاختبارات — دفعة تحقّق كاملة على commit `7982fe9b`، كلها نجحت معًا

كل الأوامر التالية شُغِّلت من `/Users/alabdullmohsen/majalis-content-fill/artifacts/majalis`
بالتتابع قبل الـcommit مباشرة، ثم مرة أخرى تلقائيًا عبر pre-commit hook
أثناء الـcommit نفسه، ثم مرة ثالثة عبر pre-push hook أثناء الـpush:

| البوابة | الأمر | النتيجة |
|---|---|---|
| TypeScript | `pnpm run typecheck` (`tsc -p tsconfig.json --noEmit`) | ✅ نجاح — 0 خطأ |
| ESLint | `pnpm run lint` (`eslint src lib --ext .ts,.tsx --max-warnings 50`) | ✅ نجاح — 0 تحذير |
| اختبارات الرجوع الشاملة | `pnpm run test:regression` | ✅ نجاح — exit code 0، **0 فشل** عبر كل الحزم الفرعية (16 حزمة، تفصيل أدناه) |
| البناء الإنتاجي الكامل | `pnpm run build` | ✅ نجاح — بما في ذلك فحوصات ما بعد البناء المدمجة (`test-dynamic-404-safety.mjs`, `test-no-regressions.mjs`) |

### تفصيل `pnpm run test:regression` (16 حزمة فرعية، كلها 0 فشل)

يشغّل بالترتيب: `test:seo` → `test:identity` → `test:guardrails` →
`lib/__tests__/assistant-safety.test.mjs` → `test:search-normalize` →
`test:scholars-integrity` → `test:governance` → `test:counts` →
`test:library-integrity` → `test:assistant-suggested` →
`test:sheikhs-dedup` → `test:inheritance-engine` (134 اختبار فرعي نجح) →
`test:learning-paths-engine` (53 اختبار فرعي نجح) →
`test:learning-assessment-grading` (14 اختبار فرعي نجح) →
`test:category-tree` (14 اختبار فرعي نجح) → `verify:quran-data` (5 فحوصات
نجحت: 114 سورة، 6236 آية، تطابق manifest مع الملفات الفعلية).

أعد التشغيل لاستنساخ النتيجة: `pnpm run test:regression` من داخل
`artifacts/majalis` على commit `7982fe9b` تحديداً.

### تفصيل `pnpm run build` — الفحوصات الداخلية (آخر تشغيل مؤكَّد)

```
✓ Pre-rendered 119 routes (58 skipped)
✓ post-build-seo: دُمج 732 ملف prerender → dist/
فُحص: 96 عالِمًا و127 كتابًا.
✓ كل سجل حي (عالِم/كتاب) له prerender مطابق — 404 الحقيقية آمنة.
فُحص: 732 صفحة مُصيَّرة مسبقًا.
✓ لا تسرّب لمحتوى الرئيسية، ولا بقايا فتاوى أو أزهر محذوفة.
```

**ملاحظة مهمة محفوظة من الدفعة السابقة**: رقم "96 عالِمًا" هو التأكيد
الآلي لإصلاح حقيقي (commit `df4735d5`، ضمن هذا الفرع) — قبل الإصلاح كانت
صفحة `/scholars` الحية تعرض 83 فقط رغم أن كل فحوصات البناء والاختبارات
كانت تتحقق من `scholars-data.ts` (96) لا من الصفحة الفعلية، فلم تكن
لتكتشف الفجوة وحدها. اكتُشفت فقط عبر تشغيل يدوي إضافي لـ
`scripts/audit-all-routes.mjs` (Playwright ضد خادم `vite preview` فعلي) —
موثَّق بالتفصيل في `CONTINUATION_PLAN.md`.

### سكربتات تدقيق محتوى إضافية (خارج `test:regression` الرسمي، شُغِّلت يدويًا في جلسات سابقة وتحقّقت 0 مشكلة، لم تُعَد بالضرورة في هذه الدفعة الأخيرة لأن لا تغيير طرأ على الملفات التي تفحصها)

- `node scripts/audit-data-quality.mjs` → 0 مشكلة (qa/lessons/sheikhs/quiz/fawaid/adhkar)
- `npx tsx scripts/audit/hadith-takhrij-check.mjs` → 0 حالة (qa/fawaid/adhkar/arbaeen/quiz)
- `npx tsx scripts/content-audit.mjs` → أرقام دقيقة مطابقة (لا تحقّق نجاح/فشل، تقرير فقط)

## الحالة الحقيقية لمحتوى المشروع (3-11) — غير مكتملة، موثَّقة بدقة تامة

هذا الملف يشهد فقط أن **الشيفرة سليمة فنيًا**. العمل الموضوعي (محتوى
شرعي موسَّع، مسارات تعليمية كاملة، مكتبة، علماء، بنك أسئلة، مقالات)
**لم يكتمل** بحكم حجم التكليف الأصلي (16 مرحلة). راجع القسم الصريح
"حالة المراحل 3-11" في أعلى `CONTINUATION_PLAN.md` — يذكر بوضوح تام أن:

- **Phase 3 (موسوعة العلوم الشرعية الهرمية)**: بدأت فعليًا — مقرر "أركان
  الإيمان الستة" (7 عناصر) داخل aqeedah، مسار "علوم القرآن" كاملاً (11
  عنصراً عبر 3 مراحل)، باب "أقسام الحديث" وباب "نخبة الفكر" (4 عناصر
  لأنواع مخالفة الثقات) داخل `mustalah-hadith`، باب "أقسام التوحيد
  الثلاثة" داخل `tawheed`، باب "الغزوات الكبرى" داخل `seerah`، باب
  "تفسير سورة يوسف" داخل `tafseer`، وباب "الفقه الحنفي بعمق" داخل
  `fiqh` — إضافة لنموذج usool-fiqh. **بهذا اكتمل تعميق كل مسارات دفعة
  الاكتشاف الأصلية الخمسة (hadith/fiqh/tafseer/seerah/tawheed)** — كل
  واحد له باب واحد على الأقل مربوط فعلياً بكتاب حقيقي. **اكتشاف منهجي
  إضافي**: استعلام شامل على كل learning_paths (لا الخمسة فقط) كشف
  مقررات "صحيح البخاري"/"صحيح مسلم" داخل hadith موجودة بعناصر تطابق
  الكتابين حرفياً لكن بلا ربط — رُبطت بلا تعديل العناصر. لا يزال عدد
  قليل من عشرات الأبواب المطلوبة إجمالاً (كل مسار له مقررات أخرى لا
  تزال ضحلة — قائمة كاملة في CONTINUATION_PLAN.md).
- **Phase 4-5 (مسارات تعليمية): 7/7 مسارات كانت فارغة تماماً الآن
  `published` بمحتوى حقيقي كامل** (uloom-quran، akhlaq، adab، nahw،
  arabic، dawah، tarbiyah)، إضافة لـusool-fiqh (النموذج الأول) ومقرر
  aqeedah/arkan-al-iman (7 عناصر) داخل مسار أصلاً منشور. **8 مسارات
  تعليمية حقيقية الآن من أصل ~19 مطلوبة في التكليف الكامل** — باقي ~11
  مساراً (كانت أصلاً غير فارغة أو خارج نطاق "السبعة الفارغة" المستهدَفة)
  لم يُبنَ بعد.
- Phase 6 (توسيع المكتبة): بدأ فعلياً — 12 كتاباً مُتحقَّقة فردياً
  (117→127). آخر ثلاثة كتب أُضيفت خصيصاً لربط عناصر تعليمية كانت موجودة
  مسبقاً بلا كتب مطابقة في الفهرس — نمط ناجح مُكرَّر ثلاث مرات. لا يزال
  بعيداً عن أي هدف توسّع كبير.
- Phase 7 (توسيع العلماء): بدأ فعلياً — 64 من 96 عالماً (ثلثا العلماء)
  أُثريَ ببيانات شيخ/تلميذ ومصادر ببليوغرافية حقيقية عبر ثماني دفعات
  (كان `sources: []` فارغاً للجميع تقريباً). تضمّنت الدفعة السابعة
  تصحيح ادّعاء خاطئ موجود مسبقاً (عفيفي). 32 لا يزالون بلا مصادر.
- **Phase 8 (الجامعات): الوحيدة المكتملة تقريبًا** — 7→15، كلها محقَّقة فردياً.
- Phase 9 (بنك الأسئلة): بدأ فعلياً — 914→943 (29 سؤالاً جديداً بشرح
  ومرجع مستقلين عبر ثلاث دفعات، بعد أن كان 914 نتيجة حذف مكررات من 950
  فقط لا إضافة). الدفعة الثالثة استهدفت أرقّ خلية فعلية في اللعبة الحية
  (مستوى "صعب" لفئتي sira/anbiya) بعد فحص طبقة `SECTION_TO_CATEGORY`
  التي كشفت أيضاً سؤالاً كان يُسقَط صامتاً من اللعبة الحية (صُحِّح).
- Phase 10 (مقالات): بدأ فعلياً — جدول `articles` جديد (RLS مُتحقَّق: anon
  يرى 0 صف)، **مقالتان** كاملتا الجودة الآن (`is_approved=false` لكلتيهما،
  تربطان عمداً نطاقين مختلفين تماماً من المحتوى المبني هذه الجلسات). لا
  صفحة عرض علنية بعد (قرار متعمَّد، خارج نطاق العمل المسند لهذا الوكيل).
- Phase 11 (ربط داخلي): أُصلحت فجوة سيطرية (روابط الكتب في صفحات كل
  المسارات التعليمية كانت نصاً بلا رابط) **ومُتحقَّق منها بصرياً فعلياً
  عبر Playwright** (43/45 رابط يعمل ويتنقّل فعلاً لصفحة الكتاب الصحيحة،
  لا فحص DOM سطحي)، وصُحِّحت تسميتان متعارضتان في مسارات أقدم. **إضافة
  الجلسة السابعة**: جدول `knowledge_relationships` (نظام "مواد مرتبطة"
  الفعلي المُستهلَك في `LibraryDetailPage`/`LessonDetailPage`) كان
  فارغاً كلياً 0 صف — عُبِّئ بـ70 علاقة بين العلماء (مؤلف_كتاب/
  شيخ_تلميذ/مرتبط)، وأُصلح عطل صامت مرافق (`TYPE_HREF.scholar` كان
  يبني رابطاً معطوباً بلا 404 ظاهرة). أنواع علاقات أخرى يدعمها الجدول
  فعلياً (`fawaid`↔`book`، `lesson`↔`book`) لم تُملأ بعد. بحث موحّد عبر
  كل أنواع المحتوى لم يُلمَس بعد.

بقايا صغيرة **مكتملة الآن** (الجلسة الرابعة): توسيع دفعة daif/mawdu
14→32 (`verified_hadith_daif_mawdu_seed_v2.sql`)، تنظيف بقايا CMS ليتيمة
`KnowledgeSourceType` (نطاق آمن مُصغَّر، `CmsContentKind` الأوسع تُرك عمدًا)،
مراجعة عيّنة من `fawaid-seed.ts` (28 اقتباسًا، ليست مراجعة شاملة).

## القيود السارية (بلا تغيير)

- فرع `majalis-content-fill` فقط. بلا دمج لـ`main`. بلا نشر إنتاج.
- كل تعديل قاعدة بيانات طُبِّق مباشرة على المشروع الحي
  `ngmvmlulzacrlicuagyp` عبر migration SQL محفوظة في `artifacts/majalis/
  supabase/*.sql` (موثَّقة بالكامل، قابلة للمراجعة والتراجع يدويًا عند
  الحاجة عبر DELETE/UPDATE مطابق).

```
READY_FOR_MERGE=true
```

(هذا يعني: البوابات الفنية الأربع نجحت معًا في دفعة واحدة فعليًا على آخر
commit، كما طُلب — **وليس** أن كل مراحل المحتوى الـ16 اكتملت (راجع القسم
أعلاه — لم تكتمل، وموثَّق بالتفصيل أين توقف العمل وما التالي في
`CONTINUATION_PLAN.md`). القرار النهائي بشأن الدمج الفعلي لـmain متروك
للمالك عبر نافذة دمج خارجية، كما هو مفهوم ومتفَق عليه. العمل مستمر
تلقائيًا حسب آخر توجيه صريح ("اكمل") — سيُحدَّث هذا الملف بعد كل دفعة
تقنية ناجحة جديدة.)
