# الأداء — قياس محدث (١٣ أغسطس ٢٠٢٦)

| مقياس | مقيس (dist محلي بعد #1080) | الهدف | حالة |
|--------|----------------------------|--------|------|
| JS رئيسي `index-*.js` gzip | **134 135 B (~131 KB)** | <250 KB ابتدائي | مقبول للقطعة الرئيسية؛ الحزمة الكلية فوق الهدف بسبب بذور محتوى كسولة |
| CSS حرج `index-*.css` gzip | **67 388 B (~66 KB)** | <60 KB | **فوق الهدف** |
| أكبر قطعة كسولة | `fawaid-curated-seed` ~132 KB gzip | — | تُحمَّل عند الطلب |
| LCP / TTI / INP / CLS @390×844 4G | لم تُقَس بـLighthouse في هذه الجلسة | <2.5s / <2.5s / <200ms / <0.1 | معلّق |
| كاش `/` و`index.html` | `public, max-age=0, must-revalidate` | بلا `no-store` على العامة | **مُصلَح** |
| كاش `version.json` / `sw.js` | `no-store` | خاص بالنشر | محفوظ |

## قرارات
1. code-splitting على مستوى المسار قائم عبر `lazyWithRetry` في `App.tsx`.
2. SW: network-only للتنقلات؛ كاش بيانات/أذكار؛ مربوط بـ`SW_BUILD_ID`.
3. الخطوة التالية للأداء: تفكيك CSS الحرج وخفض بذور المحتوى في المسار الحرج.
