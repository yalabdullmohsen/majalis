# CONTENT_GAPS_REPORT — سُنّة (جولة 3)

تاريخ: 2026-09-16  
القاعدة: **لا اختراع محتوى شرعي**. الفراغ يُوثَّق أو Empty State — لا نص غير موثّق.

## ملخص

| الفئة | الحالة | الإجراء |
|---|---|---|
| كتب مكتبة بلا URL مصدر | ~190 `pending_review` / `sources: []` | غير منشورة كروابط موثّقة؛ لا اختراع URL |
| معرفة تاريخ/تفسير | 3578 verified في ملفات المعرفة المفحوصة | لا ملء إضافي بلا مصدر |
| منتج المجمع الفقهي (UI/API/sync/sitemap) | **مُزال من الأسطح الحية r3** | Redirect `/fiqh-council` → `/fiqh` يبقى |
| فهرس البحث الموحّد للحديث | مداخل أقسام فقط | متعمد — لا megaseed |
| Supabase بدون أسرار محلية | — | Empty متوقع للدروس/الحساب |
| قرارات فقهية كمنتج | 0 حي في الكود | محظور |
| جداول council المستضافة | قد تبقى بيانات قديمة | **MANUAL_OWNER_ACTION** |

## Needs verification (لا تُنشر)

1. روابط مصادر كتب المكتبة حتى توثيقها من مراجع معتمدة خارج المستودع.
2. `content/fiqh/deferred-nawazil.json` — مراجعة داخلية فقط.
3. موسوعة أحكام مؤرشفة (`/rulings` → `/fiqh`) — سجلات `pending_review` بعد إزالة بذور المجمع.
4. إشارات نصية تاريخية إلى قرارات مجامع داخل أرشيف `rulings-encyclopedia` — ليست منتجًا حيًا ولا في البحث/sitemap.
5. أي صفوف `fiqh_council_*` متبقية على Supabase المستضاف حتى تطبيق migration المالك.

## Unsupported (لا تُنشر)

- حديث بلا مصدر/رقم مثبت.
- نسبة إلى عالم/مذهب بلا مرجع.
- أي «قرار مجمع» أو «قرار فقهي» كمنتج داخل سُنّة.
- نصوص مولَّدة بالذكاء الاصطناعي كمصدر شرعي.

## ما أُصلح في الجولة 3

- قطع مسار البحث الحي عن `fiqh_council_items`.
- قطع المزامنة الدورية للمجمع.
- إزالة مؤسسة المجمع من catalog العام.
- قطع sitemap/RSS عن جداول/عناوين القرارات المجمعية.
- قطع محرك البحث الفقهي عن RPC/جداول المجلس.
- تصحيح فتاتات SEO ووصف المعالم المكرّر الصياغة.
- إعداد migration idempotent للمالك دون ادّعاء التنفيذ المستضاف.

## MANUAL_OWNER_ACTION

الملف: `supabase/fiqh_council_product_purge_v1.sql`

1. نسخة احتياطية للجداول المعنية عبر لوحة Supabase (المالك فقط).
2. تشغيل الـmigration يدويًا على المشروع المستضاف.
3. التحقق أن الواجهة/البحث لا تعرض قرارات (متوقع بعد r3 حتى قبل الـmigration).
4. **لا يُدَّعى** في هذا الـPR أن الحذف المستضاف نُفِّذ.

Rollback: استعادة من النسخة الاحتياطية فقط؛ لا إعادة أسطح المنتج العام.

## ما لن يُملأ هنا

- مقالات/تراجم مولَّدة بالذاكرة أو بالذكاء الاصطناعي.
- فهرسة آلاف الأحاديث داخل `search/index.json`.
- أحكام فقهية جديدة أو درجات حديث مخترَعة.
- إعادة منتج المجمع الفقهي بأي اسم.
