# CONTENT_GAPS_REPORT — سُنّة (جولة 2)

تاريخ: 2026-09-16  
القاعدة: **لا اختراع محتوى شرعي**. الفراغ يُوثَّق أو Empty State — لا نص غير موثّق.

## ملخص محدّث

| الفئة | الحالة | الإجراء |
|---|---|---|
| كتب مكتبة بلا URL مصدر | ~190 `pending_review` / `sources: []` | تبقى غير منشورة كروابط موثّقة؛ لا اختراع URL |
| معرفة تاريخ/تفسير | **0 needs_review** في ملفات المعرفة المفحوصة (3578 verified) | تحديث manifest تم |
| كود المجمع الفقهي UI/بذور | **محذوف** من src الحي | Redirect `/fiqh-council` → `/fiqh` يبقى |
| فهرس البحث الموحّد للحديث | 6 مداخل أقسام (لا 14940 نصًا) | متعمد — البحث داخل الحديث في واجهة `/hadith`؛ لا megaseed في الفهرس الموحّد |
| Supabase بدون أسرار محلية | — | Empty متوقع للدروس/الحساب |
| قرارات فقهية كمنتج | 0 حي | محظور |

## Needs verification (لا تُنشر)

1. روابط مصادر كتب المكتبة حتى توثيقها من مراجع معتمدة خارج المستودع.
2. `content/fiqh/deferred-nawazil.json` — مراجعة داخلية فقط.
3. موسوعة أحكام مؤرشفة (`/rulings` → `/fiqh`).
4. إدخال المؤسسات `fiqh-council-mecca` في catalog إن بقي `needs_review`.

## Unsupported (لا تُنشر)

- حديث بلا مصدر/رقم مثبت.
- نسبة إلى عالم/مذهب بلا مرجع.
- أي «قرار مجمع» كمنتج داخل سُنّة.

## ما أُصلح في الجولة 2 (موثّق)

- علامة تجارية: خصوصية SEO + عنوان بطاقات الحفظ → سُنّة.
- درس/ندوة النوازل: إزالة وسم «مجمع فقهي» واستبدال اسم القاعة بـ«قاعة وزارة الأوقاف» (بلا اختراع محتوى شرعي).
- حذف أسطح UI وبذور المجمع غير الموصولة.

## MANUAL_OWNER_ACTION

إن وُجدت جداول Supabase قديمة لـ `fiqh_decision` / council على المشروع المستضاف:

1. مراجعة الجداول عبر لوحة Supabase (المالك فقط).
2. تطبيق migration idempotent للحذف إن وُجدت بيانات منتجية.
3. لا يُدَّعى الحذف المستضاف من هذا الـPR دون صلاحيات.

## ما لن يُملأ هنا

- مقالات/تراجم مولَّدة بالذاكرة أو بالذكاء الاصطناعي.
- فهرسة 15k حديث داخل `search/index.json` (حجم/حزمة).
- أحكام فقهية جديدة أو درجات حديث مخترَعة.
