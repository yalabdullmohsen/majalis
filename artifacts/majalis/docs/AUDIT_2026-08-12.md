# تدقيق سُنّة — ١٢ أغسطس ٢٠٢٦

> أرقام مقيسة من المستودع وقت البناء على `main@4511e69d9` وما تلاه، لا تقديرات.

## ١. الحزمة الابتدائية (من `dist/` بعد بناء محلي)

| الأصل | خام | gzip |
|------|-----|------|
| JS رئيسي `index-*.js` الأكبر | 462 955 B | **134 449 B (~131 KB)** |
| CSS حرج `index-CKDUaKLE.css` | 356 316 B | **66 448 B (~65 KB)** |

- هدف JS < 250 KB gzip: **غير محقّق** (١٣١ KB للقطعة الرئيسية فقط — مقبول نسبياً؛ الهدف يشمل كل الابتدائي).
- هدف CSS < 60 KB gzip: **غير محقّق** (٦٥ KB حرج).

LCP/TTI/INP/CLS على 390×844 + 4G: **لم تُقَس في هذه الجلسة** (تتطلّب Lighthouse CI) — تُسجَّل في `PERFORMANCE.md` عند تشغيل القياس.

## ٢. CSS والمسارات

| مقياس | القيمة |
|--------|--------|
| ملفات `src/styles/**/*.css` | **217** |
| `src/views/**` | **211** |
| `elite-2026` / `majalis-v2` / `modern-2026` | **محذوفة أصلاً** (غير موجودة على القرص) |
| مستوردة من `main.tsx` | brand-v4 · design-system · final-release · m2030/{foundation,navigation,pages,interactions} |
| يتيم مقيس | `cookie-consent.css` (غير مستورد من المكوّن) |
| pitch/promo/mockup-sandbox | مستثناة من `build`/`typecheck` في جذر المستودع |

## ٣. Route Audit (مختصر — مسارات حرجة)

| المسار | المتوقع | الفعلي قبل الإصلاح | ملاحظة |
|--------|---------|---------------------|--------|
| `/library` | فهرس كتب | **Redirect → `/`** | مكتبة `LibraryPage` موجودة وغير موصولة |
| `/library/:id` | تفصيل كتاب | يعمل | |
| `/mushaf` | مصحف QPC | يعمل | |
| `/quran/mushaf` | → `/mushaf` | **غير معرّف** | |
| `/prayer` | مواقيت | **غير معرّف** | `/prayer-times` يعمل |
| `/prayer-times` | مواقيت | يعمل | |
| `/more` | صفحة المزيد | **لا مسار** (ورقة سفلية فقط) | |
| `/updates` | تحديثات | **Redirect → `/`** | `UpdatesPage` موجود |
| `/knowledge-graph` | رسم معرفي | **Redirect → `/`** | `KnowledgeGraphPage` موجود |
| `/search` | بحث | يعمل | ليس في sitemap (مقصود لاحقاً) |
| `/quran-hub` `/lessons` `/adhkar` `/scholars` `/fiqh` `/hadith` `/quiz` | أقسام | تعمل | |

`sitemap.xml`: يحتوي `/library` و`/updates` و`/knowledge-graph` رغم التحويل للرئيسية — **تناقض SEO**.

## ٤. الوصول (عيّنة)

| مقياس | قيمة |
|--------|------|
| تطابقات `aria-label` في `*.tsx` | **1022** |
| ملفات أزرار مرشّحة بلا فحص كامل | 253 ملفاً (يحتاج بوابة تباين/لمس لاحقاً) |

## ٥. المصحف

- PR مفتوح: **#1072** (`feat/mushaf-minimal-layout`) — تصميم بسيط flow · S ثابت · بلا أرابيسك.
- تشخيص «خطوط المصحف»: `QpcFontPackBanner` — يظهر عند غياب الخط محلياً؛ **لا علم DEV** بعد.
- بوابات `test:mushaf-gates` قائمة.

## ٦. MJL-20260808

- **لا أثر** للمعرّف في `docs/` أو الكود.
- أقرب إطار: قوالب `MJL-…` في `docs/Security.md` / Disaster-Recovery.
- التشخيص: يُعامل كـ«حادثة تحميل بعد نشر» عامة — راقب SW/`version.json` وSupabase؛ لا سبب جذري مسمّى في المستودع.

## ٧. PRs مفتوحة (وقت التدقيق)

| PR | التصنيف |
|----|---------|
| #1072 مصحف بسيط | جاهز/قيد Verify build |
| #893 ترويسة تسويقية | قديم — مراجعة محتوى |

## ٨. ErrorBoundary

موجود: `src/components/ErrorBoundary.tsx` + `SectionErrorBoundary`؛ موصول في `main.tsx` و`SafeLazyRoute`.

## تحديث قياس الجلسة (2026-08-13T07:23Z)

| مقياس | قيمة |
|---|---|
| ملفات CSS تحت `src/` | 223 |
| مسارات كانت تُحوَّل للرئيسية (مكتبة/تحديثات/رسم بياني) | أُصلحت في هذا الفرع |
| `/quran/mushaf` | Redirect دائم → `/mushaf` |
| `/prayer` | Redirect دائم → `/prayer-times` |
| `/more` | MorePage حقيقية |
| طبقة native-feel (#1078) | مدموجة — لا تُحذف |
| PRs مفتوحة عند الجرد | #1072 mushaf-minimal (CI فاشل) · #1073 routes (يُستبدل بهذا) · #1074 font banner (تعارض) · #1075 rules (Verify أخضر سابقاً) · #893 content |

### تشخيص MJL-20260808 (مختصر)
لم يُعثر على رمز الحادثة في المستودع. الفرضية العاملة من سجلات النشر: انهيار تحميل الأصول بعد نشر مع كاش SW قديم أو مسار `webDir` خاطئ. التخفيف القائم: `sw-version.js` مربوط بالـcommit + `purgeNativeWebRuntimeCaches`. بوابة: `verify-deploy-cache-versioning.mjs`.
