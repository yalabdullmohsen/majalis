# الأمن — حالة أولية

| بند | حالة |
|-----|------|
| مفتاح المتصفح | anon فقط (مراجعة مستمرة) |
| RLS على bookmarks/sr_*/rulings | يُراجع عبر `test:supabase-policy-audit` |
| CSP / رؤوس الأمان | موجودة جزئياً على Vercel — تدقيق لاحق |
| kill-switch صوت | موثّق في سياسات المنصة |
| LICENSE_RISKS | يُحدَّث لـ everyayah / mp3quran |

لا أسرار مقصودة في الحزمة الأمامية؛ أي كشف يُبلَّغ فوراً.
