# جولة توحيد واجهة سُنّة — سجل التنفيذ

تاريخ البدء: 2026-09-11 · الفرع: `cursor/ssunnah-ui-unify-polish`

## الحصر (ملخص)

| الفئة | العدد التقريبي | ملاحظة |
|---|---:|---|
| شاشات `src/views` | ~211 | تشمل تفاصيل وأدوات وإدارة |
| صفحات `src/pages` | ~174 | مسارات منتج أساسية |
| مكوّنات DS جاهزة | AppCard / FeatureCard / ContentCard / ScreenShell | موجودة قبل الجولة |
| بطاقات قديمة (ui-card/mj-card/…) | مئات الاستخدامات | جُسرت عبر `ssunnah-card-unify.css` |
| هكس في CSS صفحات | آلاف | دين طويل الأمد — ترحيل تدريجي للرموز |

## P0 المنفَّذ في هذه الدفعة

1. إزالة عبارات «جارٍ/جاري تحميل…» من واجهة المستخدم واستبدالها بهيكل/`تحديث…` سياقي أو هيكل صامت.
2. `LazyRouteFallback` بلا نص ظاهر (aria فقط).
3. رموز دلالية منتج: `ssunnah-semantic-tokens.css`.
4. جسر توحيد بطاقات: `ssunnah-card-unify.css`.
5. عناوين أقسام الإعدادات أوضح وظيفيًا.
6. توسيع بوابة `loading-ux-gates` لمنع رجوع صيغ التحميل.

## لم يُكتمل بعد (موانع إطلاق جزئية بصرية)

- ترحيل كل ملفات `styles/pages/*.css` من الهكس إلى الرموز.
- ربط كل الشاشات بـ ScreenShell صراحةً.
- مراجعة جهاز حقيقي TestFlight لكل تبويب.
- حذف الطبقات القديمة بعد اكتمال الترحيل 100٪.

## حماية شرعية

- لا تعديل على نص قرآني/حديث/متون.
- المصحف مستثنى من جسور البطاقات عبر `:not([data-scripture])` و`.mushaf`.

## دفعة P1 — 2026-09-11 (`cursor/ssunnah-ui-unify-p1`)

1. إسكات نصوص الانشغال الظاهرة في الصلاة/البحث/الأذكار/التسبيح والملاحة والإعدادات.
2. الإبقاء على القوائم السابقة أثناء إعادة الجلب في الحديث/الفوائد/الأحكام (بلا وميض هيكل).
3. تنظيم الإعدادات: عناوين فرعية للمظهر مقابل القراءة/المصحف، ونقل تحديث النسخة إلى التخزين.
4. توسيع جسر البطاقات لـ `ads-card` وبطاقات نتائج البحث.
5. ربط `loading-ux-gates` و`ssunnah-polish-pass` بسلسلة soft-cards في CI.

## دفعة P2 — 2026-09-11

1. إيقاف إعادة تحميل ثيم البطاقات/التهدئة بعد `final-release` لمنع وميض الهوية عند الدخول.
2. توسيع جسر البطاقات لبطاقة تركيز الأذكار.
3. ربط الأذكار بـ `UtilityScreen` + هياكل مسار صامتة للصلاة/الإعدادات/البحث/الأذكار.
4. بوابة تمنع رجوع سلسلة إعادة تحميل ثيم البطاقات بعد final-release.

## دفعة P3 — 2026-09-11

1. إصلاح بوابة طبقات المصحف لتوافق حالة البحث الصامتة (`تحديث النتائج`).
2. ترحيل ألوان صلبة عالية الأثر في الصلاة/الرئيسية/البحث/الإعدادات إلى رموز `--mj-*`.
3. ربط التسبيح بـ `UtilityScreen`.

## دفعة P4 — 2026-09-11

1. ربط شاشات Hub إضافية بـ ScreenShell (فوائد، أدعية، معجم، بطاقات، خطط قراءة، قبلة، ورد، أذان، إشعارات).
2. رموز لونية لدرجات الأذكار وأسطح المكتبة.
3. تهدئة نصوص الانشغال في الأذان/الإشعارات/الفوائد/الحذف.

## دفعة P5 — 2026-09-11

1. ترحيل هكس درجات الحديث وحدود بطاقات Hub والفقه إلى رموز `--mj-*`.
2. توسيع جسر البطاقات لـ `hadith-hub-card` / `fiqh-category-card`.
3. تهدئة انشغال الدخول/البحث العلمي/خطط القراءة وربط شاشات إضافية بـ ScreenShell.

## دفعة P6 — 2026-09-11

1. تهدئة انشغال الأزرار/البحث/المزامنة/التصدير في الصلاة والفقه والبطاقات والاستشهادات والحساب.
2. ScreenShell (`compose="mark"`) لمراكز القرآن/المسابقات/فهرس السور/تعلّمي/القواعد/كتب الحديث.
3. إبقاء قوائم كتب الحديث السابقة أثناء التبديل + هيكل صامت عند الفراغ فقط.
4. رموز شارات البحث + توسيع جسر البطاقات (`cmp-card` / `myl2-card` / `fq-card`).
5. توسيع `loading-ux-gates` لمسارات الانشغال الجديدة.
