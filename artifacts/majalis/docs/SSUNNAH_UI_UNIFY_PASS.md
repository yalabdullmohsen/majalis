# جولة توحيد واجهة سُنّة — سجل التنفيذ

تاريخ البدء: 2026-09-11 · الفرع: `cursor/ssunnah-ui-unify-polish`

## الحصر (ملخص)

| الفئة | العدد التقريبي | ملاحظة |
|---|---:|---|
| شاشات `src/views` | ~211 | تشمل تفاصيل وأدوات وإدارة |
| صفحات `src/pages` | ~174 | مسارات منتج أساسية |
| مكوّنات DS جاهزة | AppCard / FeatureCard / ContentCard / ScreenShell | موجودة قبل الجولة |
| بطاقات قديمة (ui-card/mj-card/…) | مئات الاستخدامات | جُسرت عبر `ssunnah-card-unify.css` |
| هكس في CSS صفحات | آلاف | دين طويل الأمد — ترحيل تدريجي للرموز |

## P0 المنفَّذ في هذه الدفعة

1. إزالة عبارات «جارٍ/جاري تحميل…» من واجهة المستخدم واستبدالها بهيكل/`تحديث…` سياقي أو هيكل صامت.
2. `LazyRouteFallback` بلا نص ظاهر (aria فقط).
3. رموز دلالية منتج: `ssunnah-semantic-tokens.css`.
4. جسر توحيد بطاقات: `ssunnah-card-unify.css`.
5. عناوين أقسام الإعدادات أوضح وظيفيًا.
6. توسيع بوابة `loading-ux-gates` لمنع رجوع صيغ التحميل.

## لم يُكتمل بعد (موانع إطلاق جزئية بصرية)

- ترحيل كل ملفات `styles/pages/*.css` من الهكس إلى الرموز.
- ربط كل الشاشات بـ ScreenShell صراحةً.
- مراجعة جهاز حقيقي TestFlight لكل تبويب.
- حذف الطبقات القديمة بعد اكتمال الترحيل 100٪.

## حماية شرعية

- لا تعديل على نص قرآني/حديث/متون.
- المصحف مستثنى من جسور البطاقات عبر `:not([data-scripture])` و`.mushaf`.

## دفعة P1 — 2026-09-11 (`cursor/ssunnah-ui-unify-p1`)

1. إسكات نصوص الانشغال الظاهرة في الصلاة/البحث/الأذكار/التسبيح والملاحة والإعدادات.
2. الإبقاء على القوائم السابقة أثناء إعادة الجلب في الحديث/الفوائد/الأحكام (بلا وميض هيكل).
3. تنظيم الإعدادات: عناوين فرعية للمظهر مقابل القراءة/المصحف، ونقل تحديث النسخة إلى التخزين.
4. توسيع جسر البطاقات لـ `ads-card` وبطاقات نتائج البحث.
5. ربط `loading-ux-gates` و`ssunnah-polish-pass` بسلسلة soft-cards في CI.

## دفعة P2 — 2026-09-11

1. إيقاف إعادة تحميل ثيم البطاقات/التهدئة بعد `final-release` لمنع وميض الهوية عند الدخول.
2. توسيع جسر البطاقات لبطاقة تركيز الأذكار.
3. ربط الأذكار بـ `UtilityScreen` + هياكل مسار صامتة للصلاة/الإعدادات/البحث/الأذكار.
4. بوابة تمنع رجوع سلسلة إعادة تحميل ثيم البطاقات بعد final-release.

## دفعة P3 — 2026-09-11

1. إصلاح بوابة طبقات المصحف لتوافق حالة البحث الصامتة (`تحديث النتائج`).
2. ترحيل ألوان صلبة عالية الأثر في الصلاة/الرئيسية/البحث/الإعدادات إلى رموز `--mj-*`.
3. ربط التسبيح بـ `UtilityScreen`.

## دفعة P4 — 2026-09-11

1. ربط شاشات Hub إضافية بـ ScreenShell (فوائد، أدعية، معجم، بطاقات، خطط قراءة، قبلة، ورد، أذان، إشعارات).
2. رموز لونية لدرجات الأذكار وأسطح المكتبة.
3. تهدئة نصوص الانشغال في الأذان/الإشعارات/الفوائد/الحذف.

## دفعة P5 — 2026-09-11

1. ترحيل هكس درجات الحديث وحدود بطاقات Hub والفقه إلى رموز `--mj-*`.
2. توسيع جسر البطاقات لـ `hadith-hub-card` / `fiqh-category-card`.
3. تهدئة انشغال الدخول/البحث العلمي/خطط القراءة وربط شاشات إضافية بـ ScreenShell.

## دفعة P6 — 2026-09-11

1. تهدئة انشغال الأزرار/البحث/المزامنة/التصدير في الصلاة والفقه والبطاقات والاستشهادات والحساب.
2. ScreenShell (`compose="mark"`) لمراكز القرآن/المسابقات/فهرس السور/تعلّمي/القواعد/كتب الحديث.
3. إبقاء قوائم كتب الحديث السابقة أثناء التبديل + هيكل صامت عند الفراغ فقط.
4. رموز شارات البحث + توسيع جسر البطاقات (`cmp-card` / `myl2-card` / `fq-card`).
5. توسيع `loading-ux-gates` لمسارات الانشغال الجديدة.

## دفعة P7 — 2026-09-11

1. ScreenShell لمحاور علوم القرآن/التفسير/عبادة القرآن/الزكاة/الحج/مصطلح الحديث/الأربعين/الأحكام/مساعدة الأذان/مراتب الصلاة.
2. تهدئة انشغال منتج إضافي (حراسة الدخول، حدود الخطأ، الرئيسية، البحث الأكاديمي، الشهادات، المسارات، التنزيل الجماعي).
3. ترحيل هكس عالية الظهور في الأربعين/الإشعارات/محاور الأقسام/إعدادات الأذان إلى رموز `--mj-*`.
4. توسيع جسر البطاقات لـ `an-card` / `zk-card` / `hj-card` / `hs-term-card` / `prayer-rank-card` / `qwh-card`.

## دفعة P8 — 2026-09-11

1. ScreenShell لتجويد/قراءات/أحرف/أعلام/تلاوة/إحصاءات/تحفيظ/بحث قرآني/حلقات/أدعية قرآنية/قصص سور/أصول فقه/صلاة/جنازة/مواريث/دروس الكويت.
2. تهدئة انشغال التقييم/الاستشهاد/المساعد.
3. ترحيل هكس محايدة/هوية في الحديث/الفقه/الدروس/التفسير + توسيع جسر البطاقات.

## دفعة P9 — 2026-09-11

1. ScreenShell لتفاصيل فقه/حديث/قرآن/مسابقات/دورة سنوية + حاسبة مواريث + حلقة حفظ.
2. إبقاء نتائج البحث والأذكار أثناء إعادة الجلب/الفشل (بلا وميض فراغ).
3. ترحيل هكس إضافية في قصص الأنبياء/البحث/الحديث/الفقه/الأربعين.

## دفعة P10 — 2026-09-11

1. ScreenShell لصفحات views عالية الزيارة (صوم، أطفال، تقويم، بطاقات، من نحن، تحديثات، خصوصية، إيمان، …) + FiqhGuidePage.
2. إبقاء قوائم الحديث/الأحكام/الدروس عند فشل إعادة الجلب.
3. توسيع جسر البطاقات لـ kids/sw/cal.

## دفعة P11 — 2026-09-11

1. ترحيل واسع للهكس العارية في `styles/pages` إلى رموز `--mj-*` (~190 موضعًا عبر 56 ملفًا).
2. إضافة `--mj-brand-tint` / `--mj-warning` / `--mj-danger` في هوية بصرية موحّدة.
3. ScreenShell لصفحات views إضافية (طهارة، معجزات، سيرة، أسماء، توبة، توحيد، آداب، فضائل، رقائق، شمائل، صحابة، تاريخ، سنن يومية).

## دفعة P12 — 2026-09-11

1. ScreenShell (`UtilityScreen compose="mark"`) لبقية `views/*` عالية الظهور + `DiscoverIslamShell`؛ استثناء مقصود: Admin/Upload/AuthCallback/Submit.
2. صفحات فقه رقيقة (جمعة/أضحية/…) تبقى عبر `FiqhGuidePage` المغلّف مسبقًا.
3. ترحيل هكس عالية التكرار في `styles/pages` إلى `--mj-*`؛ إضافة `--mj-white` / `--mj-info` للأسطح البيضاء الثابتة عبر الثيمات.
4. متبقٍ متعمّد: هكس متخصصة/تدرجات دقيقة + مراجعة جهاز؛ لا حذف لطبقات CSS القديمة قبل ترحيل 100٪.
5. إصلاح تباين on-brand (fallbacks ليلي/نهاري) وتثبيت `SURFACE_WITHOUT_COLOR_BASELINE=50`.

## دفعة P13 — 2026-09-11

1. ترحيل إضافي لهكس عالية التكرار في `styles/pages` إلى `--mj-*` مع `--mj-white` آمن (لا `--mj-on-brand` للخلفيات).
2. إصلاح fallbacks تباين ليلي/نهاري بعد الترحيل؛ الإعدادات منظمة مسبقًا عبر `UtilityScreen` + `LegalSection`.
3. توسيع جسر البطاقات إن لزم؛ متبقٍ: هكس متخصصة + مراجعة جهاز بعد الدمج.
4. إصلاح تباين `/quran-hub/numbers` الليلي: لا تستخدم `--mj-ink` كـ fallback لخلفية `--mj-surface`.

## دفعة P14 — 2026-09-11

1. إبقاء محتوى التفاصيل أثناء إعادة الجلب: `if (loading && !entity)` بدل مسح الصفحة عند وجود بيانات سابقة (درس، حكم، مجلس فقه، مسار تعلّم، …).
2. لا دفع حتى يخضر CI الحالي لـ #1894 (إصلاح تباين أرقام القرآن).
3. إصلاح إرجاعات Skeleton في JSX (كانت تعديلات P14 الأولى تمسّ useEffect فقط).
4. ترحيل هكس إضافية آمنة في styles/pages.

## دفعة P15 — 2026-09-11

1. توسيع جسر البطاقات: hadith/lesson-unified/fiqh/prophet/home-daily/tarikh/lib/lpp…
2. ترحيل هكس slate/info/success إضافية في styles/pages.

## دفعة P16 — 2026-09-11

1. توسيع رموز \`--mj-warning-soft\` / \`--mj-danger-soft\`.
2. ترحيل هكس تحذير/خطر/معلومات/أسطح إضافية في \`styles/pages\`.
3. دفعة إضافية ضمن P16: ترحيل رمادي/أخضر/تحذير شائع دون المساس بتدرج مواقيت الصلاة المقدّس.

## دفعة P17 — 2026-09-11

1. إصلاح تباين \`related-knowledge\`: لا \`--mj-brand-soft\` كلون نص ليلي.
2. ترحيل هكس أسطح/حدود/حبر شائعة وآمنة في \`styles/pages\` (بدون مواقيت الصلاة، وبدون إعادة استخدام soft/info/ink خارج دورها).
3. P15 (#1896) وP16 (#1897) منشوران على الإنتاج (\`f41167aa\` ثم \`189cbed3\`).

## دفعة P18 — 2026-09-11

1. ترحيل حدود/أسطح خضراء شائعة في صفحات التعلم/الأدب/الطهارة/أركان الإسلام/المنهج/التقويم إلى \`--mj-hairline\` / \`--mj-brand-soft\` / color-mix.
2. بدون مواقيت الصلاة وبدون نصوص soft كحبر ليلي.
3. منشور على الإنتاج عبر #1903 (\`703e590c\`).

## دفعة P19 — 2026-09-11

1. تقليص \`eslint-ds-legacy-allowlist.json\` من 27 → 2 (متبقي: Mutashabihat + ProphetsFamilyTree).
2. استبدال fontSize/ألوان حرفية و\`text-white\`/\`bg-white\` برموز \`--ss-type-*\` / \`--mj-*\` في ~25 شاشة.
3. تغطية DS ≈ 99.5٪ (383/385) · دين allowlist = 2.
4. #1902 (وميض الجلسة) منشور على الإنتاج (\`917b58e3\`).

