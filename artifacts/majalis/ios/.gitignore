App/build
App/Pods
App/output
DerivedData
xcuserdata

# Cordova plugins for Capacitor (regenerated by `cap sync`)
capacitor-cordova-ios-plugins

# محتوى الويب بعد المزامنة (قد يحتوي مفاتيح Vite المضمّنة) — لا يُرفع إلى git
App/App/public/**
!App/App/public/.gitkeep

# تُتتبَّع capacitor.config.json و config.xml حتى لا تظهر مراجع حمراء في Xcode
