-- تنظيف `qa_questions` — حذف صفّ الاستيراد التجريبي **الثاني** (ج-٢٥١).
--
-- الصفّ: f9479710-a314-4086-8437-21cf26331f6a
--   question : «هل يجوز الجمع بين الصلاتين للمسافر في Phase2؟»
--   reference: «اختبار استيراد Phase2»   ⇐ مرجع تجريبي صريح، لا مصدر شرعيًّا
--   status='published' و review_status='approved' و verification_status='needs_review'
--
-- ⚠️ **ليس هو الصفَّ الذي عالجته ج-٢٢٨**: ذاك معرِّفُه 84c79f0e-ac9e-4d50-9ed1-41374ab8fe82
-- وقد حُذف فعلًا (نسخته في `data/qa-questions-deleted-2026-07-26-backup.json`)، وهذا صفٌّ
-- **بنفس السؤال والمرجع حرفًا بحرف ومعرِّفٍ جديد** ⇒ أُعيد استيرادُه بعد الحذف. فحصٌ شاملٌ
-- في هذه الدورة (`WHERE reference LIKE '%Phase2%' OR question LIKE '%Phase2%'`) أرجع
-- **صفًّا واحدًا فقط** هو هذا، فلا ثالثَ لهما.
--
-- لماذا يُحذف ولا يُصحَّح: هو **سجلّ اختبار استيراد** لا محتوى، وسؤالُه نفسه مكتوب بلاحقة
-- «في Phase2»، فتصحيحه = كتابة سؤال وجواب دينيَّين جديدين من عندنا وهو خارج نطاق التدقيق
-- (نقل لا توليد). وهذا نفس الصنف الذي حُسم بالحذف في ج-٢٢٥ في `library_items`
-- («كتاب تجريبي Phase2») وفي ج-٢٢٨ هنا، بنفس القاعدة.
--
-- ليس سجلًّا خاملًا: الصفّ **منشور فعلًا** ويظهر للمستخدم في صفحة الأسئلة والأجوبة
-- (`getQaQuestions` في `src/lib/supabase.ts` تجلب بشرط `status='published'`).
--
-- فحص المراجع قبل الحذف: `dawah_questions.related_question_ids` ⇒ **صفر إشارة** إلى هذا
-- المعرِّف. نسخة احتياطية بكل الأعمدة الثمانية عشر:
-- `artifacts/majalis/data/qa-questions-deleted-2026-07-26-backup-2.json`.
--
-- idempotent: مقيَّد بالمعرِّف وبنصّ `reference` الأصلي.
BEGIN;

DELETE FROM qa_questions
 WHERE id = 'f9479710-a314-4086-8437-21cf26331f6a'
   AND reference = 'اختبار استيراد Phase2';

COMMIT;
