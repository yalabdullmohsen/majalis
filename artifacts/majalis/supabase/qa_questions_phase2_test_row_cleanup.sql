-- تنظيف `qa_questions` — حذف صفّ الاستيراد التجريبي الوحيد في الجدول (ج-٢٢٨).
--
-- الصفّ: 84c79f0e-ac9e-4d50-9ed1-41374ab8fe82
--   question : «هل يجوز الجمع بين الصلاتين للمسافر في Phase2؟»
--   reference: «اختبار استيراد Phase2»   ⇐ مرجع تجريبي صريح، لا مصدر شرعيًّا
--   status='published' و review_status='approved' و verification_status='needs_review'
--
-- لماذا يُحذف ولا يُصحَّح: هو **سجلّ اختبار استيراد** لا محتوى، وسؤاله نفسه مكتوب بلاحقة
-- «في Phase2»، فتصحيحه = كتابة سؤال وجواب دينيَّين جديدين من عندنا وهو خارج نطاق التدقيق
-- (نقل لا توليد). وهذا **نفس الصنف** الذي حُسم بالحذف في ج-٢٢٥ في `library_items`
-- («كتاب تجريبي Phase2») بنفس القاعدة.
--
-- ليس سجلًّا خاملًا: الصفّ **منشور فعلًا** ويظهر للمستخدم في صفحة الأسئلة والأجوبة
-- (`getQaQuestions` في `src/lib/supabase.ts` تجلب بشرط `status='published'`).
--
-- فحص المراجع قبل الحذف: `dawah_questions.related_question_ids` (٨٥ صفًّا) ⇒ **صفر إشارة**
-- إلى هذا المعرِّف، ولا جدول آخر يشير إلى `qa_questions` بمفتاح أجنبي في الواجهة.
-- نسخة احتياطية كاملة بكل الأعمدة: `artifacts/majalis/data/qa-questions-deleted-2026-07-26-backup.json`.
--
-- idempotent: مقيَّد بالمعرِّف وبنصّ `reference` الأصلي.
--
-- ⚠️ لم يُطبَّق على القاعدة الحيّة في دورة إنشائه (عائق مفتاح الخدمة نفسه) — يُطبَّق يدويًّا.
BEGIN;

DELETE FROM qa_questions
 WHERE id = '84c79f0e-ac9e-4d50-9ed1-41374ab8fe82'
   AND reference = 'اختبار استيراد Phase2';

COMMIT;
