-- تنظيف `qa_questions` — حذف صفّ الاستيراد التجريبي **الثالث**، مع إصلاح سببه
-- الجذريّ في `vercel.json` (ج-٢٥٨).
--
-- الصفّ: 0d4e8319-9bbb-446b-99fa-9e37eeca7f7e
--   question : «هل يجوز الجمع بين الصلاتين للمسافر في Phase2؟»
--   reference: «اختبار استيراد Phase2»   ⇐ مرجع تجريبي صريح، لا مصدر شرعيًّا
--   evidence = NULL، و status='published' و review_status='approved'
--   created_at = 2026-07-27 00:00:42+00 ⇒ أُدرج **اليوم** لا قديمًا.
--
-- ⚠️ **وهو الثالث بنفس السؤال والمرجع حرفًا بحرف ومعرِّفٍ جديد**: حذفت ج-٢٢٨
-- المعرِّف 84c79f0e-…، وحذفت ج-٢٥١ المعرِّف f9479710-… وقيَّدت أن «مصدر الاستيراد
-- نفسَه ما زال يحمل الصفَّ التجريبيّ فقد يعود ثالثة». وقد عاد.
--
-- 🔑 **السببُ الجذريّ — وُجد وأُصلح في هذه الدورة**: `vercel.json` كان يجدول
-- `/api/cron/import-phase2-trial` كلَّ ستّ ساعات (`0 */6 * * *`)، وهو ينفّذ
-- `runPhase2TrialImport` فيستورد **قوالبَ التجربة** الأربعة إلى الجداول الحيّة:
--   data/imports/trial/questions.phase2.json ⇒ هذا الصفّ في `qa_questions`
--   data/imports/trial/books.phase2.json     ⇒ «كتاب تجريبي Phase2» (حذفته ج-٢٢٥)
--   data/imports/trial/sheikhs.phase2.json   ⇒ «الشيخ التجريبي Phase2» (حُذف مرّتين)
-- فكان كلُّ حذفٍ سابقٍ ترقيعًا يُبطله أوّلُ تشغيلٍ للجدولة. حُذفت الجدولةُ من
-- `vercel.json` وبقي المسارُ والأمرُ اليدويّ (`pnpm run import:phase2-trial`)
-- كما هما لتجربة الاستيراد عند الطلب — أي مُنع النشرُ الدوريّ لا أداةُ التجربة.
--
-- لماذا يُحذف ولا يُصحَّح: هو **سجلّ اختبار استيراد** لا محتوى، وسؤالُه نفسه
-- مكتوب بلاحقة «في Phase2»، فتصحيحه = كتابة سؤال وجواب دينيَّين جديدين من عندنا
-- وهو خارج نطاق التدقيق (نقل لا توليد).
--
-- ليس سجلًّا خاملًا: الصفّ **منشور فعلًا** ويظهر للمستخدم في صفحة الأسئلة
-- والأجوبة (`getQaQuestions` تجلب بشرط `status='published'`).
--
-- فحص المراجع قبل الحذف: `dawah_questions.related_question_ids` ⇒ **صفر إشارة**
-- إلى هذا المعرِّف. نسخة احتياطية بكل الأعمدة الثمانية عشر:
-- `artifacts/majalis/data/qa-questions-deleted-2026-07-27-backup-3.json`.
--
-- idempotent: مقيَّد بالمعرِّف وبنصّ `reference` الأصلي.
BEGIN;

DELETE FROM qa_questions
 WHERE id = '0d4e8319-9bbb-446b-99fa-9e37eeca7f7e'
   AND reference = 'اختبار استيراد Phase2';

COMMIT;
