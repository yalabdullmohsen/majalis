-- تدقيق `library_items` (٢٠٢٦-٠٧-٢٦، ج-٢٢٥) — حذف الصفوف الخمسة كلها: أربعة تنسب
-- أحكامًا شرعية إلى «المجمع الفقهي» بلا أي مستند، وخامس سجلّ اختبار تقنيّ ظاهر للعموم.
--
-- ═══ لماذا هذه الصفوف ظاهرة للمستخدم فعلًا (لا سجلات إدارية خاملة) ═══
--   • `src/lib/supabase.ts::getLibraryItemById` (سطر 553) يستعلم `library_items`
--     بـ`.eq('status','approved')` ويعرض الصفّ في `/library/:id`، وكلّها `approved`.
--   • `src/lib/learning-plan-service.ts` (سطر 252) يسحب `library_items` المعتمَدة
--     بحسب التصنيف لتوليد خطة تعلّم المستخدم؛ والتصنيف «فقه» مُدرَج في
--     `INTEREST_TO_DB_CATEGORIES.fiqh` — أي أن **سجلّ الاختبار** المصنَّف «فقه»
--     مؤهَّل للظهور داخل خطة تعلّم مستخدم حقيقي.
--
-- ═══ سبب الحذف، صفًّا صفًّا (المعاينة الكاملة في ملف النسخة الاحتياطية) ═══
--   (١) «حكم التعامل بالعملات الرقمية (المشفّرة)» — description: «بيان من المجمع الفقهي…».
--   (٢) «حكم التبرع بالأعضاء بعد الوفاة» — description: «توصية من المجمع الفقهي…».
--   (٣) «زكاة الأسهم والصناديق الاستثمارية» — description: «قرار من المجمع الفقهي…».
--   (٤) «زكاة الأسهم والصناديق الاستثمارية - قرار المجمع الفقهي» — **نسخة مكرَّرة من (٣)**
--       بنفس التصنيف ونفس الموضوع (فارقهما إعادة صياغة الوصف فقط).
--   الأربعة تنسب حكمًا شرعيًّا إلى جهة إفتاء حقيقية **بلا رقم قرار ولا دورة ولا تاريخ**،
--   و`external_url` في كلٍّ منها هو الصفحة الرئيسية العامّة `https://www.islamweb.net/fatwa/`
--   لا وثيقة القرار — أي لا يمكن لقارئ التحقُّق من نسبة الحكم إطلاقًا، ولا نصّ منقولًا
--   حرفيًّا فيها أصلًا (الوصف إعادة صياغة). و`verification_status='needs_review'` في
--   الخمسة جميعًا رغم أن `status='approved'`، أي أنها نُشرت قبل أي تحقُّق.
--   وهذا **نفس الصنف** المحسوم في نفس اليوم بـ
--   `fiqh_council_items_unsourced_attribution_cleanup.sql` (حُذف فيه ثلاثون صفًّا
--   تنسب قرارات إلى مجامع بأرقام دورات وتواريخ لا مستند لها) — فتُعامَل بنفس القاعدة.
--   لا تُعاد كتابتها بمصادر صحيحة في هذه الدورة: ذلك توليد محتوى ديني جديد لا تدقيق نقل.
--   (٥) «كتاب تجريبي Phase2 — مختصر الحكم» — سجلّ اختبار استيراد صريح
--       (description: «سجل تجريبي لاختبار استيراد المكتبة»)، و`external_url` يشير إلى
--       `/library` بالموقع نفسه. مؤجَّل تنظيفه منذ جرد المرحلة 1 (راجع CONTINUATION_PLAN.md).
--
-- ═══ لا أثر جانبيًّا للحذف ═══
--   • فُحصت المراجع الخارجية للمعرّفات الخمسة: `course_books.library_item_id` = **صفر**
--     إشارة (بل الجدول كله لا يحمل أي `library_item_id` غير فارغ)، ولا جدول آخر يشير إليها.
--   • «المكتبة» المعروضة للمستخدم فعليًّا هي الفهرس الثابت `src/lib/library-catalog.ts`
--     (موثَّق في تعليق `getLibraryItemById` وفي `LearningPathDetailPage.tsx` سطر 41:
--     «library_items شبه الفارغ في DB، لا للفهرس الثابت الفعلي»)، وسقوط الاستعلام
--     إلى الفهرس الثابت مُبرمَج أصلًا في نفس الدالّة ⇒ الحذف لا يُفرِّغ صفحة المكتبة.
--
-- نسخة كاملة من الصفوف المحذوفة (كل الأعمدة) محفوظة في:
--   artifacts/majalis/data/library-items-deleted-2026-07-26-backup.json
--
-- idempotent: الحذف بالمعرِّفات، فإعادة التشغيل بلا أثر.
--
-- ⚠️ لم يُطبَّق على القاعدة الحيّة في دورة إنشائه: قناة الكتابة مقطوعة
--    (`.env.local` فيه `VITE_SUPABASE_ANON_KEY` فقط — يقرأ ولا يكتب، وRLS تمنع الحذف
--    للدور المجهول، ولا `SUPABASE_SERVICE_ROLE_KEY` ولا psql/supabase CLI).
--    يُطبَّق يدويًّا في SQL Editor.
BEGIN;

DELETE FROM library_items WHERE id IN (
  '906a57b0-47b9-422e-9d65-2438a8e019ac', -- حكم التعامل بالعملات الرقمية (المشفّرة)
  'eebb2046-4bcb-4161-b758-ee5ad3525fa6', -- حكم التبرع بالأعضاء بعد الوفاة
  '7e0a7dbe-2250-4180-a614-57e555ef1a1a', -- زكاة الأسهم والصناديق الاستثمارية
  'b3639436-9cdb-4c9d-93dd-bd5b9aa9720d', -- زكاة الأسهم والصناديق الاستثمارية - قرار المجمع الفقهي (مكرَّر)
  'eefebb28-e1d8-4bb6-8d01-82946dcb98ce'  -- كتاب تجريبي Phase2 — مختصر الحكم (سجلّ اختبار)
);

COMMIT;

-- ═══ تحقُّق بعد التطبيق ═══
-- SELECT count(*) FROM library_items;  -- المتوقَّع: 0 (والمكتبة المعروضة تبقى من الفهرس الثابت)
