/**
 * Root landing — HomeDashboard (Quran reading entry).
 * Classic platform home lives at `/platform`.
 */
import { HomeDashboard } from "@/components/HomeDashboard";

export default function HomeDashboardPage() {
  return <HomeDashboard />;
}
