/**
 * جذر التطبيق المستهدف بعد ترحيل App.tsx (حزم C/F).
 * التشغيل الحالي يبقى عبر `src/App.tsx` و`main.tsx`.
 */
export { AppProviders } from "@/app/providers";
export { ROUTE_REGISTRY } from "@/app/router";
