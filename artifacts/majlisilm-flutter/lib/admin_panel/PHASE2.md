# Phase 2 — Admin Panel

This directory is reserved for:

- `AdminReviewController`
- Review & Moderation Hub (waveform, diffs, batch actions)
- Analytics shell + content management

**Do not implement until the owner types: Continue Phase 2**

Shared models live in `package:majlisilm_flutter/shared/...` and will be imported by admin code in Phase 2.
