# تسليم فني — لنافذة automation/tasks

> كل ما هنا اكتُشف من نافذة automation/content ولا يجوز إصلاحه من هذه النافذة
> لأنه يتطلب تعديل بنية التطبيق/سكربتات البناء لا بيانات المحتوى فقط.

---

## -1. قسم جديد جاهز للمحتوى — يحتاج سطرين فقط في App.tsx (لا محتوى إضافي مطلوب)

بطلب من المالك ("أنشئ وأكمل وافعل ما هو لمصلحة العمل")، سُدَّت فجوة تغطية `b037` (لا قسم للغة العربية إطلاقاً) من نافذة المحتوى بقدر ما يسمح نطاقها:

- **المحتوى جاهز بالكامل**: `src/lib/arabic-language-data.ts` — 7 أبواب (النحو الأساسي، النحو المتقدم، الصرف، علم المعاني، علم البيان، علم البديع، اللغة وخدمة القرآن والحديث)، 74 عنوان درس، بنفس نمط `DarsSection` المستخدم في `tarikh-islami-data.ts`/`quran-studies-data.ts` (عناوين مناهج قياسية مستقرة، لا مسائل خلافية).
- **صفحة العرض جاهزة**: `src/views/ArabicLanguagePage.tsx` — مطابقة حرفياً لنمط `TarikhIslamiPage.tsx`/`QuranStudiesPage.tsx` (تستخدم `SectionAccordionLayout` الموجود، لا مكوّن جديد).
- **المتبقي فقط** (تعمّدت عدم لمس `App.tsx` من نافذة المحتوى تجنباً لتعارض مع عمل هذه النافذة، ولأن التوجيه بنية تطبيق لا محتوى):
  1. في قسم الاستيراد الكسول: `const ArabicLanguagePage = lazy(() => import("@/views/ArabicLanguagePage"));` (بجانب سطر `TarikhIslamiPage` الموجود، السطر ~140).
  2. في قسم المسارات: `<Route path="/arabic-language"><SafeLazyRoute component={ArabicLanguagePage} /></Route>` (بجانب مسار `/tarikh-islami` الموجود، السطر ~654).
  3. اختياري: رابط تنقّل مناسب + سطر في `src/lib/masarat-data.ts` ضمن مسار تعليمي مناسب (مثل مسار "التأسيس" أو مسار متقدم لطالب علم).
  4. اختياري: إضافة المسار الجديد إلى `seo-routes.json` عبر `generate-seo.mjs` ليُفهرس بعنوان ووصف خاصين (نمط `/tarikh-islami` نفسه).

---

## 0. متبقٍ من إلغاء مصدر IslamWeb (بطلب مباشر من المالك، 2026-07-24)

نُفِّذت إزالة شاملة لكل مرجع IslamWeb من الكود والبيانات القابلة للتعديل من نافذة المحتوى (راجع `backlog.json` → `b033` للتفاصيل الكاملة). **عمداً لم تُمسّ** الملفات التالية لأنها خارج صلاحية نافذة المحتوى:

- **`supabase/*.sql`** (كل من `artifacts/majalis/supabase/` وجذر المستودع `supabase/`) — عشرات ملفات SQL تحتوي مراجع IslamWeb ضمن بيانات seed/migration فعلية. تعديل الملف لا يُلغي بيانات سبق تطبيقها فعلياً على قاعدة بيانات الإنتاج؛ يحتاج قراراً من المالك: هل يُراد فقط منع الاستخدام المستقبلي (كافٍ ما نُفِّذ في الكود) أم حذف بيانات فعلية مستوردة سابقاً من قاعدة الإنتاج (يتطلب migration جديد ينفَّذه automation/tasks بحذر شديد، خارج نطاق تعديل نصي).
- **`data/needs-post-review.jsonl`** و**`data/global-reference-report.json`** — سجلّان تاريخيان (audit log وreport مولَّد) يوثّقان استخدام IslamWeb كأداة تحقق فعلية في دورات تدقيق سابقة. تعديلهما تزوير لسجل تاريخي حقيقي، وليس إزالة مصدر حي — تُركا كما هما عمداً.

---

## 1. تصحيح تشخيص سابق + الفجوة الحقيقية المؤكدة: `platform-seed.snapshot.json` مجمَّد (خطورة: عالية، وليست حرجة كما ظُنَّ أولاً)

> **تنبيه مهم:** القسم أدناه يُصحِّح تشخيصاً أولياً خاطئاً كُتب هنا سابقاً في نفس الجلسة بناءً على قراءة جزئية للكود (فحص `seo-routes.json` وحده بلا تتبّع بقية سلسلة البناء). بعد تشغيل `pnpm run build` كاملاً وفحص `dist/` الفعلي، اتضح أن التشخيص الأول غير دقيق. التصحيح موثَّق بدل الحذف حتى لا يتكرر نفس الخطأ المنهجي (الاكتفاء بقراءة ملف واحد دون تتبّع سلسلة التوليد كاملة).

**ما تبيَّن فعلياً بالفحص المباشر لـ`dist/` بعد بناء كامل ناجح (2026-07-24):**

- `/library/*`: **مغطّى بالكامل فعلياً** — تحقّقت من `dist/library/book-hisn-al-muslim/index.html` مباشرة: العنوان الحقيقي **"حصن المسلم من أذكار الكتاب والسنة | المجلس العلمي"**، ليس الـfallback العام "كتاب شرعي". لا حاجة لأي إصلاح هنا — البند الرابع الأصلي من التكليف **غير قابل لإعادة الإنتاج حالياً**.
- `/scholars/*`: مغطّى بالكامل (99/99)، وكذلك `/library/*` (133/133) — يُولَّدان مباشرة داخل `scripts/generate-seo.mjs` من `LIBRARY_CATALOG` و`SCHOLARS` الحيّين إلى مجلد `seo-prerender/`، ثم يُدمَجان في `dist/` بواسطة `scripts/post-build-seo.mjs` (لوحظ في سجل البناء: "دُمج 736 ملف prerender"، و"كل سجل حي (عالِم/كتاب) له prerender مطابق"). هذا مسار **منفصل تماماً** عن `seo-routes.json`/`prerender.mjs` الذي فحصته سابقاً بمعزل عن الصورة الكاملة.

**الفجوة الحقيقية المؤكدة (لا تزال قائمة):** توليد الصفحات الفردية لبعض الأقسام في `generate-seo.mjs` (الأسطر ~660-690) يعتمد على `PLATFORM_SEED` المقروء من ملف ثابت مجمَّد **`scripts/platform-seed.snapshot.json`** — وليس من مصادر البيانات الحيّة. عدد العناصر في هذا الملف المجمَّد صغير جداً مقارنة بالحيّ:

| المفتاح في snapshot | العدد في الملف المجمَّد | العدد الحيّ الفعلي | المصدر الحيّ |
|---|---|---|---|
| `rulings` | **6** | **533** | `src/lib/rulings-encyclopedia-seed.generated.ts` (`RULINGS_ENCYCLOPEDIA_TOTAL`) |
| `fiqh_decisions` | 6 | غير محسوب (على الأرجح أكبر — راجع `fiqh-council-seed.ts`) | `src/lib/fiqh-council-seed.ts` |
| `courses` | 4 | غير محسوب | `src/lib/annual-courses-seed.ts` |
| `fiqh_sessions` | 2 | 12 | `src/data/fiqh-sessions-list.json` |
| `qa_items` | 8 | 360 | `src/lib/qa-seed.ts` |
| `fatwas` | 5 | — | مسار `/fatwa` مُحوَّل عمداً إلى `/fiqh-council/fatwas` (ليس بقايا ميتة — تعليق صريح في الكود بذلك) |

**التحقق العملي:** `dist/rulings/` يحوي **7 مجلدات فقط** (index + 6 أحكام فردية) من أصل 533 حكماً في الموسوعة الحيّة. هذا **هو** السبب الجذري الحقيقي للبند 4-9 الأصلي في التكليف: أي حكم من الـ527 غير المُدرَجة في الـsnapshot المجمَّد لن يحصل على `dist/rulings/{id}/index.html` مخصَّص وقت البناء، فيسقط للزواحف على أي fallback عام يخدمه التوجيه الثابت لتلك المسارات غير الموجودة في `dist/` (على الأرجح صدفة `dist/index.html` نفسها حسب إعداد Vercel rewrites، وهو ما يطابق ملاحظة "تظهر محتوى الصفحة الرئيسية" في التكليف الأصلي حرفياً).

**المطلوب من automation/tasks:** إنشاء سكربت توليد/تحديث لـ`scripts/platform-seed.snapshot.json` يقرأ من المصادر الحيّة الست أعلاه (بدل تجميده يدوياً)، أو تعديل `generate-seo.mjs` ليقرأ مباشرة من المصادر الحيّة بدل الوسيط المجمَّد. **الأولوية القصوى: `rulings`** (6 من 533 فقط — أكبر فجوة نسبية وأعلى أثر SEO).

---

## 2. تعارض كاتبَين على `src/data/library-catalog.json` (خطورة: عالية)

اكتُشف أثناء محاولة تشغيل `sync-seo-data.ts` لمزامنة `scholars-seo.json` (البند 4 من backlog.json — b004). السكربت شغّال بلا تحذير لكنه **يكتب أيضاً** إلى `artifacts/majalis/src/data/library-catalog.json` بمخطط مبسّط جداً (`id, title, author, category, description` فقط) — **يحذف** الحقول `slug, canonicalTitle, normalizedTitle, authorId, editions, verificationStatus, sources, reviewedBy, reviewedAt` التي يعتمد عليها `scripts/test-library-integrity.mjs` وسكربت آخر مخصص:

`scripts/regen-library-catalog-json.mjs` — يكتب لنفس الملف **بالمخطط الكامل الصحيح**، ويحافظ على الإثراء القائم (authorId/editions/verificationStatus) للكتب الموجودة مسبقاً.

**ما حدث فعلياً هذه الجلسة:** شغّلت `sync-seo-data.ts` (لغرض آخر تماماً — تحديث `scholars-seo.json`)، فولّد نسخة مبسّطة أفقدت 1463 سطراً من الإثراء في `library-catalog.json`. **رُصد الأمر وأُرجِع فوراً عبر `git checkout`** قبل أي commit — لم يُدفع أي فقدان بيانات. لكن هذا يعني: **أي تشغيل مستقبلي لـ`sync-seo-data.ts` (يدوياً أو ضمن سلسلة بناء) سيُتلف `library-catalog.json` بصمت** ما لم يُشغَّل `regen-library-catalog-json.mjs` بعده مباشرة لإصلاح الضرر — وهذا ترقيع لا حل.

**المطلوب من automation/tasks:** حسم أي السكربتين هو "المصدر" لهذا الملف (الأرجح: `regen-library-catalog-json.mjs` لأنه يحافظ على الإثراء المُستهلَك في `test-library-integrity.mjs`)، ثم إما:
- تعديل `sync-seo-data.ts` ليتوقف عن الكتابة لهذا الملف أصلاً (وينقل مسؤوليته لملف مرآة SEO منفصل)، أو
- توحيد المخطط بين السكربتين فعلياً.

---

## 3. للمرجعية: نتائج فحوصات محلية سريعة (لا تتطلب فعلاً تقنياً، محتوى فقط)

- `node scripts/audit-data-quality.mjs` → 4 تكرارات في Adhkar (راجع `data/data-quality-audit.json`) — عنصر backlog b008، سيُعالَج من نافذة المحتوى.
- `node scripts/test-identity.mjs` → ناجح، لا مشكلة هوية.
- `npx tsx scripts/generate-content-counts.ts` → الأعداد مشتقة آلياً بالفعل (books:133, scholars:99, fawaid:496, quizQuestions:978, mindMaps:23) — بند التكليف 4-3 (الأرقام الثابتة) **مُنفَّذ مسبقاً بالكامل**، لا حاجة لعمل إضافي.
