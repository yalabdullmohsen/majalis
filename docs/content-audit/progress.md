# تقدّم تدقيق المحتوى — Majlisilm.com

> اقرأ هذا الملف أولاً عند أي استئناف بعد /clear. لا تُعِد الجرد من الصفر.

## آخر عنصر مكتمل
`b015` — فحص برمجي شامل لجودة البيانات (Placeholder/TODO/Lorem/حقول فارغة) عبر المكتبة والمشايخ والأحكام والمجمع الفقهي والمسائل المعاصرة: **نظيف بالكامل**، لا حاجة لعمل. اكتُشف أثناءه `b014`: 18 من 63 دورة علمية سنوية بلا حقل `body` تفصيلي (بيانات لوجستية حقيقية — مشايخ/تواريخ/مساجد — لا يجوز اختلاقها، يحتاج مصدراً من الإدارة).

## الدفعتان المُنجَزتان (فرع automation/content، مدفوعتان بالكامل)
**الدفعة 1** (commit `b089830d` + `308e1fd1`):
1. **b001** — تحقّق فقط: البريد الرسمي موحّد بالفعل، لا حاجة لتعديل.
2. **b002+b003** — تصحيح صفحتَي **اليسع** و**ذو الكفل** (إزالة الجزم بالخلافة/النبوة/المكان، `linkNote` في شجرة النسب).
3. **b004** — تصحيح «تُهفة النظار»→«تحفة النظار» + مزامنة `scholars-seo.json` (كان متجمداً، فتحسّنت 99 بيوغرافيا).
4. تصحيح تشخيص فني كان خاطئاً جزئياً (راجع `technical-handoff.md` §1): `/library/*` و`/scholars/*` **مغطّيان بالكامل** فعلياً في `dist/` (تحقّق مباشر من عنوان صفحة حصن المسلم) — الفجوة الحقيقية محصورة في `platform-seed.snapshot.json` المجمَّد (6/533 حكماً فقط له صفحة فردية).
5. اكتُشف تعارض كاتبَين حقيقي على `library-catalog.json` بين `sync-seo-data.ts` و`regen-library-catalog-json.mjs` — رُصد قبل أي commit وأُرجِع، موثّق كـ`b006`.

**الدفعة 2** (commit قيد الدفع الآن):
- **b008** — دُمج تكراران حرفيان في `adhkar-seed.ts` (نفس الحديث/الراوي/العدد، فرق صياغة مصدر فقط فقط): adh-322 حُذف ودُمجت كلماته المفتاحية في adh-196، adh-290 حُذف ودُمج في adh-197. 304→302 عنصراً. تُرك زوج ثالث (adh-6 مقابل adh-328، نفس النص لكن راويين مختلفين: البراء بن عازب مقابل حذيفة) **بلا حذف** — سُجِّل كـ`b011` يحتاج تحققاً بمصدر تخريج موثوق قبل أي حسم (التزاماً بقاعدة عدم التخمين في المحتوى الحديثي).
- **b007** — تحقّق آلي: 0/99 بيوغرافيا عالِم أقل من 40 حرفاً. أُغلق بلا حاجة لعمل.

## أول عنصر للبدء منه عند الاستئناف
`b010` — توسيع مصفوفة التغطية لبقية الأقسام (علوم القرآن، التفسير، التجويد، الحديث ومصطلحه، الفقه وأصوله، العقيدة، السيرة، الصحابة، التاريخ الإسلامي، الأخلاق، اللغة العربية...) — لم يبدأ الجرد التفصيلي لهذه الأقسام بعد؛ نطاق ضخم يحتاج تقسيمه لدفعات متتالية حسب بند 10 من التكليف الأصلي (25-50 سجلاً أو قسماً كاملاً لكل دفعة). `b011` و`b014` معلَّقان (needs_source) بانتظار توفر مصدر موثوق — لا يُعاد فتحهما آلياً.

## قيود التنفيذ المتّبعة هذه الجلسة
- Agent واحد فقط، بلا subagents.
- شُغِّل `pnpm run build` كاملاً مرتين (بوابة الجودة الإلزامية في `scripts/commit-and-push-branch.sh`) — يتطلب `PORT=3000 BASE_PATH=/` بيئياً بسبب حزم تسويقية غير مرتبطة (`mockup-sandbox`, `majalis-pitch`, `majalis-promo`) تتطلب PORT إلزامياً حتى للبناء الثابت.
- عند تشغيل `sync-seo-data.ts` أو أي سكربت مزامنة: **راجع `git diff --stat` دائماً قبل الإضافة للـcommit** — ثبت عملياً أنه قد يُنتج ملفات تخص نطاقاً أوسع من المقصود (كتشخيص b006).
- لا دمج ولا push إلى main — فقط `automation/content`.
