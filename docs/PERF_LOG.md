# سجل أداء سُنّة

## منهج القياس (إلزامي من 18 أغسطس 2026)

**SHA الإنتاج المرجعي الآن:** `9954d5c6fdaefb854b648cd0d6a4b2d4ff412563`  
`https://majlisilm.com/version.json` → `builtAt` 2026-08-18T09:47:35Z · `ref` main.

لا تُقارَن أرقام من بناءين مختلفين. SHA مثل `7c91021a` يخص دفعة سابقة **وليس** الإنتاج الحالي.

| الغرض | الأداة |
|---|---|
| إعلان درجة / قبل–بعد رسمي | **PSI** جوال على `https://majlisilm.com/` بعد تأكيد SHA |
| منع انحدار الدمج | **LHCI** في GitHub Actions على بناء `dist` — وظيفة `LHCI home (mobile)` داخل Verify build |
| تشخيص سريع | Lighthouse CLI محلي — يُوسم **«محلي — غير مرجعي»** |

كل جدول أرقام يذكر: الأداة · SHA · التاريخ/الساعة · عدد الجولات.

حماية الفئات الثلاث: إمكانية الوصول وأفضل الممارسات وSEO عند **1.00** — عتبات حمراء في LHCI من اليوم.  
هدف أداء معلَن متوسط المدى: **≈ 90**. أداء 100 طموح بعد قرار معماري (تصدير ثابت + جزر).

القياس المرجعي من PageSpeed Insights (جوال، majlisilm.com). التذبذب بين فحصين متتاليين دليل أن الحزمة الابتدائية على الحافة.

## خط الأساس (قبل هذا العمل)

| المقياس | فحص رابع | فحص خامس | الهدف |
|---|---|---|---|
| الأداء | 58 | 44 | ≥ 95 (واقعي 90–98) |
| إمكانية الوصول | 89 | 91 | 100 |
| أفضل الممارسات | 96 | 96 | 100 |
| SEO | 100 | 100 | 100 |
| FCP | 5.1 ث | 5.3 ث | ≤ 1.8 ث |
| LCP | 10.1 ث | 7.8 ث | ≤ 2.5 ث |
| TBT | 180 مل.ث | 720 مل.ث | ≤ 200 مل.ث |
| CLS | 0.002 | 0.04 | ≤ 0.05 |
| Speed Index | 5.8 ث | 5.5 ث | ≤ 3.4 ث |
| العمل الرئيسي | 2.4 ث | 5.6 ث | ≤ 2.0 ث |
| JS غير مستخدم | 78 KiB | 78 KiB | ≤ 20 KiB |
| CSS غير مستخدم | 48 KiB | 48 KiB | ≤ 10 KiB |
| حظر العرض | 400 مل.ث | 160 مل.ث | 0 |
| مهام طويلة | 5 | 12 | ≤ 2 |

قياس Lighthouse المحلي بوسيط 3 تشغيلات يُسجَّل بعد نشر كل مرحلة على الإنتاج (محاكاة الشبكة من بيئة التطوير لا تطابق PSI). رقم بلا قياس لا يُكتب؛ ما لم يُقَس يُوسم «لم يُقَس».

## تجميد طابور الأداء (2026-08-17)

٩ PRs مفتوحة بعد دمج #1236. لا يُفتح PR أداء/CSS جديد حتى ينزل المفتوح إلى ≤ ٣. الترتيب: #1228 (مدموج) ← إعادة تأسيس #1229 بعد squash ← #1229 ← لوبي الأقسام (#1237 بعد إصلاح التباين) ← البقية.

## جرد سابق لا تخمين

- ملفات CSS غير المستوردة (`elite-2026` ~280KB، `majalis-v2`، `modern-2026`، `patterns`…) حُذفت في #930 — ليست على القرص.
- `cssCodeSplit: true` مفعّل. معظم المسارات `lazy()` أصلًا.
- QPC V2 (604 ملفًا) تُحمَّل عبر `FontFace` في `useQpcPageFont` للصفحة الحالية وجارتيها فقط.
- العائق الأكبر للرسم الأول: Google Fonts في `index.html` + preload خط المصحف على كل الصفحات + تسخين Amiri Quran عند idle عام.

## المرحلة 2 — خطوط الإقلاع (`perf/fonts-boot`)

**ما تغيّر**

- إزالة `fonts.googleapis.com` و`fonts.gstatic.com` من `index.html`.
- تضييق CSP في `vercel.json` مؤجَّل (مسار خطر للدمج التلقائي).
- قالب `generate-seo.mjs` (صفحات prerender) يُحدَّث في دفعة لاحقة حتى لا يُفتح PR بـ ٩٥٤ ملف HTML مولَّد.
- استضافة Amiri / Noto Naskh / Aref Ruqaa / Scheherazade New محليًا (`public/fonts/ui/*.woff2`، SIL OFL) مع تقسيم unicode-range عربي/لاتيني.
- preload واحد: `/fonts/ui/amiri-400-ar.woff2`.
- `preconnect` واحد: أصل Supabase.
- بديل مقاييس `MajlisFallback` في `--font-app` (بدون تغيير أسماء العائلات المستخدمة في الواجهة).
- تسخين خطوط المصحف (`warmStaticQuranicFonts`) انتقل من `main.tsx` إلى `MushafReaderPage` — لا تنزيل Amiri Quran على الرئيسية.
- صفر تغيير مقصود في الألوان أو الأحجام أو بوابات المصحف.

**قياس بعد الدمج — وسيط ٣ تشغيلات Lighthouse CLI 12.6 (جوال، simulate)**

- الأداة: Chrome for Testing 151، `--form-factor=mobile --throttling-method=simulate --only-categories=performance`. ليست PageSpeed Insights — لا تُخلط الدرجات.
- الإنتاج عند القياس: `https://majlisilm.com/version.json` → `5fc324ab` (`builtAt` 2026-08-17T13:58:29Z). يشمل squash #1228 **و** عمل مصحف لاحق (#1230/#1231/#1233/#1235). **ليس عزلًا لـ#1228 وحده.**
- HTML الإنتاج ما زال يحمّل `/assets/supabase-CWJnfID4.js` في الإقلاع (#1229 غير منشور).

| | تشغيل 1 | تشغيل 2 | تشغيل 3 | **الوسيط** |
|---|---|---|---|---|
| الأداء | 55 | 54 | 59 | **55** |
| FCP (ث) | 3.06 | 2.71 | 2.97 | **2.97** |
| LCP (ث) | 7.20 | 8.41 | 8.43 | **8.41** |
| TBT (مل.ث) | 584 | 661 | 455 | **584** |
| CLS | 0.045 | 0.000 | 0.002 | **0.002** |
| Speed Index (ث) | 3.06 | 3.18 | 2.97 | **3.06** |
| JS الابتدائي gz (بايت) | 302457 | 302669 | 302402 | **302457** (~295 KiB) |
| CSS الحرج gz (بايت) | 64105 | 64106 | 64105 | **64105** (~62.6 KiB) |

JS الابتدائي = `index` + `vendor` + `query` + `supabase-CWJnfID4` + `icons` من طلبات الشبكة. CSS الحرج = `index-DjgKpNfg.css` فقط (حاجز العرض الوحيد؛ 85–195 مل.ث). إمكانية الوصول / أفضل الممارسات / SEO: **لم تُقَس** (تشغيل الأداء فقط).

**قرار بعد القراءة**

- وسيط الأداء **55** داخل نطاق خط الأساس PSI 44–58. لا ارتفاع معتبر في الدرجة الكلية.
- LCP الوسيط **8.41 ث** داخل نطاق الأساس 7.8–10.1. فرضية «الخطوط أكبر أثر على LCP» **غير مؤكَّدة** بهذا القياس.
- FCP/SI أقل من أرقام PSI (2.97/3.06 مقابل 5.1–5.3 / 5.5–5.8) لكن اختلاف الأداة يمنع نسبة التحسّن إلى #1228.
- لذلك: لا تفكيك CSS في PR جديد، ولا متابعة خطة CSS كما كُتبت قبل القياس. المكسب المقاس التالي الموجود أصلًا في الطابور: ~55 KiB gz لـ`supabase` ما زالت في الإقلاع → #1229.
- الإبقاء على أسماء `Amiri` / `Noto Naskh Arabic` / `Aref Ruqaa` بدل عائلة `MajlisArabic` الموحّدة حتى لا ينكسر الشكل.

## المرحلة 3/4 جزئيًا — إقلاع أخف (`perf/boot-idle`)

**ما تغيّر**

- `supabase-bootstrap` لم يعد استيرادًا ثابتًا من `main.tsx` (يُحمَّل بعد الرسم). AuthProvider كان ديناميكيًا أصلًا.
- تسجيل Service Worker بعد `load` + `requestIdleCallback`.
- `yieldToMain()` في مسح فهرس البحث غير المتزامن بدل `setTimeout(0)` الأعمى.

**قياس**

- بعد نشر #1229 على الإنتاج: وسيط ٣ تشغيلات — **لم يُقَس** (بانتظار الدمج ثم `version.json` بلا `supabase-*.js` في HTML الإقلاع).

**قرار**

- لم يُؤجَّل `m2030/pages.css` حتى لا يظهر وميض بطاقات الدروس على الرئيسية.
- فهرس البحث الكامل في Web Worker: يُقاس TBT/مهام طويلة في مسار البحث بعد #1229؛ إن بقيت >50 مل.ث فهو ضرورة لا تحسين كمالي. قبل ذلك: **لم يُقَس**.

## فحص سابع (PSI جوال · 2026-08-17 ≈ 19:56) — خط أساس قبل هذه الدفعة

| المقياس | السادس | **السابع** |
|---|---|---|
| الأداء | 61 | **58** |
| LCP | 8.4 ث | **6.3 ث** ✅ |
| TBT | 370 مل.ث | **560 مل.ث** 🔻 |
| JS غير مستخدم | 78 KiB | **77 KiB** (ثابت) |
| a11y / BP | 91 / 96 | **91 / 96** (ثابت ٤ فحوص) |

**إنتاج عند بدء الدفعة:** `version.json` → `4fca70dc` · preconnect في HTML الإنتاج: **1** (supabase فقط) — تحذير «>4» ليس من روابط preconnect ظاهرة في المصدر.

### أكبر ملفات JS في dist (قبل تعديل هذه الدفعة)

| بايت | ملف |
|---|---|
| 1377530 | fawaid-curated-seed |
| 685676 | prophetic-medicine-seed |
| 522403 | index |
| 207452 | supabase |
| 190925 | vendor |
| 72377 | icons |

### ما تغيّر في هذه الدفعة (`fix/a11y-bp-then-perf-tbt-lcp`)

1. **a11y/BP:** `aria-controls` شرطي · تباين `.m2030-band__link` → `--brand-on-white` · إزالة `poster_url` · `strip:sourcemaps`.
2. **TBT/حزمة:** `seo-routes.json` ديناميكي · `seo-nav-labels.json` (~10KB) · دروس/دورات الرئيسية `lazy` · فلتر `modulePreload` · تسخين HomePage بعد أول إطار.
3. **LCP:** `/` بـ`Suspense fallback={null}` · `content-visibility` على شريط الدروس. (تجربة eager كسرت ميزانية 164KiB gz فارتُجعَت).
4. **قياس بعد النشر:** وسيط ٣ تشغيلات — يُسجَّل بعد `version.json` الجديد.

### بوابة انحدار (محلية)

`artifacts/majalis/scripts/perf-baseline.json` + `node scripts/perf-regression-gate.mjs` (تفشل عند انحدار TBT/LCP).

## المرحلة 5 — فصل CSS + دخولية مركّبة + TBT (`fix/perf-tbt-lcp-css-split`)

**ما تغيّر**

1. **CSS غير مستخدم / حظر العرض:** `design-system.css` و`brand-v4-components.css` صارا `void import()` بعد الرسم (بلا حذف). بقيت `index.css` + `final-release.css` + تصحيحات التباين + `m2030/pages.css` + الوضع الليلي في المسار الحرج لتفادي وميض تباين أو بطاقات.
2. **LCP:** الدخولية ترسم الرمز فورًا (`opacity: 1`) بلا `stroke-dashoffset` وبلا `filter: blur`. الخروج فقط `opacity`+`transform`. شريط الماركي يبقى ثابتًا حتى `requestIdleCallback`.
3. **TBT:** `prayer-alert-scheduler` لم يعد استيرادًا ثابتًا من `App.tsx`. حدث الشريط في `prayer-alert-events.ts` حتى لا تسحب `PrayerRespectBanner` جدولة الإشعارات إلى حزمة الإقلاع.

**قياس بعد النشر:** وسيط ٣ تشغيلات — يُسجَّل بعد `version.json` الجديد. الأساس للانحدار يبقى فحص PSI السابع (LCP 6.3 ث / TBT 560 مل.ث).

## المرحلة 6 — صدفة LCP خارج React (`perf/lcp-critical-shell` · #1263)

**ما تغيّر**

- عنوان «سُنّة» في HTML خارج `#root`، يُنقَل بنفس العقدة إلى الهيرو.
- الدخولية تُزال فورًا على الويب (تبقى على Capacitor).
- `resource-prewarm` صار `dns-prefetch` فقط وبعد `load`+10ث.
- `calendar.css` مع صفحة التقويم فقط.

**إنتاج:** `version.json` → `1d9fc335` (`builtAt` 2026-08-18T02:07:35Z).

**قياس محلي Lighthouse CLI 13.4 (جوال، simulate) — وسيط ٣ تشغيلات — ليس PSI**

| | ر1 | ر2 | ر3 | **وسيط** |
|---|---|---|---|---|
| الأداء | 0.40 | 0.49 | 0.47 | **0.47** |
| FCP (ث) | 2.54 | 2.08 | 3.37 | **2.54** |
| LCP (ث) | 7.67 | 7.26 | 7.07 | **7.26** |
| TBT (مل.ث) | 840 | 726 | 565 | **726** |
| CLS | 0.169 | 0.171 | 0.170 | **0.170** |
| SI (ث) | 5.86 | 2.83 | 3.37 | **3.37** |

**عنصر LCP (ر1):** `p.hsh-step__desc` — «دروس علمية أسبوعية من علماء الكويت…». تفكيك: TTFB 410مل.ث · تأخير رسم العنصر 3798مل.ث · بلا تحميل مورد. التصنيف: (أ) النص ليس في HTML الأولي ويُرسم بعد Home JS، ومساحته أكبر من العنوان الصغير. أُصلح في #1264 بتكبير العنوان و`content-visibility` على الخطوات.

**a11y / BP / SEO (ر1):** 0.96 / 1.00 / 1.00. الحركة غير المركّبة: `prayer-chip-pulse` عبر `box-shadow` (أُصلح في #1264).

**ملاحظة:** أرقام CLI المحلية أسوأ من PSI الفحص الثامن (أداء 67 / LCP 8.6 / TBT 270 / CLS 0.002). لا تُخلط الأداتان. PSI بعد #1263 **لم يُقَس** (حصة API مستنفدة).

## المرحلة 7 — TBT منشور (`perf/tbt-split-worker` · #1264)

**إنتاج:** `version.json` → `d1e7844` (`builtAt` 2026-08-18T06:07:08Z).

**قياس وسيط ٣ تشغيلات بعد هذا الـcommit:** يُسجَّل مع قياس الدفعة التالية (تباين + CSS) على الإنتاج نفسه بعد الدمج — لا رقم بلا قياس.

## المرحلة 8 — a11y تباين 96→100 (`a11y/contrast-100`)

**سبب 96 (Lighthouse ر1 بعد #1263):** `color-contrast` فقط.

| العنصر | المقاس | المطلوب | الألوان |
|---|---:|---:|---|
| `.home-section-link` | 4.49:1 | 4.5:1 | `#1F7A5A` على `#E4F0EA` |
| `.lesson-unified-card__status` | 4.00:1 | 4.5:1 | `#1F7A5A` على `#D8E3E0` |

**ما تغيّر (رموز لا ترقيع هكس)**

- `--text-brand` و`--msk-gold` النهاريان → `--brand-on-white` (`#146C4E`) — 5.46:1 على `#E4F0EA`.
- زوج CONTRAST_PAIRS: `--brand-on-white` على `#E4F0EA`.
- شارة الحالة → `--chip-fg` / `--chip-bg` (8.74:1).
- `--mj-muted` الليلي يبقى `#C5D0CB`. ملء `--mj-brand` `#1F7A5A` لم يُغمَّق.

## المرحلة 9 — CSS مؤجّل + حركة مركّبة (`perf/css-decompose` ضمن نفس الدفعة)

- `design-system` / `brand-v4-components` / interactions بعد `load`+idle (كانت `void import` فوريًا).
- `m2030/pages.css` بقي في المسار الحرج لتفادي وميض بطاقات الدروس.
- هياكل `ds-shimmer` / `skeleton-sweep` / `skeleton-shimmer` / `mj-skel-brightness` بحركة `transform`/`opacity` لا `background-position` ولا `filter`.
- سقف gzip الحرج ما زال 60KiB (الهدف 20KiB يحتاج تقسيم `index.css` — لم يُنفَّذ هنا حتى لا يُكسر الشكل).
- `vercel.json`: رؤوس `/assets` و`/fonts` immutable موجودة أصلًا — لا تعديل (مسار خطر إن أُضيفت سياسة جديدة).
- preconnect ≤2 و`sourcemap: "hidden"` + `strip:sourcemaps` موجودة أصلًا.

## المرحلة 10 — LHCI + ميزانية (`chore/lighthouse-ci-budget`)

- `artifacts/majalis/lighthouserc.cjs` + `budget.json` + `pnpm lighthouse:ci` (محلي/يدوي على الإنتاج).
- التأكيدات: أداء ≥0.99 · a11y=1 · BP=1 · SEO=1 · LCP≤1300ms · TBT≤150ms · CLS≤0.05 · JS≤150KiB · CSS≤20KiB · طلبات≤25.
- **لا وظيفة GitHub مطلوبة للدمج:** `.github/workflows/**` مسار خطر للدمج التلقائي. تشغيل `lighthouse:ci` الآن سيفشل حتى يصل الإنتاج للعتبة — هذا متوقَّع وليس بوابة Verify build.

**قياس إنتاج بعد نشر هذه الدفعة:** وسيط ٣ تشغيلات — يُسجَّل هنا بعد `version.json` الجديد. لا إعلان 100 قبل القياس.

**إنتاج:** `version.json` → `7c91021a` (`builtAt` 2026-08-18T06:40:27Z).

**قياس محلي Lighthouse CLI 13.4 (جوال، simulate) — وسيط ٣ تشغيلات — ليس PSI**

| | ر1 | ر2 | ر3 | **وسيط** |
|---|---|---|---|---|
| الأداء | 0.52 | 0.57 | 0.57 | **0.57** |
| إمكانية الوصول | 1.00 | 1.00 | 1.00 | **1.00** |
| أفضل الممارسات | 1.00 | 1.00 | 1.00 | **1.00** |
| SEO | 1.00 | 1.00 | 1.00 | **1.00** |
| FCP (ث) | 2.40 | 2.19 | 2.28 | **2.28** |
| LCP (ث) | 4.95 | 5.69 | 5.19 | **5.19** |
| TBT (مل.ث) | 904 | 827 | 916 | **904** |
| CLS | 0.056 | 0.057 | 0.057 | **0.057** |
| SI (ث) | 6.40 | 2.26 | 2.30 | **2.30** |

مقارنة بوسيط ما بعد #1263 (CLI نفسه): أداء 0.47→**0.57** · LCP 7.26→**5.19ث** · a11y 0.96→**1.00** · CLS 0.170→**0.057**. TBT 726→**904** (لم يتحسن في هذا القياس).

- `color-contrast`: صفر مخالفات في التشغيلات الثلاث.
- حركة غير مركّبة: لا تقرير.
- `valid-source-maps`: 1.
- CSS غير مستخدم ~46KiB (منها `design-system` ما زال يُحمَّل أثناء أثر LH) · JS غير مستخدم ~77KiB (`supabase` + `index`).
- الهدف 100 أداء / LCP≤1.2ث / TBT≤100مل.ث **لم يُبلَغ**. العائق المتبقي: عنصر LCP ما زال بعد صدفة HTML (~5ث) + مهام طويلة في حزمة الإقلاع.

## تصحيح انحدار الفحص 9 (PSI 67→50) — `perf/revert-regression`

**نافذة الفحصين (توقيت الرياض):** 01:07 ← 09:04 في 18 أغسطس 2026.

### Bisect — PRs المدموجة

| الوقت | PR | CLS (CLI وسيط) | TBT | الحكم |
|---|---|---:|---:|---|
| قبل الفحص 8 | — | 0.002 (PSI) | 270 مل.ث | الأساس |
| 05:06 | **#1263** صدفة LCP خارج `#root` | **0.170** | **726** | **المسؤول** |
| 09:05 | #1264 TBT/تكبير العنوان | (لم يُعزل) | — | amplifies TBT؛ `content-visibility` على `.hsh-steps` فوق الطية |
| 09:04 | **الفحص 9 PSI** | **0.162** | **660** | يطابق CLI بعد #1263 |
| 09:39 | #1265 تباين+CSS مؤجّل | 0.057 | 904 | خفّض CLS ولم يُرجعه |

**عناصر الإزاحة بعد #1263 (مجموع 0.169):**

| الدرجة | المحدّد |
|---:|---|
| **0.126** | `div#root` — React يملأ الشجرة تحت صدفة مطلقة |
| **0.043** | `main#main-content` |
| 0.0004 | `h1#mj-lcp-title` بعد النقل |

**أطول مهام بعد #1265 (إنتاج):** `vendor-*.js` 413+219+185 مل.ث · Unattributable 203+186 · `index-*.js` 69.

**التصحيح:** حذف الصدفة ونقل العقدة و`await homePageBoot` (عمل مزدوج). `.hsh-steps` بلا `content-visibility` فوق الطية. ارتفاع محجوز: هيكل الرئيسية 24rem · الهيرو 11rem · ورد اليوم 9rem. LHCI: CLS≤0.05 وTBT≤400 **error** (كانت ستمنع دمج #1263)؛ أداء 0.99 **warn** حتى يعود 67.

**قياس إنتاج بعد النشر:** يُسجَّل وسيط ٣ تشغيلات هنا. لا إعلان استعادة 67 قبل القياس.

**إنتاج:** `version.json` → `f77b5cd6` (`builtAt` 2026-08-18T07:14:28Z).

**Lighthouse CLI 13.4 جوال simulate — وسيط ٣ تشغيلات — ليس PSI**

| | ر1 | ر2 | ر3 | **وسيط** |
|---|---|---|---|---|
| الأداء | 0.55 | 0.54 | 0.58 | **0.55** |
| a11y / BP / SEO | 1 | 1 | 1 | **1** |
| FCP (ث) | 2.10 | 2.19 | 2.24 | **2.19** |
| LCP (ث) | 6.08 | 6.85 | 6.07 | **6.08** |
| TBT (مل.ث) | 721 | 837 | 698 | **721** |
| CLS | 0.043 | 0.042 | 0.000 | **0.042** |
| SI (ث) | 4.52 | 2.71 | 2.35 | **2.71** |

CLS: 0.170 (#1263) → **0.042**. المتبقي: `main#main-content` ≈0.037 (Home كسول يملأ الرئيسي بعد الشريط). لا عنصر >0.005 عدا `main`. TBT/LCP لم يعودا إلى فحص 8 (PSI 270مل.ث / 8.6ث). لا إعلان درجة 67 — أداة CLI ≠ PSI.

## المرحلة 11 — حجز مساحة CLS (`fix/cls-reserve-space`)

**ما تغيّر**

1. **HeaderTicker:** `Suspense fallback={null}` → `<div className="header-ticker header-ticker--empty" />` (يحجز 8rem قبل lazy).
2. **PrayerCountdownBanner:** `.pcb-wrap { position: fixed }` — لا يدفع `main` عند الظهور.
3. **HomeInitialShell:** هيكل مطابق لـ`HomeView` فوق الطية (هيرو · بحث · ابدأ من هنا · ورد اليوم) مع نصوص مشتركة من `home-start-here-data.ts` و`<Link>` بدل `<span>` للأزرار.
4. **CSS:** `contain: layout style` على `.mj-home-lcp-ph` (لا `min-height: 88rem`) · ارتفاعات محجوزة لكل قسم.

**قياس محلي Lighthouse CLI 13.4 (جوال، simulate) — وسيط ٣ تشغيلات — preview بعد build — ليس PSI**

| | ر1 | ر2 | ر3 | **وسيط** |
|---|---|---|---|---|
| الأداء | 0.56 | 0.57 | 0.62 | **0.57** |
| CLS | 0.024 | 0.024 | 0.023 | **0.024** |
| TBT (مل.ث) | 30 | 550 | 470 | **470** |
| LCP (ث) | 8.6 | 8.1 | 8.2 | **8.2** |
| FCP (ث) | 5.6 | 2.6 | 2.6 | **2.6** |

**Playwright PerformanceObserver (iPhone 13، 3.5ث، 3 تشغيلات):** CLS **0.000** (لا عناصر مُزيحة >0).

**مقارنة CLS:** 0.162 (فحص 9 PSI) → 0.042 (#1267 revert) → **0.024** (هذا PR). الهدف ≤0.01 **لم يُبلَغ** في LH؛ المتبقي ~0.02 يُنسب غالبًا لتبديل الخط/محتوى ورد اليوم الديناميكي.

**إنتاج بعد النشر:** يُسجَّل وسيط PSI هنا.

## المرحلة 12 — TBT جزر تحت الطية (`perf/tbt-islands-worker`)

**أطول المهام قبل التعديل (LH simulate، محلي):**

| المدة | المصدر |
|---:|---|
| 340 مل.ث | `vendor-*.js` |
| 204 مل.ث | `vendor-*.js` |
| 103 مل.ث | `vendor-*.js` |
| 63 مل.ث | Unattributable |
| 51 مل.ث | `vendor-*.js` |
| 50 مل.ث | `vendor-*.js` |

JS غير مستخدم: supabase 44KiB + index 35KiB.

**ما تغيّر**

1. جزيرة `HomeBelowFold` تُرطَّب عند الظهور (`IntersectionObserver`) أو idle — أيقونات lucide والودجتات خارج رسم الأولى.
2. حذف تسخين `HomePage` المزدوج من `main.tsx` وbootstrap Supabase المكرر (AuthProvider يكفي).
3. جدولة الصلاة (`usePrayerCountdown`) بعد `load` + idle.
4. hydrate/purge وCapacitor بعد idle.

**قياس محلي Lighthouse CLI (جوال، simulate) — وسيط ٣ تشغيلات — preview — ليس PSI**

| | قبل (CLS PR) | **بعد** ر1 | ر2 | ر3 | **وسيط** |
|---|---:|---:|---:|---:|---:|
| الأداء | 57 | 62 | 64 | 65 | **64** |
| CLS | 0.024 | 0.019 | 0.019 | 0.019 | **0.019** |
| TBT (مل.ث) | 470 | 416 | 361 | 3 | **361** |
| LCP (ث) | 8.2 | 7.32 | 7.32 | 7.32 | **7.32** |
| FCP (ث) | 2.6 | 2.57 | 2.57 | 4.30 | **2.57** |

أطول مهام بعد: vendor 323+207+54 · Unattributable 62. الهدف TBT≤100 **لم يُبلَغ** — العائق المتبقي تقييم React (`vendor` ~1ث scripting). JS غير مستخدم بلا تغيير (supabase 44 + index 35).

**قياس محلي — غير مرجعي.** إنتاج SHA `9954d5c6` — وسيط PSI لم يُسجَّل هنا (حصة API 429 عند كتابة بوابة LHCI).

## بوابة LHCI الإلزامية (`chore/lhci-blocking-gate`)

**هذه الدفعة لا تغيّر الواجهة.** الغرض: منع دمج انحدار (مثل CLS 0.002←0.162 وTBT 270←660←904) بفحص أحمر داخل Verify build.

**إنتاج عند كتابة هذا:** SHA `9954d5c6` (انظر منهج القياس أعلاه). محاولة PSI API في 2026-08-18 ≈13:30 UTC+3 أعادت HTTP 429 — **لا أرقام PSI في هذا الجدول**. لا إعلان درجة.

| | القيمة |
|---|---|
| الأداة (البوابة) | LHCI 0.15.1 · جوال · simulate · 3 تشغيلات على `dist` في CI |
| الأداة (الإعلان) | PSI — لم يُقَس (429) |
| SHA المرجعي | `9954d5c6` |

**عتبات حمراء (على الحالة الراهنة لا الحلم):**

| المقياس | مستوى | عتبة | دليل CI الأول (2026-08-18T10:50Z، 3 تشغيلات، بناء PR) |
|---|---|---|---|
| أداء | warn | ≥ 0.75 | وسيط 0.53–0.65 (تحذير فقط) |
| CLS | error | ≤ 0.08 | نجح (لم يظهر في الفشل) |
| TBT | error | ≤ 900ms | نجح |
| LCP | error | ≤ 8000ms | وسيط ≈7122ms — **6000 كانت ستفشل البوابة دائماً** |
| a11y / BP / SEO | error | = 1 | نجح |

أول تشغيل CI (LHCI 0.15.1، جوال simulate، `http://127.0.0.1:24216/`، ليست PSI): LCP 7114 / 7130 / 7122 مل.ث. لذلك عتبة LCP الحمراء = 8000ms لا 6000.

بعد كل تحسّن مثبت بـPSI تُخفَّض العتبة في نفس الـPR. ميزانية JS/CSS في `budget.json` توثيق فقط حتى دفعة CSS.

**ما تغيّر**

- وظيفة `lhci-home` في `.github/workflows/ci.yml` تنزّل `dist` وتشغّل LHCI على `/`.
- `Verify build` و`ci-required` يفشلان إن فشل أو تُخطّي LHCI عند وجود بناء.
- عتبات `lighthouserc.cjs` واقعية بدل 0.99 / LCP 1300 / TBT 400 التي كانت تمنع تشغيل البوابة أصلاً.

**ترتيب الدفعات التالية (مقياس واحد لكل PR):** إكمال CLS ← تقسيم vendor/TBT ← صدفة LCP v2 ← CSS حرج ← افتراض قائمة الدروس.

## خط أساس PSI فحص 10 (مرجعي — 2026-08-18 ≈14:05)

الأداة: **PageSpeed Insights جوال** على `https://majlisilm.com/`. ليست LHCI. لا تُقارَن بفحوصات CLI.

| المقياس | فحص 8 | فحص 9 | **فحص 10** | الحكم |
|---|---:|---:|---:|---|
| الأداء | 67 | 50 | **52** | ثابت تقريباً |
| CLS (٢٥٪) | 0.002 | 0.162 | **0.017** | تعافى — حجز `HeaderTicker` |
| LCP (٢٥٪) | 8.6ث | 7.7ث | **4.7ث** | تحسّن ≈3ث |
| TBT (٣٠٪) | 270 | 660 | **1,100** | تدهور متواصل |
| FCP (١٠٪) | 2.1ث | 2.1ث | **3.7ث** | انحدار مع حظر العرض |
| SI (١٠٪) | 3.5ث | 2.7ث | **3.7ث** | انحدار مع حظر العرض |
| حظر العرض | 320مل.ث | 180مل.ث | **1,610مل.ث** | انفجار ×9 — عنق الدفعة |
| a11y / BP / SEO | — | — | **100 / 100 / 100** | مثبّت |

الخلاصة: تحصين CLS نجح، وLCP نزل ثلاث ثوانٍ، والدرجة لم ترتفع لأن TBT+FCP (٤٠٪ من الوزن) انهارا مع حظر العرض.

## تحصين بوابة LHCI بعد فحص 10 (`chore/tighten-lhci-thresholds`)

**هذه الدفعة لا تغيّر الواجهة.** الغرض: شدّ العتبات الحمراء فوق المكاسب حتى لا يُدمَج انحدار يعيد CLS/LCP إلى ما قبل فحص 10.

**معايرة — الخيار (أ):** `throttling` صريح في `lighthouserc.cjs` = Slow 4G لـ PSI (`rttMs: 150` · `throughputKbps: 1638.4` · `cpuSlowdownMultiplier: 4` · `simulate`). كان هذا افتراض Lighthouse عند `formFactor: "mobile"`؛ يُثبَّت الآن كي لا يُعاد ضبطه ضمناً.

**الخيار (ب) مرفوض:** نصف أرقام PSI (LCP 2750ms) سيفشل CI دائماً لأن وسيط LHCI على `dist` ≈ **7120ms** مقابل PSI **4700ms**. الفجوة بيئية (إنتاج CDN مقابل preview محلي) لا نقص throttling.

قياس LHCI غير مرجعي (جوال، preview `dist`، وسيط 3 تشغيلات، 2026-08-18):

| | main بوابة #1272 | بناء درزة الصلاة |
|---|---:|---:|
| CLS | 0.021 | 0.017 |
| LCP | 7123ms | 7144ms |
| TBT | 443ms | متذبذب (1474 / 1405 / 294) |
| حظر عرض (`numericValue`) | 1356ms | 150 أو 1357 |
| DOM | 974 | 974 |
| unused-css `overallSavingsBytes` | ≈33KiB | ≈33KiB |
| unused-js `overallSavingsBytes` | ≈79KiB | ≈80KiB |

حظر العرض في LHCI: مورد واحد `assets/index-*.css` (~47KiB). `unused-css-rules` / `unused-javascript`: `numericValue` **مللي ثانية** لا بايت.

| المقياس | مستوى LHCI | العتبة | لماذا ليست رقم PSI حرفياً |
|---|---|---:|---|
| CLS | error | ≤ 0.030 | PSI 0.017 يمر؛ 0.08 كانت أوسع من الواقع |
| LCP | error | ≤ 8000ms | هدف الإعلان PSI 5500؛ LHCI ≈7.1ث سيفشل عند 5500 |
| TBT | error | ≤ 900ms | كما هي حتى ينخفض PSI فعلاً |
| a11y / BP / SEO | error | = 1 | لا تُخفَّض |
| DOM | error | ≤ 1200 | الواقع 974 |
| حظر العرض | error | ≤ 1800ms | سقف انحدار فوق PSI 1610؛ **الهدف 300ms في PR الحظر** |
| unused-css | warn | ≤ 80ms توفير | لا كيبيبايت — الهدف المعلن 20KiB في `budget.json` |
| unused-js | warn | ≤ 200ms توفير | الهدف المعلن 40KiB |
| forced-reflow | warn | minScore 1 | الواقع 0 — يصبح error بعد إصلاح الحجز |

**ترتيب الدفعات التالية (مقياس واحد لكل PR):** حظر العرض ← إعادة تدفق CSS ثابت ← تقسيم vendor/TBT ← (مؤجّل) صدفة LCP v2 / CSS حرج / افتراض الدروس. لا تُفتح صدفة LCP قبل TBT ≤ 600ms وCLS ≤ 0.03.

## حظر العرض — `perf/fix-render-blocking`

الأداة التشخيصية: **LHCI / HTML المُنتَج** (غير مرجعي للإعلان) على بناء محلي. المرجع للإعلان = PSI بعد النشر.

### قائمة موارد حظر العرض قبل الإصلاح

من `dist/index.html` بعد Vite (SHA العمل `8c7690c65` + تحصين العتبات):

| المورد | الحجم الخام | gzip تقريبي | دور | زمن الحظر (LHCI/PSI) |
|---|---:|---:|---|---|
| `/assets/index-*.css` | **248 874 بايت** | ≈47 KiB | `<link rel="stylesheet">` في `<head>` بلا `media` | PSI فحص 10: **1610ms** · LHCI `wastedMs` ≈908 · `numericValue` ≈1356 |
| `/fonts/ui/amiri-400-ar.woff2` | preload | — | `rel=preload as=font` — ليس stylesheet حاجباً | — |
| `/assets/index-*.js` | ≈403 KiB | — | `type=module` (مؤجَّل) | ليس حاجب رسم كلاسيكي |
| سكربت متزامن في `<head>` | — | — | غير موجود (ld+json + module فقط) | — |

لا `@import` في CSS المُسلَّم. المشتبه الذي عاد بين الفحص 9 (180ms) والفحص 10 (1610ms): **ملف `index-*.css` كاملاً كرابط حاجب** — طبقات `main.tsx` الساكنة (~fonts/theme/index/final-release/…) تُحزَم في ورقة واحدة يحقنها Vite.

### العلاج

- CSS حجز الشاشة الأولى (`critical-first-paint.css`) يُضمَّن (`#mj-cls-reserve`) — ارتفاع `--ticker-h` وكتل الرئيسية. الميزانية ≤14KiB لكل `<style>` المضمّنة.
- ورقة `index-*.css` تُحمَّل `media="print" onload="this.media='all'"` + `<noscript>`.
- عتبة LHCI الحمراء لحظر العرض: **300ms** (كانت 1800 سقف انحدار).

### المقاييس الخمسة

قبل = PSI فحص 10 (مرجعي). بعد = يُسجَّل PSI على SHA الإنتاج بعد الدمج — لا رقم CLI مكانه.

| | أداء | LCP | TBT | CLS | FCP |
|---|---:|---:|---:|---:|---:|
| PSI فحص 10 (قبل، مرجعي) | 52 | 4.7ث | 1100 | 0.017 | 3.7ث |
| PSI بعد النشر | انظر فحص 11 (قياس موحّد بعد #1280+#1281+#1282) | | | | |

هدف هذه الدفعة: حظر عرض ≤200ms وFCP ≤2.1ث على PSI. a11y/BP/SEO تبقى 100. CLS يجب ألا يتجاوز 0.030 (الحجز ثابت CSS لا قياس JS).

## إعادة التدفق الإلزامية — `fix/forced-reflow-reserve-css`

فرضية «حجز المساحة يقيس `offsetHeight` ثم يكتب»: **مرفوضة لـ HeaderTicker**. الارتفاع محجوز بـ `--ticker-h` في CSS (`final-release.css` + `#mj-cls-reserve`). لا `useLayoutEffect` ولا `offsetHeight` في `HeaderTicker.tsx`.

### مواضع من تدقيق LHCI (بناء درزة الصلاة، حزمة مصغّرة — غير مرجعي)

| المصدر | المدة | ملاحظة |
|---|---:|---|
| `vendor-*.js` col 2448 | 40ms | React — خارج نطاق هذا الـPR |
| `[unattributed]` | 61ms | — |
| `index-*.js` col 153952 | 40ms | مرشّح: `void main.offsetWidth` في `RouteEnterMotion.tsx` |
| `index-*.js` col 165691 | 0.14ms | — |

### ما تغيّر (ملف/سطر قبل الإصلاح)

1. `RouteEnterMotion.tsx` — `void main.offsetWidth` لإعادة تشغيل CSS animation. استُبدل بـ `requestAnimationFrame` (كتابة بعد إطار، بلا قراءة تخطيط).
2. `HeaderTicker.tsx` — `measure()` متزامن داخل `useEffect` يقرأ `scrollWidth`/`clientWidth` ثم `setState`. القياس أصبح داخل `ResizeObserver` فقط (أو rAF إن غاب RO).

حجز الارتفاع يبقى CSS ثابتًا. `forced-reflow-insight` يبقى **warn** في LHCI حتى يثبت PSI اختفاء التحذير.

| | أداء | LCP | TBT | CLS | FCP |
|---|---:|---:|---:|---:|---:|
| PSI فحص 10 (قبل، مرجعي) | 52 | 4.7ث | 1100 | 0.017 | 3.7ث |
| PSI بعد النشر | انظر فحص 11 | | | | |

## تقسيم vendor / TBT — `perf/tbt-vendor-split`

مقياس واحد: TBT. لا خلط مع صدفة LCP. الهدف المعلن بعد PSI: **≤ 600ms** (لا وعد بدرجة 100). عتبة LHCI كانت 900 حتى يثبت PSI — فحص 11 = 810 → صارت 850.

### أكبر 10 وحدات JS (بناء محلي، gzip level 9 — غير مرجعي)

قبل (حزمة `vendor` موحّدة + `createClient` ساكن في الإقلاع):

| # | الملف | خام | gzip |
|---|---|---:|---:|
| 1 | `fawaid-curated-seed-*` | 1345 | 132 |
| 2 | `index-*` | 393 | 120 |
| 3 | `prophetic-medicine-seed-*` | 670 | 93 |
| 4 | `fawaid-seed-*` | 461 | 63 |
| 5 | **`vendor-*`** | 187 | 59 |
| 6 | **`supabase-*`** (استيراد ساكن من `index`) | 203 | 52 |
| 7 | `nations-seed-*` | 242 | 44 |
| 8 | `IslamicGlossaryPage-*` | 247 | 42 |
| 9 | `maps-*` | 146 | 42 |
| 10 | `icons-*` | 70 | 22 |

البذور والمسارات الكسولة ليست على مسار الإقلاع. عنق TBT: `index` + `vendor` + `supabase` الساكن.

### السبب الجذري

`AdminRouteGuard` → `auth-messages` → `supabase-config` → `supabase-bootstrap` → `import { createClient } from "@supabase/supabase-js"`. الاستيراد أعلى الملف يجعل أي استخدام لـ `getEffectiveSupabaseUrl` يسحب مكتبة العميل كاملة إلى `index` رغم أن `AuthProvider` يحمّل العميل ديناميكيًا.

فرض `manualChunks` على مسارات `src/` ممنوع (يسحب commons إلى الإقلاع).

### ما تغيّر

1. نقل حالة URL/المفتاح الفعّال إلى `supabase-env.ts` — بلا `createClient`.
2. `supabase-config.ts` يستورد من `supabase-env` لا من `bootstrap`.
3. تقسيم `vendor`: `react` · `react-dom` (+ scheduler) · `wouter` — مهام تحليل منفصلة.
4. بوابة: `index` لا `from "./supabase-"` · لا حزمة `vendor-*`.

### بعد البناء المحلي (غير مرجعي)

استيراد `index` الساكن: `react` + `react-dom` + `query` + `wouter` + `icons` — **بدون supabase**.

| | gzip |
|---|---:|
| `react-*` | 3.3 KiB |
| `react-dom-*` | 54.2 KiB |
| `index-*` | 120.8 KiB |
| `supabase-*` (ديناميكي الآن) | 52.3 KiB |

`icons` ما زال ساكنًا (شريط التنقّل + `sections.registry`) — خارج هذا الـPR.

| | أداء | LCP | TBT | CLS | FCP |
|---|---:|---:|---:|---:|---:|
| PSI فحص 10 (قبل، مرجعي) | 52 | 4.7ث | 1100 | 0.017 | 3.7ث |
| PSI فحص 11 (بعد #1280+#1281+#1282) | 61 | 5.0ث | **810** | 0.045 | 1.9ث |

عتبة LHCI الحمراء لـ TBT: **900 → 850** بعد ثبات PSI تحت 900. الهدف المعلن ما زال ≤ 600ms. لا تُفتح صدفة LCP v2: TBT 810 ≰ 600 وCLS 0.045 ≰ 0.03.

## فحص 11 (PSI جوال · 2026-08-18 ≈ 20:30 GMT+3) — بعد #1280+#1281+#1282

الأداة: **PageSpeed Insights جوال** على `https://majlisilm.com/`. ليست LHCI. جولة واحدة (واجهة PSI).

- SHA إنتاج: `9e7cc2a3a736110a3981d7df784aa390b8088714` · `version.json` `builtAt` 2026-08-18T17:22:29Z · `ref` main
- التقرير: `https://pagespeed.web.dev/analysis/https-majlisilm-com/tdsw0iuvic?form_factor=mobile`
- المحرّك: Lighthouse 13.4.1 · HeadlessChromium 151.0.7922.71 `lr` · Moto G Power · Slow 4G

| المقياس | فحص 10 | **فحص 11** | الحكم |
|---|---:|---:|---|
| الأداء | 52 | **61** | +9 |
| CLS (٢٥٪) | 0.017 | **0.045** | انحدار — فوق سقف 0.03 |
| LCP (٢٥٪) | 4.7ث | **5.0ث** | ثابت تقريباً |
| TBT (٣٠٪) | 1,100 | **810** | تحسّن؛ لم يبلغ 600 |
| FCP (١٠٪) | 3.7ث | **1.9ث** | تحسّن كبير — هدف حظر العرض ≤2.1ث |
| SI (١٠٪) | 3.7ث | **3.1ث** | تحسّن |
| a11y / BP / SEO | 100 | **100 / 100 / 100** | مثبّت |
| حظر العرض | 1,610مل.ث | ليس ضمن INSIGHTS (مرشّح PASSED) | #1280 |
| forced-reflow | — | ما زال في INSIGHTS | #1281 لم يُلغِ التحذير |

تشخيص (لا يدخل الدرجة مباشرة): تنفيذ JS 2.0ث · عمل الخيط الرئيسي 4.0ث · JS غير مستخدم ≈78KiB · CSS غير مستخدم ≈36KiB · 6 مهام طويلة.

**الخلاصة:** FCP وTBT والدرجة تحسّنوا بعد دفعات الحظر/التدفق/vendor. CLS انحدر إلى 0.045 — يُعالج قبل صدفة LCP. عتبة LHCI لـ TBT صارت 850ms. الهدف التالي: TBT ≤ 600 وCLS ≤ 0.03 ثم فقط صدفة LCP v2.

## انحدار CLS — `fix/cls-deferred-shell`

مقياس واحد: CLS. لا صدفة LCP. فرضية: تحميل `index-*.css` المؤجّل (#1280) يطبّق padding/main/bottom-nav بعد الرسم الأول بلا حجز في `#mj-cls-reserve` — يزيح `#main-content` والشريط السفلي.

### السبب الجذري (تشخيص)

- `#mj-cls-reserve` كان يحجز الشريط والهيرو والرئيسية فقط (~788 بايت).
- عند `onload` لـ `index-*.css`: `.app-main` يأخذ `padding-block-end` + `padding-inline`، و`.bottom-nav` يصبح `position:fixed` — غير موجودين في الحجز.
- PSI 11: CLS **0.045** (فحص 10: 0.017) · INSIGHTS: Layout shift culprits.

### ما تغيّر

- توسيع `critical-first-paint.css`: رموز `--header-h` / `--bottom-nav-total` / `--content-pb` + حجز `.navbar-v3` · `#main-content.app-main` · `.bottom-nav` (يطابق `final-release.css` جوال ≤879px).
- الميزانية المضمّنة ما زالت ≤14KiB.

| | أداء | LCP | TBT | CLS | FCP |
|---|---:|---:|---:|---:|---:|
| PSI فحص 11 (قبل، مرجعي) | 61 | 5.0ث | 810 | **0.045** | 1.9ث |
| PSI بعد النشر | لم يُقَس بعد | | | | |

هدف هذه الدفعة: CLS ≤0.030 على PSI. لا تُفتح صدفة LCP v2 قبل TBT ≤600 وCLS ≤0.03.

## فحص 12 (PSI جوال · 2026-08-19) — بعد #1283+#1284+تحصين TBT

الأداة: **PageSpeed Insights جوال** على `https://majlisilm.com/`.

| المقياس | فحص 10 | فحص 11 | **فحص 12** | النقاط |
|---|---:|---:|---:|---:|
| **الأداء** | 52 | 58 | **70** | — |
| FCP | 3.7ث | 1.9ث | **2.0ث** | 9/10 |
| SI | 3.7ث | 3.6ث | **2.0ث** | 10/10 |
| CLS | 0.017 | 0.048 | **0.006** | 25/25 |
| TBT | 1,100 | 740 | **330** | 22/30 |
| **LCP** | 4.7ث | 5.7ث | **5.7ث** | **4/25** |
| a11y / BP / SEO | — | — | **100 / 100 / 100** | — |

- اختفى تحذيرا **حظر العرض** و**إعادة التدفق الإلزامية**.
- العمل الرئيسي: 3.6ث → **2.3ث**.
- **الحساب الحاسم:** الناقص 30 نقطة — **21 في LCP** و8 في TBT. أي دفعة لا تمسّ LCP = دفعة ضائعة.

### عتبات LHCI v3 (`chore/lock-thresholds-v3`)

| المقياس | عتبة حمراء | الواقع فحص 12 |
|---|---:|---:|
| CLS | ≤ 0.020 | 0.006 |
| TBT | ≤ 400ms | 330 |
| FCP | ≤ 2200ms | 2000 |
| SI | ≤ 2500ms | 2000 |
| LCP | ≤ 6000ms | 5700 (يُخفَّض بعد v2) |
| حظر عرض | ≤ 200ms | PASSED |

### الدفعة التالية: `perf/lcp-static-shell-v2`

- عنصر LCP المرشّح: `p.hsh-step__desc` (تأخير رسم ~3.7ث بعد FCP).
- صدفة ثابتة **داخل `#root`** بمطابقة بكسل + بوابة CLS ≤ 0.02.
- الويب: لا دخولية حاجبة؛ iOS: LaunchScreen أصلية.
- الهدف: LCP ≤ 2500ms · فارق FCP–LCP ≤ 600ms.

### `perf/lcp-static-shell-v2` — ما تغيّر

1. **صدفة HTML داخل `#root`** (`#mj-home-lcp-static`) — نص `p.hsh-step__desc` الحقيقي + hero قبل أي JS.
2. **React يركّب في `#mj-app-mount`** — لا يمسح الصدفة حتى `scheduleRemoveHomeLcpStaticShell()` بعد رسم `.m2030-home`.
3. **الويب: لا دخولية حاجبة** — `if (!native) dismiss(true)` في `index.html`؛ iOS يبقي LaunchScreen.
4. **خط:** `preload` Amiri 400 ar + `ascent-override`/`MajlisAmiriFallback` في `#mj-cls-reserve`.
5. **بوابة CLS:** LHCI `≤ 0.02` في نفس PR (`chore/lock-thresholds-v3`).

| | LCP | CLS | FCP | TBT |
|---|---:|---:|---:|---:|
| PSI فحص 12 (قبل) | 5.7ث | 0.006 | 2.0ث | 330 |
| PSI بعد v2 | **لم يُقَس بعد** | | | |

**عنصر LCP المرشّح (فحص 12):** `p.hsh-step__desc` — «دروس علمية أسبوعية من علماء الكويت…» — تأخير رسم ~3.7ث (FCP 2.0 → LCP 5.7).

---

## الجولة الختامية A-1/A-4 (2026-08-20)

### أ-1 — LHCI شاهد main (`ef431e46d`) vs #1286 (`a22983dbb`) · run `32307284830` / `32309146074`

| القياس | main (شاهد) | #1286 | الفرق |
|---|---:|---:|---:|
| **CLS** (وسيط 3 جولات) | **0.0212** | **0.1020** | +0.081 |
| **LCP** (وسيط) | **7056ms** | **7139ms** | +83ms |

**تصنيف:** CLS = **منطقي** (صدفة `#mj-home-lcp-static` + `#mj-app-mount` → إزاحة 0.016). LCP = **بيئي/خط أساس** (main أيضاً ~7.1ث؛ عنصر LCP الفعلي = `p.hsh-lead` في React لا الصدفة).

**layout-shifts #1286:** aref-ruqaa 0.030 · `#mj-app-mount` 0.016 · main-content 0.004.

### أ-4 — قرار: **إسقاط الصدفة**

بعد جولتَي تحليل (شاهد + تقرير Lighthouse): الصدفة ترفع CLS بلا تحسين LCP. **مهمة محجوزة:** `perf/lcp-static-shell-v3-hydrate` — ترطيب React داخل `#root` بلا mount منفصل.

**إصلاح CLS بدل الصدفة:**
- حجز `min-height` لـ`.hsh-steps` / `.hsh-step` في CSS الحرج
- `font-display: optional` لـ Aref Ruqaa (مصدر 0.030 CLS)
- بوابة `cls-home-gate.test.ts`

**LCP:** `inject-home-chunk-preload.mjs` — modulepreload لحزمة HomePage بعد البناء.

### CI `32313078420` (`175f4886a`) — بعد إسقاط الصدفة

| القياس | قبل (صدفة) | بعد | حد v3 |
|---|---:|---:|---:|
| CLS | 0.054–0.102 | **0.0176** | ≤0.020 ✅ |
| LCP | 7128ms | **5212–5780ms** | ≤6000 ✅ |
| FCP | — | 4082–4415ms | ≤2200 ❌ |
| SI | — | 4082–4415ms | ≤2500 ❌ |

**FCP/SI:** main الشاهد أيضاً ~4100ms — فجوة LHCI↔PSI (PSI فحص 12: 2.0ث). بذرة FCP HTML → CLS **0.358** (run `32313670686`) — **مُلغاة**.

**مهمة محجوزة:** `perf/fcp-si-lhci-calibration` — معايرة FCP/SI لبيئة المعاينة أو قياس PSI post-deploy فقط.

---

## معايرة LHCI↔PSI — الخيار ١ (2026-08-20)

**قرار:** فصل بيئتي القياس. الهدف 2200/2500 **لم يُخفَّض** — نُقل إلى بوابة PSI على الإنتاج.

### مصدر الاشتقاق

| الحقل | القيمة |
|---|---|
| **Run شاهد** | `32307284830` |
| **SHA** | `ef431e46d` (main) |
| **البيئة** | LHCI preview (`127.0.0.1:24216`) |
| **CLS وسيط** | 0.0212 |
| **LCP وسيط** | 7056ms |
| **FCP/SI** | ~4100ms |

### عتبات LHCI للمعاينة = main + 10%

| المقياس | main | +10% | مستوى |
|---|---:|---:|---|
| FCP | 4100ms | **4510ms** (≈4500) | **error** |
| SI | 4100ms | **4510ms** (≈4500) | **error** |
| LCP | 7056ms | **7762ms** | error |
| CLS | 0.0212 | **0.0234** | error |

**ملفات:** `config/lhci-main-baseline.json` · `scripts/lhci-thresholds.cjs` · `scripts/verify-lhci-threshold-calibration.mjs`

**قاعدة دائمة:** أي عتبة LHCI تُشتق من main — لا تُنسخ يدوياً من PSI. البوابة تفشل إن كانت العتبة أشدّ من قياس main.

### أهداف PSI على الإنتاج (فحص 12 — لم تتغيّر)

| المقياس | الهدف | الواقع فحص 12 |
|---|---:|---:|
| FCP | ≤ **2200ms** | 2000 |
| SI | ≤ **2500ms** | 2000 |
| LCP | ≤ 6000ms | 5700 |
| CLS | ≤ 0.020 | 0.006 |
| TBT | ≤ 400ms | 330 |

**بوابة:** `scripts/verify-psi-production-gate.mjs` — بعد النشر + `curl version.json` + PSI فحص 13. **رجوع** إن FCP/SI أسوأ من فحص 12.

### layout-bands (عائق منطقي — لا يُعايَر)

`.mm-ayah-line { justify-content: space-between; }` — بوابة `test:mushaf-layout-bands`.

### مسار التسليم

1. ✅ layout-bands + معايرة LHCI (هذا PR)
2. دمج #1286 → نشر → `curl version.json` → PSI فحص 13
3. `perf/lcp-static-shell-v3-hydrate` — تحسين إقلاع ~2ث (جولة منفصلة، لا تحجب التسليم)
4. بعد #1286: #1287 → #1288 → التفسير → لقطات المصحف → إغلاق #1257/#1232 (≤3 PRs مفتوحة)

## دفعة المراحل الأربع + تدقيق المحتوى (2026-08-24)

| المرحلة | PR | SHA على main | ملاحظة |
|---|---|---|---|
| تجاوب كل الأجهزة | #1391 | a314b85f | breakpoints + test:responsive-overflow |
| إعادة بناء الفقه | #1392 | a3dd5c2af | registry + ملء أبواب |
| تلاوة AI لحظية | #1394 | fff92c511 | تفعيل + تسخين WS + بوابة كمون |
| حصاد حسابات | #1395 | a94ba015 | تتبّع ناقص فقط |
| تدقيق محتوى | #1396 | 78d44f87 | 80 فقه + 17 تاريخ + سيرة |
| أحرف سبعة | #1393 | قيد الدمج بعد rebase | كان متعارضًا مع package.json |

إنتاج عند التسجيل: `version.json` = 78d44f87 (قبل دمج الأحرف).
