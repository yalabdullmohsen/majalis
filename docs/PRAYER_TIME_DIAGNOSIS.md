# تشخيص مواقيت الصلاة والإشعارات

تاريخ التقرير (UTC): 2026-08-17T16:19:37.837Z
اليوم في Asia/Kuwait: **2026-08-17**

## 0 — البيئة

```
Intl timeZone (الجهاز): Asia/Kuwait
getTimezoneOffset(): -180 (دقائق؛ سالب = شرق UTC)
Asia/Kuwait الآن: 19:19:37
```

> ملاحظة: `LocalNotifications.getPending()` لا يعمل في Node — يُنفَّذ على الجهاز.
> هنا نحاكي ما يبنيه `prayer-alert-scheduler` / `prayer-local-notifications` / `adhan-scheduler`.

## 1 — مواقيت 2026-08-17 جنباً إلى جنب

| المصدر | الفجر | الشروق | الظهر | العصر | المغرب | العشاء |
|---|---|---|---|---|---|---|
| adhan-js · ظهر تقويمي Kuwait (UTC+3) | 03:52 | 05:17 | 11:52 | 15:27 | 18:27 | 19:49 |
| adhan-js · ظهر الجهاز المحلي (new Date y,m,d,12) | 03:52 | 05:17 | 11:52 | 15:27 | 18:27 | 19:49 |
| adhan-js · new Date() الآن (مسار العدّاد الحالي) | 03:52 | 05:17 | 11:52 | 15:27 | 18:27 | 19:49 |
| AlAdhan API method=9 school=0 | 03:52 | 05:17 | 11:52 | 15:28 | 18:27 | 19:49 |
| مرجع خارجي (تجهيزة رسمية تقريبية) | 03:52 | 05:17 | 11:52 | 15:28 | 18:27 | 19:49 |
| staticPrayerFallback (صيفي) | 04:00 | 05:30 | 12:05 | 16:35 | 19:10 | 20:35 |

### فروقات المغرب (دقيقة؛ موجب = المصدر أبطأ من ظهر Kuwait)

- جهاز محلي مقابل ظهر Kuwait: **0**
- AlAdhan مقابل ظهر Kuwait: **0**
- fallback صيفي مقابل ظهر Kuwait: **43**
- adhan ظهر Kuwait مقابل التجهيزة الرسمية: **0**

**العطل (ب) ثابت في هذا اليوم:** صفّان أو أكثر يختلفان.

## 2 — محاكاة إشعار المغرب (2026-08-17)

```
مغرب محسوب (ظهر Kuwait): 18:27
epochForSlot(مغرب) ≈ 2026-08-18T15:27:00.837Z (Asia/Kuwait 18:27)
pre الصحيح (−15): 2026-08-18T15:12:00.837Z → 18:12
pre بإشارة خاطئة (+15): 2026-08-18T15:42:00.837Z → 18:42
adhan-scheduler wrap عند advDelay<0: wrapped=true fireAt=2026-08-18T15:12:00.837Z (18:12)
```

### قراءة الإشارة

- في `prayer-local-notifications.ts` الإشارة **سالبة** اليوم: `prayerTimeEpochMs - preAlertMinutes`.
- في `adhan-scheduler.ts` عند `advDelay < 0` يُلفّ إلى **+24 ساعة** بدل تخطي نافذة اليوم — إن فُتحت النافذة بعد وقت التنبيه المسبق يُجدول لغدٍ.
- نص «باقي ربع ساعة» **ثابت** في القوالب (`pre-15`) ولا يُشتق من الفارق الفعلي عند الإطلاق.

### سيناريو الشاهد (إشعار بعد المغرب ~١٥ د)

- إن كان المغرب المجدول = الرسمي + 0 د، فالتنبيه المسبق (−١٥) يقع عند الرسمي + -15 د.
- إن كان المجدول مطابقاً والرسمي 18:27 والتنبيه وصل ~18:42 فهذا يطابق **إشارة +١٥** لا −١٥.
- إن استُخدم fallback الصيفي (مغرب 19:10): التنبيه المسبق عند 18:55 — أي **بعد** مغرب 18:27 بـ~٢٨ د (قريب من وصف المستخدم).

## 3 — مسارات الحساب في الشيفرة (جرد)

| المسار | الملف | المستهلك |
|---|---|---|
| `computePrayerTimesForDate` / `fetchPrayerTimes` (adhan-js + كاش) | `prayer-times.ts` | العدّاد، اللوبي، المجدول، الإمساكية السنوية |
| `refreshPrayerTimesInBackground` → Supabase / `/api/prayer-times` / AlAdhan | `prayer-times.ts` | قد **يستبدل الكاش** بأرقام مختلفة دون إعادة جدولة فورية |
| `staticPrayerFallback` | `prayer-times.ts` | عند فشل الحساب — مغرب صيفي 19:10 |
| `startPrayerAlertScheduler` → Capacitor LocalNotifications | `prayer-alert-scheduler.ts` | إشعارات أصلية |
| `startAdhanScheduler` → مؤقّتات JS + SW | `adhan-scheduler.ts` | أذان + تنبيه مسبق ويب |

## 4 — آخر جدولة (من التخزين — إن وُجد في بيئة المتصفح فقط)

مفتاح `majalis-prayer-schedule-status-v1` عبر `prayer-schedule-status.ts`.
في Node: غير متاح. على الجهاز اطبع `loadPrayerScheduleStatus()` والمحافظة وطريقة الحساب.

## 5 — خلاصة قبل الإصلاح (حقائق)

1. يوجد أكثر من مصدر مواقيت قابل للاختلاف (adhan محلي، AlAdhan، Supabase، fallback) — **العطل (ب) ممكن ومُلاحَظ بين fallback/AlAdhan والجهاز.**
2. بناء `Date` لـ adhan-js من مكوّنات الجهاز المحلي يختلف عن ظهر تقويمي Kuwait عندما تختلف منطقة الجهاز — مرشّح جذري لـ(ب).
3. إشارة التنبيه المسبق في المسار الأصلي **سالبة** في الشيفرة الحالية؛ إن طابق الشاهد +١٥ فالمصدر إمّا مسار آخر/نسخة قديمة، أو مواقيت مجدولة متأخرة عن الرسمي، أو fallback.
4. لفّ `advDelay < 0` إلى الغد في `adhan-scheduler` عطل مستقل يُسقط تنبيه اليوم داخل النافذة.
5. النص الثابت «باقي ربع ساعة» يخفي أي انحراف عند الإطلاق.

---

## 6 — بعد الإصلاح (2026-08-17)

### ما تغيّر

1. **مصدر مواقيت واحد:** `getPrayerTimes` / `computePrayerTimesForDate` عبر `calendarNoonInZone`؛ التحديث الخلفي يعيد الحساب محليًا فقط (لا Supabase/AlAdhan يستبدل الكاش).
2. **`epochAtZoneMinutes`:** إزاحة الكويت UTC+3 صحيحة (`3 * 3600_000` لا `3 * 60_000`).
3. **إشارة pre:** تبقى `prayerAt − minutes`؛ في `adhan-scheduler` أُزيل لفّ `advDelay < 0` إلى +24س.
4. **نص الإشعار:** `buildScheduledPrayerNotificationCopy` يحمل وقت الصلاة (`المغرب ٦:٢٧ م · بعد ١٥ دقيقة` / `أذان المغرب`).
5. **نظافة:** `cancelAll` قبل كل جدولة، معرّفات `hash(prayer, dateISO, kind)`، نافذة ≤30، إعادة جدولة عند تغيّر المنطقة/التاريخ.
6. **حارس:** `prayer-notification-guard` يمنع pre بعد دخول الوقت وenter بعد >5 د.

### مواقيت بعد الإصلاح (adhan-js Kuwait · ظهر تقويمي)

| المصدر | الفجر | الشروق | الظهر | العصر | المغرب | العشاء |
|---|---|---|---|---|---|---|
| getPrayerTimes / العدّاد / المجدول | 03:52 | 05:17 | 11:52 | 15:27 | 18:27 | 19:49 |
| تجهيزة رسمية (AlAdhan method=9) | 03:52 | 05:17 | 11:52 | 15:28 | 18:27 | 19:49 |

فارق العصر ≤1 دقيقة — ضمن الحد. **fallback الصيفي لم يعد يُكتب فوق الكاش من الخلفية.**

### محاكاة pre المغرب بعد الإصلاح

```
مغرب: 18:27 → pre (−15): 18:12  |  enter: 18:27
إشارة +: مرفوضة في الاختبارات
```

### اختبارات

`pnpm --filter @workspace/majalis run test:prayer-notif-timing` وضمن `test:ios-notifications`.

> اختبار جهاز حقيقي (pre قبل المغرب ١٥ د + أذان عند الدخول) يبقى مطلوبًا لمعايير القبول الكاملة — Node يحاكي الجدولة فقط.

---

*وُلِّد آلياً بواسطة `artifacts/majalis/scripts/diagnose-prayer-times.mjs` — لا تعديل مواقيت يدوي في هذا الملف.*
